# Verification completion matrix (v0.4)

Maps each numbered item under **Verification scope (remaining)** in [`Solver-Status.md`](Solver-Status.md) to goals, what already exists in-tree, acceptance, effort, blockers, and a single next PR slice. **Status is not “done”** unless the acceptance row is satisfied on default CI (or the cited ship artefacts exist with gates passing); several rows remain **open** per that document.

Cross-links: [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md) (lane → memo → tests), [`Solver-Status.md`](Solver-Status.md) solver-lane sections for expanded criteria.

| # | Goal | Current shipped hooks (paths / tests) | Exact acceptance criterion | Effort | Blocker | Next concrete PR slice |
| --- | --- | --- | --- | --- | --- | --- |
| **1** | Striatus / topology shell: full B6 harness at **40×40×4**, Track L committed artefacts, Track B8 “all of”. | **Sibling repo** `umst-concrete-cartridge/`: `crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs` (`shell_topology_rib_pattern_full_v04`), `tests/shell_demo_smoke.rs`, `examples/optimize_shell_3d.rs`; artefact paths under `notebooks/_artifacts/` per [`Solver-Status.md` § Topology / shell](Solver-Status.md#solver-lane-topology-shell); `notebooks/tests/test_print_ready.py`, `notebooks/export_print_ready.py`. | **`shell_topology_rib_pattern_full_v04`** at **40×40×4**, **200** iters, seed **42**: end VF **±1%** of `target_vf=0.15`; greyness **mean(4ρ(1−ρ)) < 0.15**; spatial variance of **ρ** over **xy** **> 0.1**; end compliance **<** iter-1 compliance **×0.6**. **B8:** `shell_demo_smoke` passes; rib GIF objective backup; **`striatus_shell_v0.4.print_ready.json`** with **genus ≥ 1**, **variance ≥ 0.1**, **volume_fraction ∈ [0.10,0.25]**. **Track L:** GIF **≤5 MB**, **≥30 frames**; STL **≤8 MB**, watertight, **genus ≥ 1**, **volume_fraction ∈ [0.10,0.25]** — committed under stated paths. | **L** | Documented **NaN / bar-network CG conditioning** at full grid; may need `E_min`, PCG caps, or env-gated iteration budget (`UMST_SHELL_RIB_FULL_ITERS`); artefacts may be missing or v0.3-named until regenerated. | Fix CG / SIMP stability for **40×40×4** (or document env-gated proof + CI split), land **`shell_topology_rib_pattern_full_v04`** green, run Track L regen command from `Solver-Status.md`, commit **`striatus_emergence.gif`**, **`striatus_shell_v0.4.stl`**, **`striatus_shell_v0.4.print_ready.json`** (+ optional `.obj`), wire **`test_print_ready.py`** on CI for B7/B8 fields. |
| **2** | Q1 hex plate mechanics: move from **ratio band** toward **Kirchhoff thin-plate target** in v0.4 brief (not only locked-band ratio). | `tests/verification/mechanics_analytic.rs` — **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`**; implementation context `src/physics/q1_hex_elasticity.rs` (rustdoc cites test); features **`mechanics-voigt-cauchy`**, **`topology-density-evolution`** (Q1 hex path). | **Within-5% (or brief-stated) centre deflection vs Kirchhoff SSSS reference** on the brick path the brief targets — replacing reliance on **ratio band** alone for “ship” language; follow-up [§R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate). | **M** | Shear locking on Q1 hex; SRI / enrichment path not yet meeting thin-plate error gate; “full Kirchhoff SSSS on brick not yet covered by default CI” per main table. | Implement SRI (or brief-approved stabilization), add **default-CI** test **`plate_centre_deflection_kirchhoff_within_X_percent`** (name TBD) with explicit **L/t** and mesh documented next to **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`**; keep ratio test as regression unless brief retires it. |
| **3** | Fracture / AT2: Γ-type and coupling depth — driven **ψ⁺**, broader **(l₀,h)**, within-step THMC **u↔d**, embedding / `matrix_features` when no bar solve. | `tests/verification/fracture_gamma_convergence.rs`, `staggered_fracture_mechanics_chain.rs`, `staggered_ud_loop_milestone.rs`, `thmc_drying_shrinkage.rs` (`bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`); memo [`docs/research/v0.4_track12_staggered_fracture_mechanics.md`](research/v0.4_track12_staggered_fracture_mechanics.md) §2.3 / §7 (proposed test names). | Memo **§7** style: (1) Γ-type or dissipation harness with **ψ⁺ ≠ 0** and/or staggered bar loading; (2) THMC SI path + optional **within-step** **u↔d**; (3) energy monotone band on benchmark chain; broader **(l₀,h)** than fixed **h/l₀=¼** triple; non-embedding / **`matrix_features`** fallback documented + tested. | **L** | Today’s Γ certificates largely **ψ⁺=0**; damage at step entry for stagger; no full within-step alternation in `ThmcSolver::step` contract. | One PR: add **single** new `#[test]` from memo §7 (e.g. driven **ψ⁺** Γ extension **or** one extra **(l₀,h)** schedule) with numeric bands copied from §7; second PR slice: spec + test stub for **`matrix_features`** / no-bar path **without** claiming full THMC stagger closure. |
| **4** | Acoustics: resolve **n=128** return-map vs brief vs default CI (**n=64**, **n=100** gates). | `tests/verification/acoustics_plane_wave.rs`: CI **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`**; **`plane_wave_return_map_n128_documented_phase_slip_band`** (`#[ignore]`); energy **`undamped_energy_drift_under_half_percent_over_1000_steps`** at n=128; `newmark_acceleration_matches_dense_reference_*` in same file; `AcousticNewmarkBar1dPeriodic` rustdoc in `src/physics/solvers/acoustics.rs`. | **Either** (a) relative **L²** to `u₀` **< 2%** after one semi-discrete period at **n=128** with justified **Δt**/substep recipe **and** promote strict gate / narrow ignore **or** (b) **explicit brief + CI revision** of mesh count with updated gates. Policy in `Solver-Status.md`: do **not** un-ignore n=128 until (a) or (b). | **M** | Diagnostic: substep refinement ×16 inside **T** does not fix **~1.4** relative L² — likely **spatial dispersion / period definition** vs continuum **ω_h** (see follow-up §R2.4). | PR: implement **discrete-dispersion-aligned period** (eigenpair / discrete harmonic) **or** document brief change + adjust **`plane_wave_return_map_n*`** naming/comments; add one **strict** gate matching chosen policy; shrink ignored bracket only after strict gate exists. |
| **5** | Electrochemistry: λ_D gates on default CI; general-graph implicit Newton; variable **ε** on non-chain relaxers; large-\|zΔφ\| **f32** robustness. | `tests/verification/pnp_debye_layer.rs`: **`debye_screening_256_cells_phi_25mv_decay_length_within_band`**, **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** (`#[ignore]`); CI **`debye_implicit_dispatch_short_horizon_smoke`**, **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`**; dispatch **`solve_pnp_step_dispatch`** + **`pnp_implicit_newton_chain`** (MVP chain). Memo [`docs/research/v0.4_track14_implicit_newton_pnp.md`](research/v0.4_track14_implicit_newton_pnp.md). | (1) Drop **`#[ignore]`** on **25 mV** λ_D gate with **~12k** steps horizon within CI budget; (2) extend to **100 mV** sibling; (3) Newton residual ↓ on **non-chain** benchmark or matrix-free JVP; (4) mass band on closed graph; (5) DP option for large \|zΔφ\| — per lane “Next PR acceptance” in `Solver-Status.md`. | **L** | Dense FD Jacobian at **N=256** too costly; example run: **λ_eff** vs **λ_D** **O(1)** wrong until discrete screening / fit-window calibration; variable **ε** on Jacobi relaxers not done. | PR: **sparse or analytic Jacobian block** for Poisson–NP chain (or reduced model) so **25 mV** test completes in CI; **no** claim of full graph Newton until (3) test exists. |
| **6** | Photonics: production **2D/3D** DEC vector **curl–curl**; tensor permittivity; tighter **Fresnel** calibration. | `tests/verification/photonics_fresnel.rs`, `photonics_curl_curl_stub_default_build.rs`; `tests/dec_identities.rs` (triangle / quad-split DEC annihilation + adjoint — **not** `PhotonicsSolver::solve_maxwell_curl_curl`); memo [`docs/research/v0.4_track15_dec_curl_curl_photonics.md`](research/v0.4_track15_dec_curl_curl_photonics.md). | (1) Tighten **`two_half_spaces_fresnel_te_no_pml_matches_analytic`** LS margin **or** discrete **r_disc**-only check; (2) metric-weighted DEC **and/or** **cube** patch from assembled topology feeding **`solve_maxwell_curl_curl`**; (3) tensor **ε** path beyond scalar node **ε_r**. | **L** | Today: 1-D TE chain reductions + stub default build; DEC geometry tests decoupled from photonics solve. | PR: **metric-weighted** `d₁` adjoint / curl on assembled **2-face** or **cube** slice **wired into** one new photonics integration test (single patch), plus Fresnel margin delta documented in test. |
| **7** | Rheology: **true pressure Poisson** (or MAC staggered) so developed-channel tests leave **`#[ignore]`**. | `tests/verification/rheology_poiseuille.rs`: **`chorin_surrogate_poisson_amplification_regression_guard`** (CI), **`chorin_steady_channel_64x16_vs_regularized_reference`** (`#[ignore]`), **`chorin_developed_channel_centreline_vs_regularized_reference`** (`#[ignore]`); **`channel_lattice_64x16_quadrilateral_edge_count`**; `src/physics/solvers/rheology_flow.rs` surrogate Poisson (Richardson on tangent-projected gradient). | (1) Centreline **L²** vs analytic **< 15%** at stated **dt** / steps on **64×16**; (2) pressure Poisson RHS aligned with discrete **div u***; (3) optional env long benchmark — per lane; surrogate amplification **not** ~10³/step to NaN on **65×17**. | **L** | Surrogate Poisson **dt-independent** blow-up on developed channel; **`chorin_surrogate_poisson_amplification_regression_guard`** documents current band until replaced. | PR: replace surrogate step with **discrete Poisson solve** + BCs on same stencil, re-run **`chorin_steady_channel_64x16_vs_regularized_reference`**; adjust or retire **`chorin_surrogate_poisson_amplification_regression_guard`** once real solve lands. |
| **8** | THMC: **monolithic** implicit Newton on fully coupled unknowns; optional within-step **u↔d** stagger. | `tests/verification/thmc_drying_shrinkage.rs` — implicit **(T,α)** only: **`thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`**, **`thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm`**, multi-Newton monotone, **`thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split`**, drying/shrinkage bands; assembler trial in related paths; memo [`docs/research/v0.4_track13_monolithic_newton_thmc.md`](research/v0.4_track13_monolithic_newton_thmc.md). | **‖R‖** down across inner Newton on **stacked** THMC unknowns (not only **(T,α)** block); `tol` early exit; linearized thermal vs analytic under **full** implicit map once assembled; optional **u↔d** stagger spec’d in memo — per lane “Next acceptance” in `Solver-Status.md`. | **L** | Split step: humidity does not branch on implicit **(T,α)**; **`update_damage`** once after outer loop; no dense Jacobian for full stack at scale. | PR: define **residual vector** for smallest coupled chain **(T,h,α,u)** (or memo subset), **dense FD Jacobian ≤64 DOFs**, one test **`thmc_monolithic_residual_newton_lowers_norm_two_nodes`** (name TBD) — no production-scale Krylov until this passes. |
| **9** | Statistical mechanics: **virial / coexistence** route into **`upscale_potentials`** (Burn tensor), not only Johnson scalar side-car. | `tests/verification/statmech_lj_bridge_contract.rs`, `statmech_lj_johnson_eos_reference.rs`, `statmech_vinet_eos.rs`; optional feature **`statistical-mechanics-johnson-reference`**; memo [`docs/research/v0.4_track16_virial_lj_bridge.md`](research/v0.4_track16_virial_lj_bridge.md); lane table in `Solver-Status.md` (**[B,2]** vs state **(ρ*,T*)** gap). | (1) Reference **K** from MD or published EOS at one state; (2) **`upscale_potentials`** error vs reference **< stated tolerance**; (3) document **T** / cutoff dependence — per lane. | **L** | Tensor rows today **(ε,σ)** only; Johnson **`f64`** reference not inside Burn **`upscale_potentials`** without API extension (**[B,4]** or parallel state tensor). | PR: extend batch contract (**documented** columns for **(ρ*,T*)** or parallel state tensor), route one Johnson or tabulated virial evaluation into **`upscale_potentials`** behind feature flag, add **`upscale_potentials_matches_johnson_at_reference_state`** (single point, tight tol). |
| **10** | Mechanics **transient** 3D solid stack (**M ü + C u̇ + Ku = f**) on mechanics graph **and** **contact / friction** — not quasi-static bar-network only. | Quasi-static: **`VectorMechanicsSolver::solve_equilibrium`** — `tests/verification/mechanics_analytic.rs`, `adjoint_compliance_analytic.rs`; **scalar** bar Newmark: **`tests/verification/acoustics_plane_wave.rs`** (**`acoustics-newmark`**), **not** vector 3D solid elasticity on mechanics graph. Memo [`docs/research/contact_mechanics_scope.md`](research/contact_mechanics_scope.md). | **Either** ship **default-CI** tests for **transient vector** mechanics on stated graph DOFs with **Newmark/generalized-α** and analytic or manufactured solution **or** explicitly scope “not in v0.4” in brief + README; **contact**: constraints + Coulomb friction with verification tests — until then, docs must **not** claim unified transient/contact stack. | **L** | No **`M ü + C u̇ + Ku`** on general 3D solid DOFs in one verification story; acoustics bar is **scalar** 1-D periodic operator; no contact constraints in shipped stack. | PR slice **A (docs honesty):** README + `Solver-Status` table footnote tying **`acoustics-newmark`** to **scalar bar** only. **PR slice B (engineering):** single **2-node** transient mechanics chain with diagonal **M,K** and analytic mode — one ignored or fast test proving wiring before full graph. |

---

### How to use this matrix

- **Effort:** **S** ≈ single focused test or doc PR; **M** ≈ one subsystem change with tests; **L** ≈ multi-file / API / performance work spanning milestones.
- **Hooks** are what exists **today** to anchor claims; they do **not** imply the acceptance row is met.
- Regenerate path integrity after editing [`Solver-Status.md`](Solver-Status.md): `python3 scripts/check_solver_status.py --check-paths` (from `umst-manifold/`).
