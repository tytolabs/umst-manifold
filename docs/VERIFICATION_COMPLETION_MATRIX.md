# Verification completion matrix (v0.4)

Maps each numbered item under **Verification scope (remaining)** in [`Solver-Status.md`](Solver-Status.md) to goals, what already exists in-tree, acceptance, effort, blockers, and a single next PR slice. **Status is not “done”** unless the acceptance row is satisfied on default CI (or the cited ship artefacts exist with gates passing); several rows remain **open** per that document.

Cross-links: [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md) (lane → memo → tests), [`Solver-Status.md`](Solver-Status.md) solver-lane sections for expanded criteria.

| # | Goal | Current shipped hooks (paths / tests) | Exact acceptance criterion | Effort | Blocker | Next concrete PR slice |
| --- | --- | --- | --- | --- | --- | --- |
| **1** | Topology / shell (**Tracks B + L**): run **`shell_topology_rib_pattern_full_v04`** at **40×40×4** (documented **NaN** / bar-network **CG conditioning**); commit Track L artefacts (`striatus_emergence.gif`, `striatus_shell_v0.4.stl`, `striatus_shell_v0.4.print_ready.json`, optional `.obj`) and satisfy Track B8 — regen command + paths in **Solver lanes — Topology / shell**. | **Sibling repo** `umst-concrete-cartridge/`: `crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs` (`shell_topology_rib_pattern_full_v04`), `tests/shell_demo_smoke.rs`, `examples/optimize_shell_3d.rs`; **topology–mechanics inner loop (manifold crate):** `src/physics/adjoint.rs` (`AdjointCompliance::forward_and_loss` → stiffness from `SimpElasticMaterial` → `VectorMechanicsSolver::packed_bar_network_equilibrium` in `src/physics/mechanics.rs`) — **f32** projected PCG capped at **`min(max_cg_iterations, 3N)`**, early exit when **‖P(f−Ku)‖₂ ≤ max(pcg_tolerance, cg_tolerance)·‖Pf‖₂**, optional Jacobi preconditioner; `src/physics/topology_filter.rs` (`HelmholtzFilter::apply` respects caller iteration count — no longer forces a **400**-step minimum over a **240**-step caller); in-loop **`VolumeProjection`** bisection on AD can hit Burn scatter limits — **`optimize_shell_3d` / smoke defaults:** **`UMST_SHELL_SELF_WEIGHT=0`** (traction-only while **f32** bar PCG is hardened), **`UMST_SHELL_VOL_LOOP=0`** (terminal-only projection unless **`UMST_SHELL_VOL_LOOP=1`**), **`UMST_SHELL_PCG=1`**, Track B6 **`E_min = 10⁻³ E₀`** at **40×40×4**; optional **`UMST_SHELL_HELM=0`**, **`UMST_SHELL_RIB_FULL_ITERS`** for shortened ignored harness; artefact paths under `notebooks/_artifacts/` per [`Solver-Status.md` § Topology / shell](Solver-Status.md#solver-lane-topology-shell); `notebooks/tests/test_print_ready.py`, `notebooks/export_print_ready.py`. | **`shell_topology_rib_pattern_full_v04`** at **40×40×4**, **200** iters, seed **42**: end VF **±1%** of `target_vf=0.15`; greyness **mean(4ρ(1−ρ)) < 0.15**; spatial variance of **ρ** over **xy** **> 0.1**; end compliance **<** iter-1 compliance **×0.6**. **B8:** `shell_demo_smoke` passes; rib GIF objective backup; **`striatus_shell_v0.4.print_ready.json`** with **genus ≥ 1**, **variance ≥ 0.1**, **volume_fraction ∈ [0.10,0.25]**. **Track L:** GIF **≤5 MB**, **≥30 frames**; STL **≤8 MB**, watertight, **genus ≥ 1**, **volume_fraction ∈ [0.10,0.25]** — committed under stated paths. | **L** | **Adjoint / PCG / AD conditioning:** non-finite **`u`** or edge elongations **`δ_e`** poison raw compliance and the discrete-adjoint surrogate; **f32** PCG + **self-weight** + low CG budget + tiny **`E_min`** (lane repro: `UMST_SHELL_MAX_CG=200` + `UMST_SHELL_E_MIN_REL=5e-9` → NaN at iter 1); large-slab **Helmholtz** on the autodiff tape and in-loop volume projection; artefacts may be missing or v0.3-named until regenerated. | Raise **`UMST_SHELL_MAX_CG`**, keep **`UMST_SHELL_E_MIN_REL≈10⁻³`**, PCG / traction / projection flags per lane; land **`shell_topology_rib_pattern_full_v04`** green (or env-gated proof + CI split), run Track L regen, commit **`striatus_emergence.gif`**, **`striatus_shell_v0.4.stl`**, **`striatus_shell_v0.4.print_ready.json`** (+ optional `.obj`), wire **`test_print_ready.py`** on CI; **`f64`** inner equilibrium deferred per lane. |
| **2** | **Mechanics (plates):** move Q1 hex SRI toward the **Kirchhoff** target in the completion brief versus today’s **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`** ratio band ([§R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate)). | `tests/verification/mechanics_analytic.rs` — **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`** (**ratio band** for locked Q1 vs thin-plate reference, not a within-5% thin-plate error gate; full Kirchhoff SSSS on brick not default-CI); **`tests/verification/adjoint_compliance_analytic.rs`** — 4-node bar compliance + adjoint gradient vs FD (same **mechanics** research lane, **PCG** inner equilibrium on bars); `src/physics/q1_hex_elasticity.rs`; features **`mechanics-voigt-cauchy`**, **`topology-density-evolution`** (Q1 hex path). | **Within-5% (or brief-stated) centre deflection vs Kirchhoff SSSS reference** on the brick path the brief targets — replacing reliance on **ratio band** alone for “ship” language; follow-up [§R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate). | **M** | Shear locking on Q1 hex; SRI / enrichment path not yet meeting thin-plate error gate; cantilever / plate coverage split per main `Solver-Status` mechanics row. | Implement SRI (or brief-approved stabilization), add **default-CI** test **`plate_centre_deflection_kirchhoff_within_X_percent`** (name TBD) with explicit **L/t** and mesh documented next to **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`**; keep ratio test as regression unless brief retires it. |
| **3** | **Fracture:** Γ-limit with driven **ψ⁺**; broader **(l₀,h)** schedules; within-step THMC **u↔d** stagger; embedding / **`matrix_features`** when no bar solve — memo §2.3 / §7. | `tests/verification/fracture_gamma_convergence.rs`, `staggered_fracture_mechanics_chain.rs`, `staggered_ud_loop_milestone.rs`, `thmc_drying_shrinkage.rs` (`bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`); memo [`docs/research/v0.4_track12_staggered_fracture_mechanics.md`](research/v0.4_track12_staggered_fracture_mechanics.md) §2.3 / §7 (proposed test names). | Memo **§7** style: (1) Γ-type or dissipation harness with **ψ⁺ ≠ 0** and/or staggered bar loading; (2) THMC SI path + optional **within-step** **u↔d**; (3) energy monotone band on benchmark chain; broader **(l₀,h)** than fixed **h/l₀=¼** triple; non-embedding / **`matrix_features`** fallback documented + tested. | **L** | Today’s Γ certificates largely **ψ⁺=0**; damage at step entry for stagger; no full within-step alternation in `ThmcSolver::step` contract. | One PR: add **single** new `#[test]` from memo §7 (e.g. driven **ψ⁺** Γ extension **or** one extra **(l₀,h)** schedule) with numeric bands copied from §7; second PR slice: spec + test stub for **`matrix_features`** / no-bar path **without** claiming full THMC stagger closure. |
| **4** | **Acoustics:** align **`plane_wave_return_map_n128_documented_phase_slip_band`** with default return-map gates **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`**, or revise brief mesh count — **Solver lanes — Acoustics**. | `tests/verification/acoustics_plane_wave.rs`: CI **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`** (same CFL-scaled substep recipe as n=100); **`plane_wave_return_map_n128_documented_phase_slip_band`** (`#[ignore]`); energy **`undamped_energy_drift_under_half_percent_over_1000_steps`** at **n=128** (energy proxy — **not** a return-map certificate); `newmark_acceleration_matches_dense_reference_*`; `AcousticNewmarkBar1dPeriodic` rustdoc in `src/physics/solvers/acoustics.rs`. | **Either** (a) relative **L²** to `u₀` **< 2%** after one semi-discrete period at **n=128** with justified **Δt**/substep recipe **and** promote strict gate / narrow ignore **or** (b) **explicit brief + CI revision** of mesh count with updated gates. Policy in `Solver-Status.md`: do **not** un-ignore n=128 until (a) or (b). | **M** | **§R2.4 diagnostic:** **16×** more equal substeps within the same semi-discrete period **T=2π/ω_h** leaves n=128 relative **L²** ~**1.39** (order-one phase slip, not mild 2% exceedance) — mismatch **not** resolved by substep refinement alone; likely **spatial dispersion** and/or **continuum ω_h** vs discrete travelling mode for the return-map benchmark. | PR: **discrete-dispersion-aligned period** (eigenpair / discrete harmonic) **or** documented brief + CI mesh revision; add one **strict** gate matching policy; shrink ignored bracket only after strict gate exists. |
| **5** | **Electrochemistry:** **sparse/cheap Jacobian** + **harness alignment** (including **unit-graph Poisson vs `mesh_spacing = h`** in the coupled map) so **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** / **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** can run on **default CI** (today **`debye_implicit_dispatch_short_horizon_smoke`** + **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`**); **general-graph** implicit Newton; variable **ε** on **non-chain** relaxers; large-\|zΔφ\| **f32** robustness ([§R2.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r23--pnp-λ_d-quasi-steady-gates-un-ignored)). | `tests/verification/pnp_debye_layer.rs`: CI **`debye_implicit_dispatch_short_horizon_smoke`**, **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** (substitute trajectory checks — **not** exponential-fit λ_D gates); ignored **`debye_screening_256_cells_phi_25mv_decay_length_within_band`**, **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** (**`dt`·`steps` = `1.5e-3`×`10_000` ≈ 15** nondimensional time units — same as **`Solver-Status.md`** electrochemistry row); **`debye_screening_admissibility_check`** + **`debye_implicit_newton_linearized_sg_for_lambda_d_gate()`** (`linearize_sg_fickian: true`, analytic **(3N)×(3N)** Jacobian); dispatch **`solve_pnp_step_dispatch`** + **`pnp_implicit_newton_chain`** (MVP chain); SG **`mesh_spacing`** scaling vs **unit-graph** Poisson — `electrochemistry` rustdoc. Memo [`docs/research/v0.4_track14_implicit_newton_pnp.md`](research/v0.4_track14_implicit_newton_pnp.md). | (1) Harness alignment + sparse/cheap Jacobian so **25 mV** λ_D gate can **drop `#[ignore]`** on default CI; (2) extend **100 mV** (φ₀=4 V_T) sibling; (3) Newton residual ↓ on **non-chain** benchmark or matrix-free JVP; (4) mass band on closed graph; (5) double-precision path for large \|zΔφ\| — per lane “Next PR acceptance” in `Solver-Status.md` + follow-up [§R2.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r23--pnp-λ_d-quasi-steady-gates-un-ignored). | **L** | Quasi-steady **`λ_eff`** still **O(1)** vs continuum **`λ_D`** after **`mesh_spacing = h`**, variable-ε Poisson, linearised SG Newton — baseline **`--release`** sample **λ_eff≈3.71** vs **λ_D=1/√2≈0.707** (**|λ_eff−λ_D|/λ_D ≈ 4.25**, outside Track F ±5 % / ±15 % bands without widening); dense Newton at **N=256** costly; full nonlinear SG at Newton level when **`linearize_sg_fickian: false`** even slower; **φ₀=4 V_T** (**100 mV**) ignored λ_D gate **tracks behind** **25 mV** until harness / Jacobian CI land. | PR: discrete-screening / fit-window + **unit-graph vs physical h** alignment; **sparse or cheaper Jacobian** (or reduced horizon) so **25 mV** gate runs in CI; then general-graph implicit Newton slice — **no** claim of full graph until (3) test exists. |
| **6** | **Photonics:** production **2D/3D** DEC vector **curl–curl**; tensor permittivity; tighter **Fresnel** calibration ([§R3.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r32--dec-vector-curl-curl-on-2d3d-patches)). | `tests/verification/photonics_fresnel.rs` (incl. **`dec_te_primal_tensor_matches_chain_stencil`** — DEC primal matvec vs hand TE stencil), `tests/verification/photonics_curl_curl_stub_default_build.rs` (`photonics` **off** → **`solve_maxwell_curl_curl`** documented identity stub); `tests/dec_identities.rs`: **`dec_curl_d1_annihilates_gradient_on_triangle_faces_b2_burn`**, **`dec_primal_d1_adjoint_identity_single_triangle_burn`**, dense companion **`dec_curl_d1_annihilates_gradient_on_triangle`**; **quad-split shared-edge:** **`dec_curl_d1_annihilates_gradient_quad_split_two_faces_burn`**, **`dec_primal_d1_adjoint_identity_quad_split_two_faces_burn`** (`src/physics/dec_primal.rs` COO — **none** call `PhotonicsSolver::solve_maxwell_curl_curl`). Memo [`docs/research/v0.4_track15_dec_curl_curl_photonics.md`](research/v0.4_track15_dec_curl_curl_photonics.md). | (1) Tighten **`two_half_spaces_fresnel_te_no_pml_matches_analytic`** LS margin **or** discrete **r_disc**-only check; (2) metric-weighted DEC **and/or** **cube** patch from assembled topology feeding **`solve_maxwell_curl_curl`**; (3) tensor **ε** path beyond scalar node **ε_r** — per lane + follow-up [§R3.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r32--dec-vector-curl-curl-on-2d3d-patches). | **L** | Today: 1-D TE **`E_y`** curl–curl reductions + piecewise **`ε_r`** + stub default build; DEC identities on hand-authored triangles / quad-split, not full mesh assembly into photonics solve. | PR: **metric-weighted** `d₁` adjoint / curl on assembled **2-face** or **cube** slice **wired into** one new photonics integration test (single patch), plus Fresnel margin delta documented in test. |
| **7** | **Rheology:** **true pressure Poisson** (or **MAC-style** staggered solve) so **`chorin_steady_channel_64x16_vs_regularized_reference`** and **`chorin_developed_channel_centreline_vs_regularized_reference`** can graduate from **`#[ignore]`** — **`chorin_surrogate_poisson_amplification_regression_guard`** brackets surrogate amplification today ([§R2.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r22--rheology-chorin-64--16--fix-surrogate-poisson)). | `tests/verification/rheology_poiseuille.rs`: **`chorin_surrogate_poisson_amplification_regression_guard`** (CI), **`chorin_steady_channel_64x16_vs_regularized_reference`** (`#[ignore]`), **`chorin_developed_channel_centreline_vs_regularized_reference`** (`#[ignore]`); **`channel_lattice_64x16_quadrilateral_edge_count`**; `src/physics/solvers/rheology_flow.rs` surrogate Poisson (Richardson on tangent-projected gradient). | (1) Centreline **L²** vs analytic **< 15%** at stated **dt** / steps on **64×16**; (2) pressure Poisson RHS aligned with discrete **div u***; (3) optional env long benchmark — per lane; surrogate amplification **not** ~10³/step to NaN on **65×17**. | **L** | Surrogate Poisson **dt-independent** blow-up on developed channel; **`chorin_surrogate_poisson_amplification_regression_guard`** documents current band until replaced. | PR: replace surrogate step with **discrete Poisson solve** + BCs on same stencil, re-run **`chorin_steady_channel_64x16_vs_regularized_reference`**; adjust or retire **`chorin_surrogate_poisson_amplification_regression_guard`** once real solve lands. |
| **8** | **THMC:** **monolithic** implicit Newton on **fully coupled stacked** unknowns; optional within-step **u↔d** stagger ([§R3.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r31--monolithic-thmc-newton-on-t-h-u-c)). | `tests/verification/thmc_drying_shrinkage.rs` — implicit **(T,α)** only: **`thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`**, **`thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm`**, multi-Newton monotone, **`thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split`**, drying/shrinkage bands; assembler trial in related paths; memo [`docs/research/v0.4_track13_monolithic_newton_thmc.md`](research/v0.4_track13_monolithic_newton_thmc.md). | **‖R‖** down across inner Newton on **stacked** THMC unknowns (not only **(T,α)** block); `tol` early exit; linearized thermal vs analytic under **full** implicit map once assembled; optional **u↔d** stagger spec’d in memo — per lane “Next acceptance” in `Solver-Status.md`. | **L** | Split step: humidity does not branch on implicit **(T,α)**; **`update_damage`** once after outer loop; no dense Jacobian for full stack at scale. | PR: define **residual vector** for smallest coupled chain **(T,h,α,u)** (or memo subset), **dense FD Jacobian ≤64 DOFs**, one test **`thmc_monolithic_residual_newton_lowers_norm_two_nodes`** (name TBD) — no production-scale Krylov until this passes. |
| **9** | **Statistical mechanics:** **virial / coexistence** route into **`upscale_potentials`** ([§R3.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r33--lj-virial-bridge-inside-upscale_potentials)). | `tests/verification/statmech_lj_bridge_contract.rs`, `statmech_lj_johnson_eos_reference.rs`, `statmech_vinet_eos.rs`; optional feature **`statistical-mechanics-johnson-reference`**; memo [`docs/research/v0.4_track16_virial_lj_bridge.md`](research/v0.4_track16_virial_lj_bridge.md); lane table in `Solver-Status.md` (**[B,2]** vs state **(ρ*,T*)** gap). | (1) Reference **K** from MD or published EOS at one state; (2) **`upscale_potentials`** error vs reference **< stated tolerance**; (3) document **T** / cutoff dependence — per lane. | **L** | Tensor rows today **(ε,σ)** only; Johnson **`f64`** reference not inside Burn **`upscale_potentials`** without API extension (**[B,4]** or parallel state tensor). | PR: extend batch contract (**documented** columns for **(ρ*,T*)** or parallel state tensor), route one Johnson or tabulated virial evaluation into **`upscale_potentials`** behind feature flag, add **`upscale_potentials_matches_johnson_at_reference_state`** (single point, tight tol). |
| **10** | **Mechanics (transient solids & contact):** shipped mechanics is quasi-static **`VectorMechanicsSolver::solve_equilibrium`** on the bar-network **1-skeleton** — **not** a CI-unified **M ü + C u̇ + Ku = f** stack on general **3D solid DOFs**. The **`acoustics-newmark`** 1-D periodic bar Newmark lane (`tests/verification/acoustics_plane_wave.rs`) is a **scalar** bar-wave semi-discrete operator, **not** vector **3D solid transient elasticity** on the mechanics graph. **Contact / collision / friction:** none in the shipped stack — [contact mechanics scope](research/contact_mechanics_scope.md). | Quasi-static shipped path: **`VectorMechanicsSolver::solve_equilibrium`** / **`packed_bar_network_equilibrium`** — **f32** projected **PCG** on the bar-network 1-skeleton (`src/physics/mechanics.rs`); `tests/verification/mechanics_analytic.rs`, `adjoint_compliance_analytic.rs`. **Scalar** semi-discrete bar wave (**not** vector 3D solid transient on mechanics graph): **`tests/verification/acoustics_plane_wave.rs`** (**`acoustics-newmark`**, `AcousticNewmarkBar1dPeriodic`). Memo [`docs/research/contact_mechanics_scope.md`](research/contact_mechanics_scope.md). | **Either** ship **default-CI** tests for **transient vector** mechanics on stated graph DOFs with **Newmark/generalized-α** and analytic or manufactured solution **or** explicitly scope “not in v0.4” in brief + README; **contact**: constraints + Coulomb friction with verification tests — until then, docs must **not** claim unified transient/contact stack. | **L** | No **`M ü + C u̇ + Ku`** on general 3D solid DOFs in one verification story; acoustics lane is **scalar** 1-D periodic bar operator, disjoint from vector mechanics graph; no contact / inter-body Coulomb friction in shipped stack. | PR slice **A (docs honesty):** README + `Solver-Status` table footnote tying **`acoustics-newmark`** to **scalar bar** only. **PR slice B (engineering):** single **2-node** transient mechanics chain with diagonal **M,K** and analytic mode — one ignored or fast test proving wiring before full graph. |

---

### How to use this matrix

- **Effort:** **S** ≈ single focused test or doc PR; **M** ≈ one subsystem change with tests; **L** ≈ multi-file / API / performance work spanning milestones.
- **Hooks** are what exists **today** to anchor claims; they do **not** imply the acceptance row is met.
- Regenerate path integrity after editing [`Solver-Status.md`](Solver-Status.md): `python3 scripts/check_solver_status.py --check-paths` (from `umst-manifold/`).
