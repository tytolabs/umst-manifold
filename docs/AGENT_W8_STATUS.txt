UMST W8 — manifest-bridge (completed)
=====================================
Date: 2026-05-29 (remote closure)
Workspace: MaOS-Workspace/umst-manifold + umst-concrete-cartridge (git-pinned)

## Manifold (`umst-manifold`)

- `src/lib.rs`: `pub mod manifest` (always on; not feature-gated).
- `src/manifest/mod.rs` re-exports:
  - `GateRegistry`, `GroundingContract`, `UmstManifest`, `UmstManifestBuilder`
  - from `src/manifest/umst_manifest.rs`
- `Cargo.toml` features:
  - `manifest-bridge = []`  (downstream marker)
  - `manifold-manifest = []`
- **Phase 1 publish:** `tytolabs/umst-manifold` `main` @ **fe2243716112f2504b063e55eb1e15e97ced4bdc** (`fe22437`); CI run 26649667467 green.

## Concrete cartridge (`umst-concrete-cartridge`)

- `crates/umst-concrete-cartridge/Cargo.toml`:
  - `umst-manifold = { git = "…/umst-manifold.git", rev = "fe22437…" }`
  - `manifest-bridge = ["umst-manifold/manifest-bridge", …]`
- **G-02:** workspace `[patch]` removed; GHA `manifest-bridge` on git dep only.
- `src/facade/mod.rs`: `#[cfg(feature = "manifest-bridge")] pub use umst_manifold::manifest::*;`

## Cargo verification (exit 0)

```text
cd umst-manifold && cargo check
cd umst-concrete-cartridge && cargo test -p umst-concrete-cartridge --features manifest-bridge
bash umst-manifold/scripts/w8_publish_readiness.sh   # accepts git pin OR optional [patch]
```

## Optional monorepo dev

- Workspace `[patch]` to `../umst-manifold` is **optional** for unpublished manifold work; not required for G-02 / remote CI.

## W8 status: DONE (local + remote)

- **G-01** manifold publish @ **fe22437**
- **G-02** concrete cartridge CI without `[patch]`
- **G-03** supercap remote `manifest-bridge` CI — optional follow-up
