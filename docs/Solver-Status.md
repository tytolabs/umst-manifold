# Solver status (v0.4)

**Verification index:** one page of section → memo → tests — [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md). **Completion matrix** (numbered **Verification scope (remaining)** items → PR slices): [`VERIFICATION_COMPLETION_MATRIX.md`](VERIFICATION_COMPLETION_MATRIX.md).

From the `umst-manifold/` tree in this workspace, the v0.4 solver-completion brief is **`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`** (the same relative path from `umst-manifold/docs/`).

For planned **solver multithreading** (rayon hot-loops, BLAS interaction, feature surface), see the [v0.4 multithreading brief](../../composer_prompts/v0.4_multithreading_solver_parallelisation.md).

Public mapping of solver-facing modules to **lane** (`solver-stable` vs `solver-research` in `Cargo.toml`), **verification** tests, and notes.

**CI** (`.github/workflows/rust.yml`): **`solver-status`** — `python3 scripts/check_solver_status.py --check-paths --check-memo-links --check-statmech-verification-set` (**Verification** test paths exist; memo links to `research/*.md` beside this file and backticked ``docs/research/*.md`` paths under the repo root exist; the `solvers::statistical_mechanics` row lists the full `statmech_*` verification set — see `scripts/check_solver_status.py` docstring). **`lint`** (merge gate) — `cargo fmt --all -- --check` and `cargo clippy --all-targets --features solver-experimental -- -D warnings` (same feature union as meta `solver-tests` / v0.4 ship checklist). **`research-stack`** — `cargo test --verbose --release --features solver-experimental` with one retry on failure; runs on pushes to `main` and on `workflow_dispatch`.

**Rule:** use “implemented” in prose below only where the listed **Verification** tests exercise the claim on CI (default `cargo test` path for stable rows; `--features solver-experimental` for research rows unless noted).


| Solver                                                                                  | Lane     | Verification                                                                                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `solvers::topology_solver` (`TopologyOptimizer`, density evolution)                     | stable   | `tests/topology_continuation.rs`, `tests/topology_filter.rs`                                    | Feature `topology-density-evolution`. Heat-equation / SIMP-adjacent evolution; conservative stable-lane entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `mechanics::VectorMechanicsSolver`, `adjoint::AdjointCompliance`, bar / plate mechanics | research | `tests/verification/mechanics_analytic.rs`, `tests/verification/adjoint_compliance_analytic.rs` | Features `mechanics-voigt-cauchy`, `topology-density-evolution` (Q1 hex path). **Live:** cantilever chain; **adjoint:** 4-node bar compliance + gradient vs FD. **Plate:** Q1 hex shear-locking aware checks; Kirchhoff cross-check is `plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band` (**ratio band** for locked Q1 vs thin-plate reference, not a within-5% thin-plate error gate; full Kirchhoff SSSS on brick not yet covered by default CI — follow-up brief [§R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate)). **Geometric NL / finite-strain gap:** [mechanics scope memo](research/mechanics_geometric_nonlinearity_scope.md).                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `solvers::fracture_field` (`PhaseFieldFractureSolver`)                                  | research | `tests/verification/fracture_gamma_convergence.rs`, `tests/verification/staggered_fracture_mechanics_chain.rs`, `tests/verification/staggered_ud_loop_milestone.rs`, `tests/verification/thmc_drying_shrinkage.rs` (kinematic THMC ↔ helper parity) | Feature `fracture-at2`. **Implemented:** inner AT2 damage relaxation (red–black, odd outer passes); `at2_length_scale_sweep_non_regression` (multi–\(l\) admissibility smoke); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not sharp-interface Γ-limit); **`at2_gamma_convergence_three_length_scales`** — partial Γ-type check: three \((l_0,h)\) pairs, \(h/l_0=\tfrac14\), \(\psi^+\!=0\), discrete \(D_h\) vs **`Gc`** \(\lt 2\%\) and non-worsening across refinement (fixed-strain relaxation); **`at2_discrete_surface_functional_toy_chain_matches_hand_total`** (hand-checked discrete \(D_h\) on a 3-node toy via shared `discrete_at2_bar_surface_energy_1d`); **`staggered_fracture_compliance_monotone_increasing`** (staggered bar compliance vs outer iterations). **Milestone 1:** documented `update_damage_staggered` harness on a 3-node chain with **mechanics equilibrium** strain at degraded stiffness, an **analytic strain surrogate**, and **`strain_tensor_for_fracture_after_mechanics`** for **post-mechanics** symmetric strain on the same chain (see `staggered_ud_loop_milestone` module docs). **THMC (`thmc-coupled`):** with SI `node_positions` `[N,3]` and compatible BC mask, **`ThmcSolver::step`** feeds fracture from **`strain_tensor_from_bar_network_displacement`** on the equilibrium displacement (same kinematics as post-mechanics helper; `thmc_drying_shrinkage::bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`). **Remaining:** Γ-limit with driven \(\psi^+\); broader \((l_0,h)\); within-step THMC \(u\!\leftrightarrow\!d\); embedding / `matrix_features` fallbacks — **Solver lanes — Fracture** + [track 12 memo](research/v0.4_track12_staggered_fracture_mechanics.md) §2.3 / §7 (§7 = proposed acceptance + test names only). **Compressive crushing / DP–cap plasticity:** not implemented; AT2 drive uses spectral **ψ⁺** (tensile principal strains) only — `docs/research/concrete_compressive_plasticity_scope.md`; composer context [`v0.4_phase_2_3_master_plan.md`](../../composer_prompts/v0.4_phase_2_3_master_plan.md) (P2.4 fracture, P3.1 staggered mechanics). **Trust:** highly compressed slender struts have **no** in-repo crushing/yield cap. |
| `solvers::acoustics` (`AcousticWaveSolver`, `AcousticNewmarkBar1dPeriodic`)             | research | `tests/verification/acoustics_plane_wave.rs`                                                    | Feature `acoustics-newmark`. **Live:** `newmark_acceleration_matches_dense_reference_n8`, `newmark_acceleration_matches_dense_reference_n128` (f64 dense ref), `plane_wave_return_map_n64_l2_within_two_percent`, `plane_wave_return_map_n100_l2_within_two_percent` (semi-discrete period `T=2π/ω_h`; n=64 uses the same CFL-scaled substep recipe as n=100), `plane_wave_h_refinement_second_order_band`, `undamped_energy_drift_under_half_percent_over_1000_steps` (**n=128** energy proxy). Inner linear solve uses **f64** Cholesky (f32 factorization diverged at large `n`). **CI vs brief:** return-map gates are **n=64** and **n=100**; **n=128** with the same CFL-scaled substep recipe shows large phase slip (relative \(L^2\) to `u₀` **~1.4** in baseline — not a 2% margin miss); opt-in ignored bracket `plane_wave_return_map_n128_documented_phase_slip_band`. `AcousticWaveSolver` remains graph-free nodal contraction only. See **Solver lanes — Acoustics**. |
| `solvers::electrochemistry` (`ElectroChemicalSolver`)                                   | research | `tests/verification/pnp_debye_layer.rs`                                                         | Feature `electrochemistry-pnp` (canonical Track I; forwards to `electrochemistry-mvp` for `#[cfg]`). **Verified on default CI (non-ignored tests in this path):** SG NP + path-chain **Thomas** Poisson (`try_solve_poisson_chain_thomas`); **explicit Picard** only in `solve_pnp_step` → `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep, \(\rho(c^{(k)})\!\to\!\Phi\), then \(c^{n+1}=c^n-\Delta t\,\nabla\!\cdot J(\Phi,c^{(k)})\)); **`NewtonPnpContext` is not used on that path.** Tests: Fickian zero-field, screening smoke, `picard_coupling_iters_finite_smoke`, `backward_euler_implicit_newton_matches_split_in_linearized_small_dt_limit`, **`debye_implicit_dispatch_short_horizon_smoke`** (Track 14 **`solve_pnp_step_dispatch`** + **`pnp_implicit_newton_chain`**, short chain — not the λ_D exponential-fit gates), **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** (same dispatch wiring; samples implicit BE `‖R‖₂` along a Debye-style trajectory — **CI substitute** for the ignored continuum λ_D LS gates). **Partial — parallel Track 14 API:** MVP-chain **implicit backward Euler** + damped Newton via `try_solve_pnp_backward_euler_newton_chain` (host `f64` dense Jacobian); **opt-in from solver config** (default unchanged): set `pnp_implicit_newton_chain: Some(NewtonPnpContext {…})` and call `solve_pnp_step_dispatch` (falls back to explicit Picard if the graph is not an MVP batch-1 chain). **`#[ignore]` on default CI — not counted under default-CI “implemented” prose:** **Quasi-steady λ_D exponential-fit gates:** **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** (φ₀ = 1 V_T) and **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** (φ₀ = 4 V_T, Gouy–Chapman-weighted band) are **`#[test]` + `#[ignore = "…"]`** in `pnp_debye_layer.rs` (messages cite the measured **`λ_eff≈3.71`** vs continuum **`λ_D=1/√2≈0.707`** gap). They share **`solve_pnp_step_dispatch`** + solver config **`pnp_implicit_newton_chain: Some(…)`** with **`debye_implicit_newton_linearized_sg_for_lambda_d_gate()`** — **`linearize_sg_fickian: true`**, **`max_newton_iters: 24`**, host **`f64`** residual tol **`1e-9`** (Fickian-linearised SG in the implicit BE residual → **analytic** Jacobian; **not** full nonlinear SG at Newton level). The admissibility harness sets **`mesh_spacing = h = L/(N-1)`** so SG flux and the interior LS abscissa use physical **\(x\)**, while the MVP chain **Poisson** block stays a **unit-graph** stencil (unchanged by `mesh_spacing` — see module rustdoc). **`dt`·`steps` = `1.5e-3`×`10_000` ≈ 15** nondimensional time units (not default CI). Measured **`λ_eff`** still misses continuum **`λ_D`** by **\(\mathcal O(1)\)** (see **Solver lanes — Electrochemistry**). Each damped-Newton step solves a dense **\((3N)\times(3N)\)** system (**analytic** Jacobian when **`linearize_sg_fickian: true`**; column FD if **`false`** — even slower at **N=256**). **Remaining:** variable \(\varepsilon\) on **non-chain** relaxers; sparse Jacobian at **N=256** (follow-up brief [§R2.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r23--pnp-λ_d-quasi-steady-gates-un-ignored)); large-\(|z\Delta\phi|\) f32 robustness — **Solver lanes — Electrochemistry** and `docs/research/v0.4_track14_implicit_newton_pnp.md`.
| `solvers::photonics` (`PhotonicsSolver`, `PhotonicsHelmholtzSolver`)                    | research | `tests/verification/photonics_fresnel.rs`, `tests/verification/photonics_curl_curl_stub_default_build.rs` | Feature `photonics-fdfd` (canonical Track I; forwards to `photonics` for `#[cfg]`); deprecated alias `photonics-scaffold` → `photonics-fdfd`. **Implemented:** scalar TE FDFD Helmholtz (Thomas + PML); `curl_curl_y_mode_matches_scalar_helmholtz` and **`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`** exercise `PhotonicsSolver::solve_maxwell_curl_curl` on a uniform x-chain (\(E_y\) from \(J_y\)), including **non-uniform** node \(\varepsilon_r\); `dec_te_primal_tensor_matches_chain_stencil` (DEC primal matvec vs hand TE stencil); `two_half_spaces_fresnel_te_no_pml_matches_analytic` (Dirichlet-closed chain vs bridged continuum Fresnel + residual checks, loose field LS tol). **Default lane:** `photonics_curl_curl_stub_default_build` asserts `solve_maxwell_curl_curl` is an identity when the `photonics` feature is **off** (documented stub). **Remaining:** full DEC vector curl–curl on 2D/3D patches; tensor permittivity; tighter discrete semi-infinite Fresnel calibration. See **Solver lanes — Photonics**.                                                                                                                                                                                                                                                           |
| `solvers::rheology_flow` (`BinghamFlowSolver`)                                          | research | `tests/verification/rheology_poiseuille.rs`                                                     | Feature `rheology-bingham`. **Implemented:** analytic Poiseuille / plug-width / no-slip; regularized Bingham centreline and small-\(\varepsilon\) vs Buckingham profile; Chorin `chorin_single_step_finite_smoke`; **`chorin_surrogate_poisson_amplification_regression_guard`** (two-step surrogate amplification bracket on **65×17**); Roussel \(\lambda\) explicit-step parity + Wangler-style quiescent yield-growth fit; `channel_lattice_64x16_quadrilateral_edge_count` (graph scaffold). **Not yet on default CI:** `chorin_developed_channel_centreline_vs_regularized_reference` (`#[ignore]` — centreline vs regularized ref awaits pressure Poisson + BCs); `chorin_steady_channel_64x16_vs_regularized_reference` (`#[ignore]` — surrogate Poisson ~\(10^3\)/step **dt-independent** on **65×17**). See **Solver lanes — Rheology**. |
| `solvers::thmc` (`ThmcSolver`, …)                                                       | research | `tests/verification/thmc_drying_shrinkage.rs`                                                   | Feature `thmc-coupled`. **Implemented:** drying + shrinkage band; explicit transport + hydration via configurable `ThmcHydrationKinetics`; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; **track 13 partial:** backward-Euler **thermal + hydration \(\alpha\)** residual in `thmc_residual::ThmcImplicitEulerThermalHydrationResidual` (`thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`, `thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm` for one damped Newton on the same \((T,\alpha)\) block). **Fracture coupling (SI bar path):** post-mechanics \(\varepsilon(\mathbf u)\) via **`strain_tensor_from_bar_network_displacement`** when `node_positions` is `[N,3]` — **Solver lanes — Fracture** + [track 12 memo](research/v0.4_track12_staggered_fracture_mechanics.md) §2.3; kinematic parity test `bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`. **Remaining (research memo 13):** monolithic implicit Newton on fully coupled THMC; **within-step** \(u\!\leftrightarrow\!d\) stagger — **Solver lanes — THMC** and `docs/research/v0.4_track13_monolithic_newton_thmc.md`.
| `solvers::statistical_mechanics`                                                        | research | `tests/verification/statmech_vinet_eos.rs`, `tests/verification/statmech_lj_bridge_contract.rs`, `tests/verification/statmech_lj_johnson_eos_reference.rs` | **Stable lane:** Vinet scalar EOS (`statistical-mechanics-vinet`). **Research:** `upscale_potentials` LJ→continuum **placeholder** (`statmech_lj_bridge_contract`). **Johnson (1993)** `f64` EOS lives in `lj_johnson_1993_reference`; it is **not** called inside `upscale_potentials` — tensor rows carry only \((\varepsilon,\sigma)\), while \(K^*\) needs **reduced** \((\rho^*,T^*)\), so types do not align without a wider bridge API. **Opt-in** feature `statistical-mechanics-johnson-reference`: scalar `bulk_modulus_from_lj_state_johnson1993` re-export + in-module `upscale_placeholder_bulk_modulus_documented_gap_vs_johnson_scalar_path`; integration `statmech_lj_johnson_eos_reference` (`placeholder_upscale_bulk_modulus_disagrees_with_johnson_reference_documented`). **`upscale_potentials` remains partial** (placeholder \(K,\gamma_{\mathrm{gc}}\)). Cartridge `physics::clinker_eos` holds phase tables. See **Solver lanes — Statistical mechanics** and `docs/research/v0.4_track16_virial_lj_bridge.md`.


## Research memos (v0.4 follow-on)

Numbered files under `docs/research/` (`v0.4_track12_staggered_fracture_mechanics.md` … `v0.4_track16_virial_lj_bridge.md`). Each memo is **present** on disk for tracks **12–16**. Full-track acceptance remains **open** where solver lanes below say **partial**; shipped milestones are cited in the main table and memos.

- **12 — Fracture / AT2:** [memo](research/v0.4_track12_staggered_fracture_mechanics.md). Partial implementation in listed fracture tests; §7 lists proposed **`#[test]`** names for future Γ-type and coupling work.
- **13 — THMC:** [memo](research/v0.4_track13_monolithic_newton_thmc.md). Partial: backward-Euler **\((T,\alpha)\)** residual + damped Newton smoke on tiny chains.
- **14 — Electrochemistry:** [memo](research/v0.4_track14_implicit_newton_pnp.md). Partial: Picard path + MVP-chain implicit Newton dispatch (`solve_pnp_step_dispatch`).
- **15 — Photonics:** [memo](research/v0.4_track15_dec_curl_curl_photonics.md). Partial: 1-D TE chain reductions in `photonics_fresnel.rs`.
- **16 — Statistical mechanics:** [memo](research/v0.4_track16_virial_lj_bridge.md). Partial: Johnson reference + documented placeholder gap vs `upscale_potentials`.


## Verification scope (remaining)

Single checklist of **open verification / ship items** (solver-by-solver); solver-lane sections below hold acceptance detail and commands without repeating this list verbatim.

1. **Topology / shell (Tracks B + L):** Run **`shell_topology_rib_pattern_full_v04`** at **40×40×4** (documented NaN / bar-network CG conditioning); commit Track L artefacts (`striatus_emergence.gif`, `striatus_shell_v0.4.stl`, `striatus_shell_v0.4.print_ready.json`, optional `.obj`) and satisfy Track B8 — regeneration command and paths under **Solver lanes — Topology / shell**. **Track L demo (cartridge):** `notebooks/_run_shell_demo.sh` from **`umst-concrete-cartridge/`** repo root (env `UMST_SHELL_DUMP_ITER` / `UMST_SHELL_DUMP_STRIDE` / `UMST_SHELL_ITERS`; optional `UMST_SHELL_*` stability knobs — **`../../umst-concrete-cartridge/docs/Solver-Status.md`** §Topology / shell + `optimize_shell_3d` rustdoc).
2. **Mechanics (plates):** Move Q1 hex SRI toward the Kirchhoff target in the completion brief versus today’s **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`** ratio band ([follow-up §R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate)).
3. **Fracture:** Γ-limit with driven **ψ⁺**; broader **(l₀,h)** schedules; within-step THMC **u↔d** stagger; embedding / `matrix_features` when no bar solve — memo §2.3 / §7.
4. **Acoustics:** Align **`plane_wave_return_map_n128_documented_phase_slip_band`** with default return-map gates **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`**, or revise brief mesh count — **Solver lanes — Acoustics**.
5. **Electrochemistry:** Today **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** and **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** are **`#[ignore]`** (omitted from default `cargo test`); default CI substitutes **`debye_implicit_dispatch_short_horizon_smoke`** + **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`**. **Open:** sparse/cheap Jacobian + harness alignment (including **unit-graph Poisson vs `mesh_spacing = h`** in the coupled map) so the exponential-fit gates can **drop `#[ignore]`** and join default CI; general-graph implicit Newton; variable ε on non-chain relaxers; large-|zΔφ| f32 robustness ([follow-up §R2.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r23--pnp-λ_d-quasi-steady-gates-un-ignored)).
6. **Photonics:** Production 2D/3D DEC vector curl–curl; tensor permittivity; tighter Fresnel calibration ([follow-up §R3.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r32--dec-vector-curl-curl-on-2d3d-patches)).
7. **Rheology:** True pressure Poisson (or MAC-style staggered solve) so **`chorin_steady_channel_64x16_vs_regularized_reference`** and **`chorin_developed_channel_centreline_vs_regularized_reference`** can graduate from `#[ignore]` — **`chorin_surrogate_poisson_amplification_regression_guard`** brackets surrogate amplification today ([follow-up §R2.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r22--rheology-chorin-64--16--fix-surrogate-poisson)).
8. **THMC:** Monolithic implicit Newton on fully coupled stacked unknowns; optional within-step **u↔d** stagger ([follow-up §R3.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r31--monolithic-thmc-newton-on-t-h-u-c)).
9. **Statistical mechanics:** Virial / coexistence route into **`upscale_potentials`** ([follow-up §R3.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r33--lj-virial-bridge-inside-upscale_potentials)).
10. **Mechanics (transient solids & contact):** Shipped mechanics is quasi-static **`VectorMechanicsSolver::solve_equilibrium`** on the bar-network 1-skeleton — **not** a CI-unified **M ü + C u̇ + Ku = f** stack on general 3D solid DOFs. The **`acoustics-newmark`** 1-D periodic bar Newmark lane (`tests/verification/acoustics_plane_wave.rs`) exercises a **scalar** bar-wave semi-discrete operator, **not** vector 3D solid transient elasticity on the mechanics graph. **Contact / collision / friction:** no contact constraints or inter-body Coulomb friction in the shipped stack — see [contact mechanics scope](research/contact_mechanics_scope.md).

Lower-priority stretch items from the follow-up brief (stacked THMC beyond split ship, DEC Maxwell breadth, LJ bridge wiring, within-step stagger) appear above where they touch default CI or ship artefacts.


## Solver lanes — THMC

<span id="solver-lane-thmc"></span>

| | **Shipped** `ThmcImplicitTAlphaNewtonConfig` | **Remaining scope:** monolithic Newton (research memo 13) |
| --- | --- | --- |
| **Implicit unknowns** | \(T^{n+1},\alpha^{n+1}\) only (backward Euler + dense FD Jacobian on \(\le 64\) DOFs, batch 1) | \(T,h,\alpha,u,\ldots\) jointly in one \(R(U^{n+1})=0\) with consistent blocks / Krylov–JFNK |
| **Fracture strain (SI bar)** | Post-\(u\) symmetric \(\varepsilon\) via **`strain_tensor_from_bar_network_displacement`** when `node_positions` is `[N,3]` and the BC mask allows bar equilibrium — same kinematics as **`strain_tensor_for_fracture_after_mechanics`** after one solve; CI: `bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics` in `thmc_drying_shrinkage.rs` | **Within-step** \(u\!\leftrightarrow\!d\) stagger (mechanics still uses `damage` snapshot at step entry; single end-of-step `update_damage`); non-embedding / `matrix_features` fallback |
| **CI boundary** | Assembler: `thmc_t_alpha_newton_residual_preserves_hydro_mechanics_fields` (trial `h`,`u` untouched by Newton). Solver: `thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split` (transport does not branch on implicit \(T,\alpha\)). | Stacked \(\|R\|\) decrease; adaptive `dt`; memo milestones |

**Shipped split vs monolithic (summary):** `ThmcImplicitTAlphaNewtonConfig` (when `Some`) runs **damped Newton** only on the backward-Euler **\((T,\alpha)\)** block via `ThmcImplicitEulerThermalHydrationResidual` (dense FD Jacobian, \(\le 64\) stacked DOFs, batch 1). **`ThmcSolver::step` / `step_experimental` substep order (each outer pass, then once per step):** \((T,\alpha)\) → humidity \(h\) (Laplacian + drying) → bar equilibrium \(u\) if SI embedding allows → repeat `max_newton` times for split residuals; **`update_damage`** runs **once** after the outer loop with post-\(u\) strain on the SI path. Humidity does **not** use the implicit \((T,\alpha)\) branch (CI: `thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split`). The table rows above are the verified boundary vs track‑13 scope. **True monolithic** track‑13 work remains **open:** one residual \(R(U^{n+1})=0\) for the **fully coupled** step \((T,h,\alpha,u,\ldots)\) with consistent Jacobians / Krylov and adaptive `dt`. **Partial milestones already in CI:** `ThmcHydrationKinetics` calibration surface; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; `thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`; damped Newton lowers \(\|R\|_2\) on the \((T,\alpha)\) block (`thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm`, `thmc_implicit_euler_t_alpha_multi_newton_monotone_residual_norm_decrease`); linearized thermal Newton vs analytic decay in the same file. **Next acceptance:** \(\|R\|\) down across inner Newton on a 1D chain for the **stacked** THMC unknowns; `tol` early exit (test path may sync); linearized thermal block vs analytic mode under the **full** implicit map once assembled. **Research memo 13** above routes detail to this section + `docs/research/v0.4_track13_monolithic_newton_thmc.md`.


## Solver lanes — Statistical mechanics

<span id="solver-lane-statistical-mechanics"></span>

**Closed in this PR (partial):** `statmech_lj_bridge_contract` (`lj_bridge_scaling_non_regression`) on \(K \propto \varepsilon/\sigma^3\), \(\gamma \propto \varepsilon/\sigma^2\); **Johnson–Zollweg–Gubbins (1993)** LJ EOS reference grid in `statmech_lj_johnson_eos_reference` / `lj_johnson_1993_reference` (including explicit check that `upscale_potentials` **does not** match Johnson-derived bulk modulus — placeholder honesty); optional feature **`statistical-mechanics-johnson-reference`** exposes the same-name **`bulk_modulus_from_lj_state_johnson1993`** API on `statistical_mechanics` for side-by-side comparisons (see unit test in-module).

**Path sketch — Johnson / virial → `upscale_potentials` (target wiring, not shipped in the Burn tensor):**

| Stage | What is evaluated | State / parameters |
| --- | --- | --- |
| **Virial / MD** | \(P = \rho k_B T - \frac{1}{3V}\langle\sum_{i<j}\mathbf r_{ij}\cdot\mathbf F_{ij}\rangle\); then \(K_T=\rho(\partial P/\partial\rho)_T\) at matched protocol (cutoff, tail) | \(\rho, T\) plus potential parameters |
| **Johnson (1993) reference** | MBWR-style reduced EOS: \(P^*(\rho^*,T^*)\), \(K^*=\rho^*(\partial P^*/\partial\rho^*)_{T^*}\) in LJ reduced units (\(T^*=k_BT/\varepsilon\), \(\rho^*=\rho\sigma^3\)) | \((\rho^*,T^*)\) only in the analytic surface |
| **Physical \(K_T\)** | \(K_T = (\varepsilon/\sigma^3)\,K^*\) — [`lj_johnson_1993_reference::bulk_modulus_from_reduced`](../../src/physics/solvers/lj_johnson_1993_reference.rs) | joins \((\rho^*,T^*)\) with \((\varepsilon,\sigma)\) |
| **`upscale_potentials` (today)** | Placeholder \(K \propto \varepsilon/\sigma^3\), \(\gamma_{\mathrm{gc}}\propto \varepsilon/\sigma^2\) on **`[B,2]`** rows | **columns 0–1:** \((\varepsilon,\sigma)\) only — **no** \(\rho^*,T^*\) channel, so no Johnson/virial evaluation inside the tensor |

**Tensor channel design (future API):** extend the batch row so thermodynamic state is explicit, e.g. **`[B, 4]`** with columns \((\varepsilon,\sigma,\rho^*,T^*)\) in consistent reduced or SI conventions, **or** keep **`[B,2]`** LJ knobs and add a parallel **`[B,2]`** state tensor \((\rho^*,T^*)\) (or density \(+\) temperature in cartridge units) passed to a new `StatisticalBridge` method. The differentiable Burn path would broadcast the state pair into the same virial/EOS or tabulated+smooth-interp map as the `f64` reference lane.

**Feature gate sketch:** default builds: **`upscale_potentials`** only (research placeholder). **`statistical-mechanics-johnson-reference`:** adds scalar **`bulk_modulus_from_lj_state_johnson1993`** re-export for **`f64`** parity checks against Johnson \(K^*\); does **not** change the **`[B,2]`** tensor contract. A full Johnson/virial **inside** the Burn bridge requires the extended tensor (or config-carried default \((\rho^*,T^*)\), documented and test-gated) so physics is not hidden.

**Still open — physical bridge:** virial / coexistence / Kirkwood–Buff route from \((\varepsilon,\sigma)\) to \((K,\gamma_{\mathrm{gc}})\) with uncertainty bands.

**Next PR acceptance criteria:** (1) reference \(K\) from MD or published EOS at one state point; (2) `upscale_potentials` error vs reference &lt; stated tolerance; (3) document temperature / cutoff dependence.


## Solver lanes — Fracture

<span id="solver-lane-fracture"></span>

**Verified under `fracture-at2` (cumulative, see table row):** multi–length-scale **admissibility** smoke (`at2_length_scale_sweep_non_regression`); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not sharp-interface Γ-limit); **`at2_gamma_convergence_three_length_scales`** — three \((l_0,h)\) pairs with \(h/l_0=\tfrac14\), \(\psi^+\!=0\), exponential damage seed; discrete \(D_h\) within **&lt; 2%** of **`Gc`** and non-worsening under refinement (`tests/verification/fracture_gamma_convergence.rs`).

**Latest (harness hygiene only):** **`at2_discrete_surface_functional_toy_chain_matches_hand_total`** exercises **`discrete_at2_bar_surface_energy_1d`**, the same closed-form \(D_h\) sum used above (3-node hand total — **does not** extend Γ claims or \(\psi^+\) coverage). **Research:** explicit acceptance + proposed **`#[test]`** names for driven \(\psi^+\), multi-ratio \((l_0,h)\), staggered \(D_h\) vs \(G_c\), and THMC within-step stagger — [track 12 memo §7](research/v0.4_track12_staggered_fracture_mechanics.md#7-research-backlog-gamma-limit-psi-plus-multi-l0h-schedules).

**Still open:** Γ-limit / \(D_h\) certificates with **nonzero tensile driving** \(\psi^+\) and mechanics-coupled refinement; broader \((l_0,h)\) schedules than the fixed \(h/l_0\) triple; **within-step** THMC stagger so mechanics and fracture can alternate on updated \(d\) inside one `step` (today `damage_m` for transport/mechanics is fixed at step entry; fracture runs once after outer passes); dissipation vs \(G_c\) on refined meshes under staggered \(u\!\leftrightarrow\!d\) beyond compliance monotonicity.

**Next PR acceptance criteria:** (1) extend Γ-type or dissipation harness with \(\psi^+\!\neq 0\) and/or staggered bar loading; (2) **THMC SI path:** fresh \(\varepsilon(\mathbf u)\) after bar equilibrium **met** (above); optional **within-step** \(u\!\leftrightarrow\!d\) iterations or documented single-pass contract; (3) energy monotone band on benchmark chain.


## Solver lanes — Photonics

<span id="solver-lane-photonics"></span>

**Closed in this PR (partial):** uniform x-chain **TE \(E_y\)** curl–curl reduction equals scalar Helmholtz (`curl_curl_y_mode_matches_scalar_helmholtz`); same operator identity under **piecewise** node \(\varepsilon_r\) (`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`); Burn DEC primal matvec matches hand TE stencil (`dec_te_primal_tensor_matches_chain_stencil`); **default builds** (`photonics` **off**): `photonics_curl_curl_stub_default_build` locks [`PhotonicsSolver::solve_maxwell_curl_curl`] as a documented **no-op** (identity on representative chain tensors — not a physics solve).

**Still open:** DEC-assembled vector curl–curl on **2D/3D** patches (\(d_1\) / face topology); tensor permittivity; tighter Fresnel field match vs a **discrete** semi-infinite reference (current CI test uses bridged continuum target and a relaxed LS band).

**Closed partial — discrete DEC triangle (not photonics solve path):** [`tests/dec_identities.rs`](../tests/dec_identities.rs) ships **`dec_curl_d1_annihilates_gradient_on_triangle_faces_b2_burn`** (\(d_1(d_0\omega)=0\) through synthetic **`edges_b1`/`faces_b2`** COO tensors and Burn gather/scatter in [`dec_primal`](../src/physics/dec_primal.rs)) and **`dec_primal_d1_adjoint_identity_single_triangle_burn`** (unweighted \(d_1\) / \(d_1^\top\) Frobenius adjoint on the same face **COO**). **`dec_curl_d1_annihilates_gradient_on_triangle`** is the dense **`ring_b1`** matmul companion on the **same** CCW 3-cycle (hand-coded face sum); it is not the only annihilation check. **Incremental toward 2D patch curl–curl:** **`dec_curl_d1_annihilates_gradient_quad_split_two_faces_burn`** and **`dec_primal_d1_adjoint_identity_quad_split_two_faces_burn`** — a **4-node / 5-edge** quad split into two CCW triangles sharing diagonal **0–2**, so one global edge carries **two signed `faces_b2` columns** (curl annihilation + adjoint scatter on the shared edge). None of these call [`PhotonicsSolver::solve_maxwell_curl_curl`](../src/physics/solvers/photonics.rs) or lift incidence from a full mesh assembly.

**Next PR acceptance criteria:** (1) tighten `two_half_spaces_fresnel_te_no_pml_matches_analytic` LS margin and/or add discrete `r_disc`-only comparison; (2) extend DEC curl / adjoint coverage to **metric-weighted** operators and/or **cube** patches with **`faces_b2` (and higher)** populated from **assembled graph / manifold state**, beyond the hand-authored single-triangle **COO** used above — toward wiring full **2D/3D** vector curl–curl in photonics.


## Solver lanes — Acoustics

<span id="solver-lane-acoustics"></span>

**Closed in this PR (partial):** implicit Newmark on a 1-D periodic bar — `newmark_acceleration_matches_dense_reference_n8` / `_n128` vs dense reference (inner linear solve uses **f64** Cholesky; f32 factorization diverges at large `n`); `plane_wave_return_map_n64_l2_within_two_percent` and `plane_wave_return_map_n100_l2_within_two_percent` (same return-map / substep recipe; **n=64** and **n=100** gates; situates the n=128 **open return-map question** relative to the ignored n=128 harness); `plane_wave_h_refinement_second_order_band`; `undamped_energy_drift_under_half_percent_over_1000_steps` at **n=128** (energy proxy, not the return map).

**Still open — return map at brief mesh count:** the v0.4 brief’s **128-node** return map is not the default CI gate: with the **same** CFL-scaled substep recipe as the **n=100** test, **n=128** shows large discrete **phase slip** (relative discrete \(L^2\) back to `u₀` **~1.4** in baseline runs — order unity, not a mild exceedance of the 2% band). Opt-in ignored bracket: `plane_wave_return_map_n128_documented_phase_slip_band` (`#[ignore]`). Nodal `AcousticWaveSolver` without graph stiffness remains a separate surface.

**Follow-up brief — §R2.4 diagnostic:** integrating the same bar to the same spatial semi-discrete period \(T=2\pi/\omega_h\) with **16×** more equal substeps within \(T\) leaves the n=128 relative \(L^2\) to `u₀` near **1.39** (unchanged within float noise), so the mismatch is **not** resolved by substep refinement alone — see rustdoc on `AcousticNewmarkBar1dPeriodic` in `acoustics.rs`. Further options are summarized in [`../../composer_prompts/v0.4_phase_3_followup_for_composer.md`](../../composer_prompts/v0.4_phase_3_followup_for_composer.md) §R2.4.

**Dispersion / return-map fix options** (see follow-up brief §R2.4):
- **\(\Delta t\) calibration:** Tune the outer timestep and/or inner Newmark substeps against the **stability and accuracy** band of the coupled **spatial–temporal** scheme (CFL-style scaling on the bar graph). Substep refinement **within** a fixed semi-discrete period \(T=2\pi/\omega_h\) does **not** remove order-one phase slip if the dominant error is **spatial dispersion** (diagnostic above); any \(\Delta t\)-only remediation must be justified against that observation.
- **\(\omega_h\) / period calibration for the return map:** The return-map tests compare the state after one **target** period. If that target uses a **continuum-ideal** \(\omega_h\) while the semi-discrete operator supports a different discrete travelling mode (graph stiffness + Newmark), the “return” benchmark measures **phase mismatch vs reference**, not necessarily integrator failure. Options: derive the comparison period from the **actual** discrete harmonic for the mode under test (eigenpair / discrete dispersion) instead of plugging continuum \(\omega_h\) alone; or relax/revise the brief mesh count after documenting the discrete dispersion curve.

**Policy — n=128 gate:** Do **not** remove `#[ignore]` from `plane_wave_return_map_n128_documented_phase_slip_band` or promote **n=128** to a default CI return-map gate until either (a) the **Next PR acceptance criteria** below are satisfied (strict n=128 return-map gate passes), or (b) the brief / grid target is explicitly revised and CI reflects it.

**Warning — what CI does *not* certify:** `undamped_energy_drift_under_half_percent_over_1000_steps` (n=128) bounds the **discrete mechanical-energy proxy** over many substeps; it does **not** imply return-map agreement with the initial sinusoid after one semi-discrete period \(\,T=2\pi/\omega_h\,\) at **n=128**. Treat **`plane_wave_return_map_n64_l2_within_two_percent`** and **`plane_wave_return_map_n100_l2_within_two_percent`** as the default return-map physics gates until an n=128 strict gate ships or the brief count is revised.

**Next PR acceptance criteria:** (1) return-map relative \(L^2\) &lt; 2% at **n=128** with a justified timestep/substep recipe, **or** document and CI a revised mesh count; (2) if the slip is fixed, promote a strict n=128 gate and drop or narrow the ignored bracket test; (3) optional: graph-based acoustic stiffness path beyond nodal contraction.


## Solver lanes — Electrochemistry

<span id="solver-lane-electrochemistry"></span>

**Closed in this PR (partial):** Poisson–NP **explicit Picard** coupling in `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep); MVP path-chain Thomas Poisson with **harmonic edge \(\varepsilon_{i+1/2}\)** and net charge RHS (**`\nabla\cdot(\varepsilon\nabla\phi)=-(c^+-c^-)F`**, consistent uniform-\(\varepsilon\) reduction); SG flux **`mesh_spacing`** scaling (**`J\propto D/h`**; Poisson stencil is **unit-graph** in index space — not rescaled by `mesh_spacing`; see `electrochemistry` rustdoc); **tensor SG Bernoulli** evaluated elementwise via **`f64`** exponentials for large \(|z\Delta\phi|\); **MVP-chain implicit backward Euler** + damped Newton (`try_solve_pnp_backward_euler_newton_chain`, host `f64`) with residual regression tests in `pnp_debye_layer.rs`. **Wiring note:** implicit Newton is **not** merged into `solve_pnp_step_experimental`; the default `solve_pnp_step` path remains explicit Picard-only. Research users can opt in via **`pnp_implicit_newton_chain`** + **`solve_pnp_step_dispatch`**. **λ_D admissibility harness** (`debye_screening_admissibility_check`) sets **`mesh_spacing = h = L/(N-1)`** so SG matches the physical fit abscissa, uses **`debye_implicit_newton_linearized_sg_for_lambda_d_gate()`** (`linearize_sg_fickian: true`) for analytic Jacobian / tractable `--release` runs on **`N=256`**. **CI-fast dispatch smoke:** `debye_implicit_dispatch_short_horizon_smoke` … **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** … Neither replaces the ignored exponential-fit λ_D gates.

**Still open:** coupled implicit Newton on **general graphs**; variable \(\varepsilon\) on **non-chain** Jacobi Poisson relaxers; **sparse/banded Jacobian** for **full SG** implicit Newton at **N=256** (dense FD columns remain costly); **λ_D quasi-steady gates** (both tests **`#[ignore]`** in source): on the **`N=256`** admissibility harness with **uniform** \(\varepsilon_r\), **`mesh_spacing = h`**, and linearised-SG implicit Newton via `debye_implicit_newton_linearized_sg_for_lambda_d_gate()`, exponential LS **`λ_eff`** vs continuum **`λ_D`** remains **off by \(\mathcal O(1)\)** — baseline **`--release`** sample **\(λ_{\mathrm{eff}}\approx 3.71\)** vs **\(λ_D=1/\sqrt2\approx 0.707\)** (**\(|\lambda_{\mathrm{eff}}-\lambda_D|/\lambda_D \approx 4.25\)** — outside the Track F ±5 % / ±15 % bands **without** widening them). Default **CI** smokes (`debye_implicit_dispatch_short_horizon_smoke`, **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`**) use the shipped scaling mix (**unit-graph** Poisson + default **`mesh_spacing = 1`** on SG — **not** the **`mesh_spacing = h`** admissibility bundle); see those tests’ module docs. Likely contributors to the ignored gates include **unit-graph Poisson vs physical \(h\)** in the coupled map, quasi-steady / window choice, and discrete screening — needs calibration before **`#[ignore]`** removal; φ₀=4 V_T sibling tracks the same scale issue behind the 25 mV gate.

**Next PR acceptance criteria:** (1) residual \(\|R_\Phi\|+\|R_c\|\) decreases under damped Newton on a **non-chain** benchmark or matrix-free JVP path; (2) mass conservation band on closed graph; (3) double-precision option for large \(|z\Delta\phi|\); (4) sparse / cheaper Jacobian or reduced horizon so **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** can **run on default CI** (drop **`#[ignore]`**), then extend **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** (φ₀=4 V_T).


## Solver lanes — Rheology

<span id="solver-lane-rheology"></span>

**Closed in this PR (partial):** **64×16** channel graph **edge-count** scaffold (`channel_lattice_64x16_quadrilateral_edge_count`, **1968** undirected edges on the quad stencil used by `rheology_poiseuille` harnesses). Live analytics / regularized 1-D references / Chorin short smoke / \(\lambda\)–thixotropy checks run under **`--features rheology-bingham`** (see table row).

**Still open:** (1) **`chorin_developed_channel_centreline_vs_regularized_reference`** — `#[ignore]` (stub: centreline vs regularized reference awaits pressure Poisson + BCs); no runtime until pressure Poisson + BCs land. (2) **`chorin_steady_channel_64x16_vs_regularized_reference`** — `#[ignore]` (surrogate pressure Poisson ~10³/step, dt-independent on 65×17 — see test docstring); the shipped Chorin step’s **surrogate** pressure Poisson (`rheology_flow.rs`: fixed-count Richardson on a tangent-projected gradient) yields **dt-independent** blow-up on the **65×17** developed-channel graph (~\(10^3\)× amplification per step to NaN — see that test’s docstring); steady centreline vs regularized analytic is blocked until a true pressure solve (or MAC-style staggered grid) plus inlet/outlet treatment. **Active regression guard:** `chorin_surrogate_poisson_amplification_regression_guard` brackets the two-step \(\|u\|_\infty\) amplification band on **65×17** (revise or drop when a real pressure Poisson ships). **Design:** discrete Poisson on [`TopologicalLaplacian`](../src/physics/laplacian.rs), BC matrix for channel I/O, Chorin projection wiring — [research/rheology_pressure_poisson_roadmap.md](research/rheology_pressure_poisson_roadmap.md).

**Warning — what CI does *not* certify:** `chorin_single_step_finite_smoke`, quadrature / analytic Bingham checks, and **`channel_lattice_64x16_quadrilateral_edge_count`** do **not** validate **developed** Chorin flow against plane Poiseuille on the full channel graph. Until the surrogate pressure step is replaced (or staggered MAC + inlet/outlet), these opt-in `#[ignore]` tests remain the documented boundary; do not infer channel physics from the small-lattice smoke alone.

**Next PR acceptance criteria:** (1) centreline \(L^2\) error vs analytic &lt; 15% at stated `dt` / step count on **64×16**; (2) pressure correction Poisson RHS aligned with discrete divergence of \(u^\*\); (3) optional env-gated long benchmark (`UMST_RHEOLOGY_POISEUILLE_BUDGET_MS`).


## Solver lanes — Topology / shell (Tracks B + L)

<span id="solver-lane-topology-shell"></span>

**Related pointers:** Striatus ship checks are summarized in **Verification scope (remaining)** above and in [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md); this section expands **Track B / Track L** artefacts and commands (**B6 / B8**).

**Source brief:** [`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md) — **Track B** (topology shell / Striatus), **Track L** (GIF + committed artefacts).

### v0.3 vs v0.4 honesty

- The brief defines **target** public filenames and thresholds for **v0.4**. A checkout may still ship **v0.3-only** names (e.g. `striatus_shell_v0.3.*`) or lack rib-pattern completion — **do not** claim “v0.4 Striatus shell complete” until the **exact** filenames below exist, meet **Track L** size/structural gates, and **Track B8** checks are satisfied (tests + `print_ready.json`).
- **CI vs brief:** default cartridge CI runs **`shell_topology_rib_pattern_quick`** (`cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental`). **Full B6** (`shell_topology_rib_pattern_full_v04`, **`#[ignore]`**): **`UMST_SHELL_RIB_PATTERN=1`** + `cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental -- --ignored` — **40×40×4**, **200** Adam steps (optional **`UMST_SHELL_RIB_FULL_ITERS`** in **1…200** for smoke); multi-minute CPU typical after PCG residual early exit. The brief’s proof may stay env-gated; see cartridge **[`../../umst-concrete-cartridge/docs/Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md)** preamble for the same summary.
- **B6 bar equilibrium:** `packed_bar_network_equilibrium` caps PCG at **`min(max_cg_iterations, 3N)`** and **early-exits** when \(\|P(f-Ku)\|_2 \le \max(\texttt{pcg\_tolerance},\texttt{cg\_tolerance})\,\|Pf\|_2\). The full B6 harness raises `max_cg_iterations` and uses **`E_{\min} = 10^{-3} E_0`** at **40×40×4** for solve fidelity and SIMP void–solid contrast; optional **`UMST_SHELL_RIB_FULL_ITERS`** (see cartridge test `//!`) shortens the ignored harness for smoke. **`UMST_SHELL_HELM`:** default **off** on the Burn tape (same as `optimize_shell_3d`); **`UMST_SHELL_HELM=1`** enables graph Helmholtz in-loop (opt-in; scatter backward can fail at large Striatus `N` on Burn 0.13). Details in `shell_topology_rib_pattern.rs` module docs.

### NaN / conditioning — `optimize_shell_3d` (40²×4)

**Where NaNs enter:** [`AdjointCompliance::forward_and_loss`](../src/physics/adjoint.rs) builds nodal stiffness from `SimpElasticMaterial`, runs inner [`VectorMechanicsSolver::packed_bar_network_equilibrium`](../src/physics/mechanics.rs) (projected **f32** PCG, capped iteration count, optional Jacobi preconditioner), then `masked_dot` → raw compliance `c_raw`. The discrete-adjoint surrogate re-lifts inner tensors with `Tensor::from_inner`; non-finite **`u`** or edge elongations **`δ_e`** poison **`c_raw`** and the surrogate. **Self-weight** body force scales with filtered density on large slabs and can drive bar PCG into **non-finite** `u` in **`f32`** (cartridge `shell_demo_smoke` uses traction-only for that reason). **Helmholtz** Richardson on the **Burn Autodiff** tape can amplify round-off on large 3-D graphs; [`HelmholtzFilter::apply`](../src/physics/topology_filter.rs) honours the constructor iteration count (it no longer forces a **400**-step minimum over a **240**-step caller). **In-loop [`VolumeProjection`]** bisection on AD can trip Burn scatter backward limits on some slab sizes; set **`UMST_SHELL_VOL_LOOP=0`** to skip it during optimisation (mean ρ can drift toward **0.5** and worsen bar conditioning on **40²×4** unless self-weight / Helmholtz / other knobs are aligned). Default in the cartridge example remains **on** (`1` or unset) so mean VF tracks **`UMST_SHELL_VF`**.

**Reproduce (cartridge example — finite with default env flags):**

```bash
cd umst-concrete-cartridge
cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render'
```

**Stress ill-conditioning (historical NaN at iter 1 — low PCG budget + tiny `E_min`):**

```bash
cd umst-concrete-cartridge
UMST_SHELL_MAX_CG=200 UMST_SHELL_E_MIN_REL=5e-9 \
  cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render'
```

**Full B6 ignored harness:**

```bash
cd umst-concrete-cartridge
UMST_SHELL_RIB_PATTERN=1 cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental shell_topology_rib_pattern_full_v04 -- --ignored
```

**Mitigations:** raise **`UMST_SHELL_MAX_CG`**, keep **`UMST_SHELL_E_MIN_REL≈1e-3`** (Track B6 **`10^{-3}E_0`**), keep **`UMST_SHELL_PCG=1`**, **`UMST_SHELL_SELF_WEIGHT=0`** for traction-only while **`f32`** bar PCG is hardened, **`UMST_SHELL_HELM=0`** to A/B the graph filter on AD, **`UMST_SHELL_VOL_LOOP=0`** only when debugging backward (expect mean ρ near **0.5** without other constraints); future **f64** inner equilibrium remains deferred.

### Exact artefact paths (relative to `umst-concrete-cartridge/` repo root)

| Role | Path |
| --- | --- |
| Emergence animation | `notebooks/_artifacts/striatus_emergence.gif` |
| Mesh export (v0.4 name) | `notebooks/_artifacts/striatus_shell_v0.4.stl` |
| Print-ready JSON (B7 / B8 / L) | `notebooks/_artifacts/striatus_shell_v0.4.print_ready.json` |
| Optional mesh | `notebooks/_artifacts/striatus_shell_v0.4.obj` |

**Anti-goal (brief):** do **not** commit `crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy` — only final **`striatus_emergence.gif`**, **`striatus_shell_v0.4.stl`**, and **`striatus_shell_v0.4.print_ready.json`** are intended as committed artefacts (plus optional `.obj`).

### Track L — regeneration command (verbatim from brief)

Run from the **workspace root** that contains `umst-concrete-cartridge/` (the brief’s first line `cd`s into it):

```bash
cd umst-concrete-cartridge
rm -rf crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy
rm -rf notebooks/_artifacts/frames notebooks/_artifacts/striatus_emergence.gif
UMST_SHELL_DUMP_ITER=1 UMST_SHELL_DUMP_STRIDE=10 UMST_SHELL_ITERS=200 \
    bash notebooks/_run_shell_demo.sh
```

**Track L acceptance (brief):** `notebooks/_artifacts/striatus_emergence.gif` exists, **≤ 5 MB**, **≥ 30 frames**; `notebooks/_artifacts/striatus_shell_v0.4.stl` exists, **≤ 8 MB**, watertight, **`genus ≥ 1`**, **`volume_fraction ∈ [0.10, 0.25]`**; commit GIF and STL to git.

**Print-ready / structural JSON:** after marching-cubes and gates, run **[`../../umst-concrete-cartridge/notebooks/export_print_ready.py`](../../umst-concrete-cartridge/notebooks/export_print_ready.py)** so **`striatus_shell_v0.4.print_ready.json`** includes the B7 variance/genus/volume-fraction fields; **[`../../umst-concrete-cartridge/notebooks/tests/test_print_ready.py`](../../umst-concrete-cartridge/notebooks/tests/test_print_ready.py)** asserts them.

### Track B — shell acceptance (exact names)

| Item | Location |
| --- | --- |
| Optimiser example | `umst-concrete-cartridge/crates/umst-concrete-cartridge/examples/optimize_shell_3d.rs` |
| Smoke / determinism | `crates/umst-concrete-cartridge/tests/shell_demo_smoke.rs` |
| Rib-pattern test (B6) | `crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs` — brief thresholds at **40 × 40 × 4**, **200** iters, seed **42**: end volume fraction **±1 %** of `target_vf = 0.15`; greyness **mean(4ρ(1−ρ)) < 0.15**; spatial variance of **ρ** over **xy** **> 0.1**; compliance at end **<** compliance at iter **1 × 0.6**. |

**Track B8 (brief “all of”):** (1) `shell_demo_smoke` passes; (2) `shell_topology_rib_pattern` at **40 × 40 × 4**; (3) GIF shows rib formation (subjective; objective backup: variance/genus checks); (4) **`striatus_shell_v0.4.print_ready.json`** has **`genus ≥ 1`** AND **`variance ≥ 0.1`** AND **`volume_fraction ∈ [0.10, 0.25]`**.


## Track J — documentation honesty (v0.4 brief)

Cross-check: **[`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md)** — Track J in that file.

- **J1.** This document’s table is the public solver ↔ lane ↔ verification mapping; prose uses “implemented” only where the **Verification** column matches the CI rule in the header.
- **J2.** [`README.md`](../README.md) summarizes solver maturity and links here plus the v0.4 brief at [`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md) (correct relative URL from this `docs/` file when `composer_prompts/` sits beside `umst-manifold/` in the parent workspace). [`README.md`](../README.md) uses `../composer_prompts/...` for that brief because it is one directory shallower.
- **J3.** Short index: [`PROOF-STATUS.md`](PROOF-STATUS.md) — one row per main-table solver with **`lane`**, **`benchmark_test`**, and **`verification_status`** (`mechanised` \| `analytic-benchmark` \| `literature` \| `none`); stable rows must not leave **`benchmark_test`** empty. Extended verification notes and research-track pointers stay in this file.
- **J4.** [`References.bib`](References.bib) holds a Track-J bibliography skeleton (Bendsøe–Sigmund, Allaire, Sigmund 1997, Stolpe–Svanberg, Scharfetter–Gummel, Buckingham, Roussel et al., Manzano et al., CEB-FIP Model Code 2010, Wangler et al.); extend with DOIs and page-accurate metadata as solver rustdoc cites them.

