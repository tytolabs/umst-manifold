# Solver status (v0.4)

**Verification index:** one page of section → memo → tests — [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md). **Completion matrix** (numbered **Verification scope (remaining)** items → PR slices): [`VERIFICATION_COMPLETION_MATRIX.md`](VERIFICATION_COMPLETION_MATRIX.md).

From the `umst-manifold/` tree in this workspace, the v0.4 solver-completion brief is **`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`** (the same relative path from `umst-manifold/docs/`).

For planned **solver multithreading** (rayon hot-loops, BLAS interaction, feature surface), see the [v0.4 multithreading brief](../../composer_prompts/v0.4_multithreading_solver_parallelisation.md).

Public mapping of solver-facing modules to **lane** (`solver-stable` vs `solver-research` in `Cargo.toml`), **verification** tests, and notes.

**CI** (`.github/workflows/rust.yml`): **`solver-status`** — `python3 scripts/check_solver_status.py --check-paths --check-memo-links --check-statmech-verification-set` (**Verification** test paths exist; memo links to `research/*.md` beside this file and backticked ``docs/research/*.md`` paths under the repo root exist; the `solvers::statistical_mechanics` row lists the full `statmech_*` verification set — see `scripts/check_solver_status.py` docstring). **`lint`** (merge gate) — `cargo fmt --all -- --check` and `cargo clippy --all-targets --features solver-experimental -- -D warnings` (same feature union as meta `solver-tests` / v0.4 ship checklist). **`research-stack`** — `cargo test --verbose --release --features solver-experimental` with one retry on failure; runs on pushes to `main` and on `workflow_dispatch`.

**Track B6 (CI vs opt-in):**

| Gate | Test | Command sketch |
| --- | --- | --- |
| **Green on default CI** | `shell_topology_rib_pattern_quick` | `cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental` (add `--release` for faster numerics if desired). **Quick harness (2026-05-11):** default **9×8×3**, **≤20** Adam outers (`UMST_RIB_QUICK` default), roof ramp **r=0.55** at **96 Pa** nodal scale, Heaviside **β=28**, **`DensityNet`** width **56**, Adam **0.0075**; **greyness** = roof-slice `mean(4ρ(1−ρ))` on **post–volume-projection** ρ, asserted **<** `4·vf·(1−vf)−0.008` at measured **vf** (release sample **vf≈0.15**, **greyness≈0.490**, **g_uni=0.510**, **c1/c0≈0.47**, **xy_var≈5.6×10⁻⁴**). Full B6 **`<0.15`** on **volume** mean at **40²×4 / 200** outers remains on **`shell_topology_rib_pattern_full_v04`**. |
| **`#[ignore]` full Striatus B6** | `shell_topology_rib_pattern_full_v04` | Set **`UMST_SHELL_RIB_PATTERN=1`**, then `cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental shell_topology_rib_pattern_full_v04 --release -- --ignored`. Put **`--release` on the `cargo test` side** (before `--`); flags after `--` are forwarded to the test binary and are not compile profiles. |

**Full B6:** **40×40×4**, **200** Adam outers by default; **`UMST_SHELL_RIB_FULL_ITERS`** (clamped **1…200**) shortens the loop for smoke only — **one outer** still runs the full Striatus-scale **forward + autodiff backward** and can take **many CPU minutes** in `--release**, and **does not** satisfy the brief greyness / compliance-ratio gates (those need the full **200**-outer schedule). Budget **hours** for the default **200**-outer proof on a typical workstation unless noted otherwise. **Greyness / roof (2026-05-11 log + today's defaults):** see **Solver lanes — Topology / shell → [2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)** — last documented **200**-outer **`--release`** miss **~0.51** vs **< 0.15** (**finite**, **~7655 s**); short smokes show **`c1`** improves with **x** ramp more than greyness; **no** fresh **200**-outer in-repo proof that greyness clears.

**Rule:** use “implemented” in prose below only where the listed **Verification** tests exercise the claim on CI (default `cargo test` path for stable rows; `--features solver-experimental` for research rows unless noted).


| Solver                                                                                  | Lane     | Verification                                                                                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `solvers::topology_solver` (`TopologyOptimizer`, density evolution)                     | stable   | `tests/topology_continuation.rs`, `tests/topology_filter.rs`                                    | Feature `topology-density-evolution`. Heat-equation / SIMP-adjacent evolution; conservative stable-lane entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `mechanics::VectorMechanicsSolver`, `adjoint::AdjointCompliance`, bar / plate mechanics | research | `tests/verification/mechanics_analytic.rs`, `tests/verification/adjoint_compliance_analytic.rs` | Features `mechanics-voigt-cauchy`, `topology-density-evolution` (Q1 hex path). **Live:** cantilever chain; **`packed_bar_network_equilibrium_uniform_axial_strain_tip_load_distinct_from_acoustic_newmark_bar_path`** (Solver-Status **#10** — vector static bar network vs scalar **`acoustics-newmark`**); **adjoint:** 4-node bar compliance + gradient vs FD. **Plate:** Q1 hex shear-locking aware checks; Kirchhoff cross-check is `plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band` (**ratio band** `10^{-4}<w/w_K<2\times10^{-3}` with bilinear-consistent top pressure `ExtrudedPlateMechanics::body_force_top_uniform_pressure`; not a within-5% thin-plate error gate; full Kirchhoff SSSS on brick not yet covered by default CI — follow-up brief [§R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate)). **Geometric NL / finite-strain gap:** [mechanics scope memo](research/mechanics_geometric_nonlinearity_scope.md).                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `solvers::fracture_field` (`PhaseFieldFractureSolver`)                                  | research | `tests/verification/fracture_gamma_convergence.rs`, `tests/verification/staggered_fracture_mechanics_chain.rs`, `tests/verification/staggered_ud_loop_milestone.rs`, `tests/verification/thmc_drying_shrinkage.rs` (kinematic THMC ↔ helper parity) | Feature `fracture-at2`. **Implemented:** inner AT2 damage relaxation (red–black, odd outer passes); `at2_length_scale_sweep_non_regression` (multi–\(l\) admissibility smoke); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not sharp-interface Γ-limit); **`at2_gamma_convergence_three_length_scales`** — partial Γ-type check: three \((l_0,h)\) pairs, \(h/l_0=\tfrac14\), \(\psi^+\!=0\), discrete \(D_h\) vs **`Gc`** \(\lt 2\%\) and non-worsening across refinement (fixed-strain relaxation); **`at2_discrete_surface_functional_toy_chain_matches_hand_total`** (hand-checked discrete \(D_h\) on a 3-node toy via shared `discrete_at2_bar_surface_energy_1d`); **`staggered_fracture_compliance_monotone_increasing`** (staggered bar compliance vs outer iterations). **Milestone 1:** documented `update_damage_staggered` harness on a 3-node chain with **mechanics equilibrium** strain at degraded stiffness, an **analytic strain surrogate**, and **`strain_tensor_for_fracture_after_mechanics`** for **post-mechanics** symmetric strain on the same chain (see `staggered_ud_loop_milestone` module docs). **THMC (`thmc-coupled`):** with SI `node_positions` `[N,3]` and compatible BC mask, **`ThmcSolver::step`** feeds fracture from **`strain_tensor_from_bar_network_displacement`** on the equilibrium displacement (same kinematics as post-mechanics helper; `thmc_drying_shrinkage::bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`). **Remaining:** Γ-limit with driven \(\psi^+\); broader \((l_0,h)\); within-step THMC \(u\!\leftrightarrow\!d\); embedding / `matrix_features` fallbacks — **Solver lanes — Fracture** + [track 12 memo](research/v0.4_track12_staggered_fracture_mechanics.md) §2.3 / §7 (§7 = proposed acceptance + test names only). **Compressive crushing / DP–cap plasticity:** not implemented; AT2 drive uses spectral **ψ⁺** (tensile principal strains) only — `docs/research/concrete_compressive_plasticity_scope.md`; composer context [`v0.4_phase_2_3_master_plan.md`](../../composer_prompts/v0.4_phase_2_3_master_plan.md) (P2.4 fracture, P3.1 staggered mechanics). **Trust:** highly compressed slender struts have **no** in-repo crushing/yield cap. |
| `solvers::acoustics` (`AcousticWaveSolver`, `AcousticNewmarkBar1dPeriodic`)             | research | `tests/verification/acoustics_plane_wave.rs`                                                    | Feature `acoustics-newmark`. **Live:** `newmark_acceleration_matches_dense_reference_n8`, `newmark_acceleration_matches_dense_reference_n128` (f64 dense ref), **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`**, **`plane_wave_return_map_n128_l2_within_two_percent`** (return map after one lumped eigenperiod \(T=2\pi/\Omega\) with \(m=\rho\Delta x\); same CFL-scaled substep recipe); `plane_wave_h_refinement_second_order_band`; `undamped_energy_drift_under_half_percent_over_1000_steps` (**n=128** energy proxy). Inner linear solve uses **f64** Cholesky (f32 factorization diverged at large `n`). **`AcousticWaveSolver`** remains graph-free nodal contraction only. See **Solver lanes — Acoustics**. |
| `solvers::electrochemistry` (`ElectroChemicalSolver`)                                   | research | `tests/verification/pnp_debye_layer.rs`                                                         | Feature `electrochemistry-pnp` (canonical Track I; forwards to `electrochemistry-mvp` for `#[cfg]`). **Verified on default CI (`pnp_debye_layer` when `electrochemistry-pnp` is enabled; root **`research-stack`** runs **`--release`):** SG NP + path-chain **Thomas** Poisson (`try_solve_poisson_chain_thomas`); **explicit Picard** only in `solve_pnp_step` → `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep, \(\rho(c^{(k)})\!\to\!\Phi\), then \(c^{n+1}=c^n-\Delta t\,\nabla\!\cdot J(\Phi,c^{(k)})\)); **`NewtonPnpContext` is not used on that path.** Tests: Fickian zero-field, screening smoke, `picard_coupling_iters_finite_smoke`, `backward_euler_implicit_newton_matches_split_in_linearized_small_dt_limit`, **`debye_implicit_dispatch_short_horizon_smoke`** (Track 14 **`solve_pnp_step_dispatch`** + **`pnp_implicit_newton_chain`**, short chain), **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** (same dispatch wiring with default **`mesh_spacing = 1`**; samples implicit BE `‖R‖₂` along a Debye-style trajectory — **orthogonal** to the **`mesh_spacing = h`** λ_D LS gates below). **Quasi-steady λ_D exponential LS gates (default `pnp_debye_layer`, not `#[ignore]`):** **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** (φ₀ = 1 V_T, interior LS **`λ_eff`** vs continuum **`λ_D`**, **±11 %**) and **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** (φ₀ = 4 V_T, **±15 %**) use **`solve_pnp_step_dispatch`** + **`pnp_implicit_newton_chain: Some(debye_implicit_newton_linearized_sg_for_lambda_d_gate())`** — **`linearize_sg_fickian: true`**, **`max_newton_iters: 24`**, host **`f64`** residual tol **`1e-9`** (Fickian-linearised SG in the implicit BE residual → **analytic** Jacobian; **not** full nonlinear SG at Newton level). Harness sets **`mesh_spacing = h = L/(N-1)`** with Poisson **\(h^2\)**-scaled interior RHS consistent with the SG **`J\propto D/h`** map; tail-heavy LS window **≈0.58…0.97** on interior indices at **`N=256`**, **`dt`·`steps` = `1.5e-3`×`10_000` ≈ 15** nondimensional time units. **`linearize_sg_fickian: true`** (Debye-gate / analytic path): each Newton correction is **\(O(N)\)** via **three Thomas solves** (no dense \((3N)\times(3N)\) solve). **`linearize_sg_fickian: false`** (**chain** implicit Newton): **node-major band** column FD Jacobian (envelope **O(N·bw)**); Newton correction via **dense expand + Gauss** on that band (**O(dim³)** per linear solve with **dim=3N** — tractable only under **`max_chain_nodes`** / modest **N**; **not** a pivot-safe **O(N·bw²)** band LU). **`full_sg_frozen_jacobian_inner_iters`** can freeze the assembled Jacobian across inner Newton iterations. **Runtime honesty:** the pair is **~0.7 s** in **`--release`** on typical laptops but **~10 s each** in unoptimised **`cargo test`** — prefer **`--release`** for local reruns. **Remaining:** pivot-safe **O(N·bw²)** band factorisation or Krylov correction **without** **O(dim³)** dense expand at large **N**; sparse layout on **general graphs**; variable \(\varepsilon\) on **non-chain** relaxers; general-graph implicit Newton; large-\(|z\Delta\phi|\) f32 robustness — **Solver lanes — Electrochemistry** and `docs/research/v0.4_track14_implicit_newton_pnp.md`.
| `solvers::photonics` (`PhotonicsSolver`, `PhotonicsHelmholtzSolver`)                    | research | `tests/verification/photonics_fresnel.rs`, `tests/verification/photonics_curl_curl_stub_default_build.rs` | Feature `photonics-fdfd` (canonical Track I; forwards to `photonics` for `#[cfg]`); deprecated alias `photonics-scaffold` → `photonics-fdfd`. **Implemented:** scalar TE FDFD Helmholtz (Thomas + PML); `curl_curl_y_mode_matches_scalar_helmholtz` and **`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`** exercise `PhotonicsSolver::solve_maxwell_curl_curl` on a uniform x-chain (\(E_y\) from \(J_y\)), including **non-uniform** node \(\varepsilon_r\); `dec_te_primal_tensor_matches_chain_stencil` (DEC primal matvec vs hand TE stencil); `two_half_spaces_fresnel_te_no_pml_matches_analytic` (Dirichlet-closed chain vs bridged continuum Fresnel + residual checks, loose field LS tol). **Default lane:** `photonics_curl_curl_stub_default_build` asserts `solve_maxwell_curl_curl` is an identity when the `photonics` feature is **off** (documented stub). **Remaining:** full DEC vector curl–curl on 2D/3D patches; tensor permittivity; tighter discrete semi-infinite Fresnel calibration. See **Solver lanes — Photonics**.                                                                                                                                                                                                                                                           |
| `solvers::rheology_flow` (`BinghamFlowSolver`)                                          | research | `tests/verification/rheology_poiseuille.rs`                                                     | Feature `rheology-bingham`. **Implemented:** analytic Poiseuille / plug-width / no-slip; regularized Bingham centreline and small-\(\varepsilon\) vs Buckingham profile; Chorin `chorin_single_step_finite_smoke`; **pressure Poisson:** Jacobi-preconditioned CG on \(-\mathcal{L}\) (`rheology_flow.rs`); **`f02120d`:** hardened Jacobi-PCG pressure; **`chorin_surrogate_poisson_amplification_regression_guard`** (historical name; two-step \(\|u\|_\infty\) bracket on **65×17**); `chorin_channel_65x17_thirty_substeps_remain_finite`; **`chorin_developed_channel_centreline_vs_regularized_reference`** + **`chorin_steady_channel_64x16_vs_regularized_reference`** on **`research-stack`** (**no `#[ignore]`** — short-horizon **65×17** smokes: finite ‖u‖/‖p‖, centreline below developed regularized reference / ‖u‖∞ band); Roussel \(\lambda\) explicit-step parity + Wangler-style quiescent yield-growth fit; `channel_lattice_64x16_quadrilateral_edge_count` (graph scaffold). **Not default `cargo test` without features:** lane needs **`rheology-bingham`** (bundled in root **`research-stack`**). **Still not CI at default stable union:** long-run steady \(L^2\) / plug-width vs the regularized 1-D reference (multi-thousand-step acceptance) — research harness when MAC + open BCs land. See **Solver lanes — Rheology**. |
| `solvers::thmc` (`ThmcSolver`, …)                                                       | research | `tests/verification/thmc_drying_shrinkage.rs`                                                   | Feature `thmc-coupled`. **Implemented:** drying + shrinkage band; explicit transport + hydration via configurable `ThmcHydrationKinetics`; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; **track 13 partial:** backward-Euler **thermal + hydration \(\alpha\)** residual in `thmc_residual::ThmcImplicitEulerThermalHydrationResidual` (`thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`, `thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm` for one damped Newton on the same \((T,\alpha)\) block). **Track 13 — monolith bookkeeping (Phase 1–4):** Phase **1** — field-major stacked flat residual including a **placeholder** \(R_u=m(\mathbf u-\mathbf u^n)\) tail + `evaluate_quasi_static_r_u` vs `projected_bar_equilibrium_residual` (`thmc_implicit_euler_t_h_alpha_u_placeholder_r_u_and_flat_layout_two_nodes`, `thmc_r_u_zero_at_solved_equilibrium_two_node_chain`); Phase **2** — `assemble_with_quasi_static_r_u` / `stacked_flat_residual_field_major_quasi_static` + L² parity (`thmc_monolithic_residual_blocks_consistent_two_nodes`); Phase **3** — dense damped Newton on field-major \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) (`one_damped_newton_step_with_quasi_static_r_u`, `damped_newton_iterations_with_quasi_static_r_u`; CI `thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes`); Phase **4** (notional shrink **increment** in quasi-static \(R_u\), coupling plan §4) — optional `ThmcImplicitEulerThermalHumidityHydrationResidual::ru_shrinkage_water_cement_ratio` feeds per-edge \(\Delta\varepsilon_{\mathrm{sh}}(h^n\to h^{\mathrm{trial}})\) into `projected_bar_equilibrium_residual` (`shrink_strain_from_saturation_loss_tensor`, `thmc_quasi_static_r_u_shrink_increment_flat_humidity_parity_two_node_chain`, `thmc_quasi_static_r_u_shrink_increment_raises_norm_when_humidity_drops_two_node_chain`) — roadmap: [v0.4_track13_monolithic_thmc_mechanics_coupling_plan.md](research/v0.4_track13_monolithic_thmc_mechanics_coupling_plan.md). **Phase 5 (integration, small-DOF) — `3e93a0a`:** [`ThmcSolver::monolithic_thmc_newton`](../src/physics/solvers/thmc.rs) wires `ThmcImplicitEulerThermalHumidityHydrationResidual::damped_newton_iterations_with_quasi_static_r_u` inside [`ThmcSolver::step`](../src/physics/solvers/thmc.rs) → **`step_experimental`** per outer pass when `batch==1`, `[N,3]` `node_positions`, \(\mathrm{DOF}_\mathrm{stack}\le 64\), **`drying_last_node_evaporation_k == 0`**, mutually exclusive with `implicit_t_alpha_newton`; optional call-site guard [`ThmcSolver::step_monolithic_implicit`](../src/physics/solvers/thmc.rs); **`ThmcMonolithicNewtonConfig::tol`** / **`relative_tol`** wire stacked \(\|R\|_2\) early-exit predicates into `damped_newton_iterations_with_quasi_static_r_u` (host reads; see [`thmc_residual.rs`](../src/physics/solvers/thmc_residual.rs)); CI `thmc_step_monolithic_newton_*` (including tol parity tail on `thmc_step_monolithic_newton_matches_standalone_dense_newton_two_nodes`), `thmc_monolithic_newton_residual_tol_early_exit_truncates_norm_trail`, `thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes` in `thmc_drying_shrinkage.rs`. **Fracture coupling (SI bar path):** post-mechanics \(\varepsilon(\mathbf u)\) via **`strain_tensor_from_bar_network_displacement`** when `node_positions` is `[N,3]` — **Solver lanes — Fracture** + [track 12 memo](research/v0.4_track12_staggered_fracture_mechanics.md) §2.3; kinematic parity test `bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`. **Remaining:** large-\(N\) monolith / JFNK; **within-step** \(u\!\leftrightarrow\!d\) stagger — **Solver lanes — THMC** and `docs/research/v0.4_track13_monolithic_newton_thmc.md`.
| `solvers::statistical_mechanics`                                                        | research | `tests/verification/statmech_vinet_eos.rs`, `tests/verification/statmech_lj_bridge_contract.rs`, `tests/verification/statmech_lj_johnson_eos_reference.rs` | **Stable lane:** Vinet scalar EOS (`statistical-mechanics-vinet`). **Research:** `upscale_potentials` LJ→continuum **placeholder** (`statmech_lj_bridge_contract`). **Johnson (1993)** `f64` EOS lives in `lj_johnson_1993_reference`; **`upscale_potentials` still does not call it** — Burn tensor rows carry only \((\varepsilon,\sigma)\), while \(K^*\) needs **reduced** \((\rho^*,T^*)\). **Scalar bridge (always-on):** `physical_bulk_modulus_johnson1993` / `relative_placeholder_bulk_modulus_gap_vs_johnson1993` compose Johnson \(K^*\) with \((\varepsilon,\sigma)\) for physical \(K_T\) on the path a future extended tensor row would use; integration `physical_bulk_modulus_johnson1993_statmech_bridge_matches_lj_reference` + `placeholder_upscale_bulk_modulus_disagrees_with_johnson_reference_documented`. **Opt-in** feature `statistical-mechanics-johnson-reference`: `bulk_modulus_from_lj_state_johnson1993` re-export + `upscale_placeholder_bulk_modulus_documented_gap_vs_johnson_scalar_path` (f32 `upscale_potentials` vs scalar Johnson). **`upscale_potentials` remains partial** (placeholder \(K,\gamma_{\mathrm{gc}}\)). Cartridge `physics::clinker_eos` holds phase tables. See **Solver lanes — Statistical mechanics** and `docs/research/v0.4_track16_virial_lj_bridge.md`.


## Research memos (v0.4 follow-on)

Numbered files under `docs/research/` (`v0.4_track12_staggered_fracture_mechanics.md` … `v0.4_track16_virial_lj_bridge.md`). Each memo is **present** on disk for tracks **12–16**. Full-track acceptance remains **open** where solver lanes below say **partial**; shipped milestones are cited in the main table and memos.

- **12 — Fracture / AT2:** [memo](research/v0.4_track12_staggered_fracture_mechanics.md). Partial implementation in listed fracture tests; §7 lists proposed **`#[test]`** names for future Γ-type and coupling work.
- **13 — THMC:** [Newton / JFNK memo](research/v0.4_track13_monolithic_newton_thmc.md); [monolithic THMC + mechanics coupling plan](research/v0.4_track13_monolithic_thmc_mechanics_coupling_plan.md). Partial: backward-Euler **\((T,\alpha)\)** residual + damped Newton smoke on tiny chains; Phase 1–2 stacked-\(u\) bookkeeping + quasi-static \(R_u\) assembly tests above; **Phase 3** dense Newton on \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) (`thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes`); **Phase 4** shrink-increment hook in `evaluate_quasi_static_r_u` (optional `ru_shrinkage_water_cement_ratio`) + tests named in the THMC table row; **Phase 5** **`3e93a0a`:** **`ThmcMonolithicNewtonConfig`** on **`monolithic_thmc_newton`** wired in **`ThmcSolver::step` → `step_experimental`** (`thmc_step_monolithic_newton_*`, `thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes`).
- **14 — Electrochemistry:** [memo](research/v0.4_track14_implicit_newton_pnp.md). Partial: Picard path + MVP-chain implicit Newton dispatch (`solve_pnp_step_dispatch`); quasi-steady **`debye_screening_256_cells_*`** λ_D LS gates on default CI (**`8a2163a`**, no **`#[ignore]`**).
- **15 — Photonics:** [memo](research/v0.4_track15_dec_curl_curl_photonics.md). Partial: 1-D TE chain reductions in `photonics_fresnel.rs`.
- **16 — Statistical mechanics:** [memo](research/v0.4_track16_virial_lj_bridge.md). Partial: Johnson reference + documented placeholder gap vs `upscale_potentials`.


## Verification scope (remaining)

Single checklist of **open verification / ship items** (solver-by-solver); solver-lane sections below hold acceptance detail and commands without repeating this list verbatim.

1. **Topology / shell (Tracks B + L):** Run **`shell_topology_rib_pattern_full_v04`** at **40×40×4** (documented NaN / bar-network CG conditioning); commit Track L artefacts (`striatus_emergence.gif`, `striatus_shell_v0.4.stl`, `striatus_shell_v0.4.print_ready.json`, optional `.obj`) and satisfy Track B8 — regeneration command and paths under **Solver lanes — Topology / shell**. **Track L demo (cartridge):** `notebooks/_run_shell_demo.sh` from **`umst-concrete-cartridge/`** repo root (env `UMST_SHELL_DUMP_ITER` / `UMST_SHELL_DUMP_STRIDE` / `UMST_SHELL_ITERS`; optional `UMST_SHELL_*` stability knobs — **`../../umst-concrete-cartridge/docs/Solver-Status.md`** §Topology / shell + `optimize_shell_3d` rustdoc). **Quick vs full greyness (honest):** default CI **`shell_topology_rib_pattern_quick`** asserts **roof-slice** `mean(4ρ(1−ρ))` on **post–volume-projection** ρ **<** `4·vf·(1−vf)−0.008` — **not** the full-B6 **volume** mean **< 0.15** (that gate lives only on **`shell_topology_rib_pattern_full_v04`** with **200** outers; see **Track B6** table above and [2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)). **Quick vs demo roof (code):** quick uses **9×8×3**, **≤20** outers, **96 Pa**, **r = 0.55** (see **`shell_topology_rib_pattern.rs`** `#!` / **`run_rib_quick_metrics`**); **`optimize_shell_3d`** uses **50 Pa** and the **x** ramp only when **`UMST_SHELL_ROOF_RAMP=1`** (**`UMST_SHELL_ROOF_RAMP_F`**, default **0.2**), else **50 Pa** uniform. The **opt-in B6 harness** uses **`UMST_SHELL_ROOF_RAMP=0`** → uniform roof; **unset** or any value other than **`0`** / **`1`** turns the gentle **x** ramp **on** at **`UMST_SHELL_ROOF_RAMP_F`** (default **0.2**) so CI quick matches Striatus load asymmetry — **`optimize_shell_3d`** still requires literal **`UMST_SHELL_ROOF_RAMP=1`** for the same ramp. **Shell demo operator notes** (pitfalls: short `UMST_SHELL_ITERS`, self-weight, stale `iter_*.npy` / manifest, Trimesh deps): see **Solver lanes — Topology / shell** → *Shell demo — operator notes* below and the mirrored bullets in [`../../umst-concrete-cartridge/docs/Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md) (*Shell demo — operator notes* under Track L).
2. **Mechanics (plates):** Move Q1 hex SRI toward the Kirchhoff target in the completion brief versus today’s **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`** ratio band ([follow-up §R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate)).
3. **Fracture:** Γ-limit with driven **ψ⁺**; broader **(l₀,h)** schedules; within-step THMC **u↔d** stagger; embedding / `matrix_features` when no bar solve — memo §2.3 / §7.
4. **Acoustics (closed in-tree):** return-map **n=128** gate — **`plane_wave_return_map_n128_l2_within_two_percent`** on default CI; period **\(T=2\pi/\Omega\)** with lumped \(m=\rho\Delta x\) (see **Solver lanes — Acoustics**). Prior slip (~1.4 rel \(L^2\)) was a **mis-timed period** (FD \(\omega_{\mathrm{disp}}\) vs lumped \(\Omega\)), not substep starvation.
5. **Electrochemistry:** **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** and **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** now run on default **`pnp_debye_layer`** with **`electrochemistry-pnp`** (Poisson **`h^2`** interior RHS + **`mesh_spacing = h`** + tail LS window; **±11 %** / **±15 %** vs continuum **`λ_D`**). **`debye_implicit_dispatch_short_horizon_smoke`** + **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** remain complementary dispatch / residual smokes. **Shipped increment:** Fickian-linearised implicit Newton (`linearize_sg_fickian: true`) uses **\(O(N)\)** **three-Thomas** corrections (unit test parity vs dense Jacobian on the same linear system). **Full-SG chain lane:** band FD Jacobian + **dense expand + Gauss** Newton correction (see **Solver lanes — Electrochemistry**); **still open:** cheap pivot-stable linear solve at large **N**, **general-graph** implicit Newton; variable ε on non-chain relaxers; large-|zΔφ| f32 robustness ([follow-up §R2.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r23--pnp-λ_d-quasi-steady-gates-un-ignored) — narrative; λ_D LS gates are no longer deferred).
6. **Photonics:** Production 2D/3D DEC vector curl–curl; tensor permittivity; tighter Fresnel calibration ([follow-up §R3.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r32--dec-vector-curl-curl-on-2d3d-patches)).
7. **Rheology:** MAC-style staggered solve and/or consistent **open** \(x\) BCs so long-run developed flow matches plane Poiseuille — graph **Jacobi-PCG** on \(-\mathcal{L}\) is shipped for the Chorin pressure step (`rheology_flow.rs`). **`f02120d`:** hardened Jacobi-PCG pressure; **`chorin_steady_channel_64x16_vs_regularized_reference`** and **`chorin_developed_channel_centreline_vs_regularized_reference`** run on **`research-stack`** (**`rheology-bingham`**) **without `#[ignore]`** as **short-horizon** **65×17** smokes (finite / bracket semantics — **not** long-run steady **L²** vs Poiseuille). **`chorin_surrogate_poisson_amplification_regression_guard`** (historical name) remains a two-step amplification bracket on **65×17** ([follow-up §R2.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r22--rheology-chorin-64--16--fix-surrogate-poisson)).
8. **THMC:** Production-scale monolithic Newton–Krylov / JFNK on large graphs; optional within-step **u↔d** stagger; adaptive **`dt`** on stacked residual ([follow-up §R3.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r31--monolithic-thmc-newton-on-t-h-u-c)). *(**`3e93a0a`:** small-\(N\) dense Phase 5 path in **`ThmcSolver::step` → `step_experimental`** behind **`monolithic_thmc_newton: Some(ThmcMonolithicNewtonConfig { .. })`** — see THMC table row.)*
9. **Statistical mechanics:** Virial / coexistence route into **`upscale_potentials`** ([follow-up §R3.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r33--lj-virial-bridge-inside-upscale_potentials)).
10. **Mechanics (transient solids & contact):** Shipped mechanics is quasi-static **`VectorMechanicsSolver::solve_equilibrium`** on the bar-network 1-skeleton — **not** a CI-unified **M ü + C u̇ + Ku = f** stack on general 3D solid DOFs. The **`acoustics-newmark`** 1-D periodic bar Newmark lane (`tests/verification/acoustics_plane_wave.rs`) exercises a **scalar** bar-wave semi-discrete operator, **not** vector 3D solid transient elasticity on the mechanics graph. **Contact / collision / friction:** no contact constraints or inter-body Coulomb friction in the shipped stack — see [contact mechanics scope](research/contact_mechanics_scope.md).

Lower-priority stretch items from the follow-up brief (stacked THMC beyond split ship, DEC Maxwell breadth, LJ bridge wiring, within-step stagger) appear above where they touch default CI or ship artefacts.


## Solver lanes — THMC

<span id="solver-lane-thmc"></span>

| | **Shipped:** `ThmcImplicitTAlphaNewtonConfig` **or** small-DOF **`ThmcMonolithicNewtonConfig`** (**`3e93a0a`**, `step` → `step_experimental`) | **Still open (memo 13 / §R3.1):** large-\(N\) monolith, sparse or matrix-free Jacobian, adaptive **`dt`**, within-step **u↔d** |
| --- | --- | --- |
| **Implicit unknowns** | **Split:** \(T^{n+1},\alpha^{n+1}\) only (backward Euler + dense FD Jacobian on \(\le 64\) DOFs, batch 1). **Monolith (small graph):** stacked \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) when **`monolithic_thmc_newton: Some(_)`** and geometry/drying guards pass — same dense cap. | At production \(N\): \(T,h,\alpha,u,\ldots\) jointly in one \(R(U^{n+1})=0\) with consistent blocks / Krylov–JFNK (beyond dense \(\le 64\) harness) |
| **Fracture strain (SI bar)** | Post-\(u\) symmetric \(\varepsilon\) via **`strain_tensor_from_bar_network_displacement`** when `node_positions` is `[N,3]` and the BC mask allows bar equilibrium — same kinematics as **`strain_tensor_for_fracture_after_mechanics`** after one solve; CI: `bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics` in `thmc_drying_shrinkage.rs` | **Within-step** \(u\!\leftrightarrow\!d\) stagger (mechanics still uses `damage` snapshot at step entry; single end-of-step `update_damage`); non-embedding / `matrix_features` fallback |
| **CI boundary** | Assembler: `thmc_t_alpha_newton_residual_preserves_hydro_mechanics_fields` (trial `h`,`u` untouched by Newton). Solver: `thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split` (transport does not branch on implicit \(T,\alpha\)). Monolith: `thmc_step_monolithic_newton_*`, `thmc_monolithic_newton_residual_tol_early_exit_truncates_norm_trail`, `thmc_monolithic_newton_relative_to_initial_early_exit_truncates_norm_trail`, `thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes`. | At scale: adaptive `dt`; matrix-free / AD-safe Jacobian follow-ups beyond the small-\(N\) dense Newton harness |

**Shipped split vs monolithic (summary):** `ThmcImplicitTAlphaNewtonConfig` (when `Some`) runs **damped Newton** only on the backward-Euler **\((T,\alpha)\)** block via `ThmcImplicitEulerThermalHydrationResidual` (dense FD Jacobian, \(\le 64\) stacked DOFs, batch 1). **`ThmcMonolithicNewtonConfig`** (when `Some`) replaces **each outer-pass** operator-split sequence \((T,\alpha)\to h\to u\) with **`damped_newton_iterations_with_quasi_static_r_u`** on \((T,h,\alpha,\mathbf u)\) when SI **`[N,3]`** embedding holds, \(\mathrm{DOF}_\mathrm{stack}\le 64\), **`drying_last_node_evaporation_k == 0`**, and `implicit_t_alpha_newton` is **`None`** (mutually exclusive). **`ThmcSolver::step` / `step_experimental` substep order when monolithic is off (each outer pass, then once per step):** \((T,\alpha)\) → humidity \(h\) (Laplacian + drying) → bar equilibrium \(u\) if SI embedding allows → repeat `max_newton` times for split residuals; **`update_damage`** runs **once** after the outer loop with post-\(u\) strain on the SI path. Humidity does **not** use the implicit \((T,\alpha)\) branch (CI: `thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split`). The table rows above are the verified boundary vs track‑13 scope. **True monolithic** track‑13 work at **production scale** remains **open:** Krylov–JFNK, adaptive `dt`, and consistent sparse Jacobians for large \(N\). **Partial milestones already in CI:** `ThmcHydrationKinetics` calibration surface; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; `thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`; damped Newton lowers \(\|R\|_2\) on the \((T,\alpha)\) block (`thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm`, `thmc_implicit_euler_t_alpha_multi_newton_monotone_residual_norm_decrease`); linearized thermal Newton vs analytic decay in the same file; **Phase 1 toward monolith:** placeholder \(R_u\) tail + `evaluate_quasi_static_r_u` (`thmc_implicit_euler_t_h_alpha_u_placeholder_r_u_and_flat_layout_two_nodes`, `thmc_r_u_zero_at_solved_equilibrium_two_node_chain`); **Phase 3:** `thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes` — see [`docs/research/v0.4_track13_monolithic_thmc_mechanics_coupling_plan.md`](research/v0.4_track13_monolithic_thmc_mechanics_coupling_plan.md). **Phase 3 (verification harness):** dense damped Newton on \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) — `thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes`. **Phase 5 (integration, `3e93a0a`):** same stacked Newton **inside** [`ThmcSolver::step`](../src/physics/solvers/thmc.rs) → **`step_experimental`** behind **`monolithic_thmc_newton`** — `thmc_step_monolithic_newton_matches_standalone_dense_newton_two_nodes`, mutual-exclusion / drying guards, **`thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes`** (coupled BE \(\|R\|_2\) vs operator split on the 2-node harness), **`ThmcSolver::step_monolithic_implicit`**. **Still open after Phase 5 (at scale / beyond small-DOF harness):** linearized thermal block vs analytic mode under the **full** implicit map once assembled at scale; research memo 13 routes detail to this section + `docs/research/v0.4_track13_monolithic_newton_thmc.md` + coupling plan link. **Shipped (small graph):** monolithic path forwards **`ThmcMonolithicNewtonConfig::tol`** / **`relative_tol`** (stacked \(\|R\|_2\) absolute / relative gates; see [`thmc.rs`](../src/physics/solvers/thmc.rs)) into `damped_newton_iterations_with_quasi_static_r_u` as `stacked_residual_l2_tolerance` / `stacked_residual_relative_to_initial` (host \(\|R\|_2\) reads); CI covers absolute + relative early-exit assembler tests and solver parity tail on `thmc_step_monolithic_newton_matches_standalone_dense_newton_two_nodes`. **Dense-cap guard:** when field-major stacked unknowns exceed **64**, `ThmcSolver::step` returns `Err` starting with `ThmcSolver::step: monolithic_thmc_newton stacked DOFs > 64` (same cap as implicit \((T,\alpha)\) Newton).


## Solver lanes — Statistical mechanics

<span id="solver-lane-statistical-mechanics"></span>

**Closed in this PR (partial):** `statmech_lj_bridge_contract` (`lj_bridge_scaling_non_regression`) on \(K \propto \varepsilon/\sigma^3\), \(\gamma \propto \varepsilon/\sigma^2\); **Johnson–Zollweg–Gubbins (1993)** LJ EOS reference grid in `statmech_lj_johnson_eos_reference` / `lj_johnson_1993_reference` (including explicit check that `upscale_potentials` **does not** match Johnson-derived bulk modulus — placeholder honesty); **scalar Johnson bridge helpers** `physical_bulk_modulus_johnson1993` / `relative_placeholder_bulk_modulus_gap_vs_johnson1993` on `statistical_mechanics` (integration `physical_bulk_modulus_johnson1993_statmech_bridge_matches_lj_reference`); optional feature **`statistical-mechanics-johnson-reference`** exposes **`bulk_modulus_from_lj_state_johnson1993`** for tensor-vs-scalar comparisons (see unit test in-module).

**Path sketch — Johnson / virial → `upscale_potentials` (target wiring, not shipped in the Burn tensor):**

| Stage | What is evaluated | State / parameters |
| --- | --- | --- |
| **Virial / MD** | \(P = \rho k_B T - \frac{1}{3V}\langle\sum_{i<j}\mathbf r_{ij}\cdot\mathbf F_{ij}\rangle\); then \(K_T=\rho(\partial P/\partial\rho)_T\) at matched protocol (cutoff, tail) | \(\rho, T\) plus potential parameters |
| **Johnson (1993) reference** | MBWR-style reduced EOS: \(P^*(\rho^*,T^*)\), \(K^*=\rho^*(\partial P^*/\partial\rho^*)_{T^*}\) in LJ reduced units (\(T^*=k_BT/\varepsilon\), \(\rho^*=\rho\sigma^3\)) | \((\rho^*,T^*)\) only in the analytic surface |
| **Physical \(K_T\)** | \(K_T = (\varepsilon/\sigma^3)\,K^*\) — [`lj_johnson_1993_reference::bulk_modulus_from_reduced`](../../src/physics/solvers/lj_johnson_1993_reference.rs) | joins \((\rho^*,T^*)\) with \((\varepsilon,\sigma)\) |
| **`upscale_potentials` (today)** | Placeholder \(K \propto \varepsilon/\sigma^3\), \(\gamma_{\mathrm{gc}}\propto \varepsilon/\sigma^2\) on **`[B,2]`** rows | **columns 0–1:** \((\varepsilon,\sigma)\) only — **no** \(\rho^*,T^*\) channel, so no Johnson/virial evaluation inside the tensor |

**Tensor channel design (future API):** extend the batch row so thermodynamic state is explicit, e.g. **`[B, 4]`** with columns \((\varepsilon,\sigma,\rho^*,T^*)\) in consistent reduced or SI conventions, **or** keep **`[B,2]`** LJ knobs and add a parallel **`[B,2]`** state tensor \((\rho^*,T^*)\) (or density \(+\) temperature in cartridge units) passed to a new `StatisticalBridge` method. The differentiable Burn path would broadcast the state pair into the same virial/EOS or tabulated+smooth-interp map as the `f64` reference lane.

**Feature gate sketch:** default builds: **`upscale_potentials`** only (research placeholder). **`statistical-mechanics-johnson-reference`:** adds scalar **`bulk_modulus_from_lj_state_johnson1993`** re-export for **`f64`** parity checks against Johnson \(K^*\); does **not** change the **`[B,2]`** tensor contract. A full Johnson/virial **inside** the Burn bridge requires the extended tensor (or config-carried default \((\rho^*,T^*)\), documented and test-gated) so physics is not hidden.

**Still open — physical bridge:** virial / coexistence / Kirkwood–Buff route from \((\varepsilon,\sigma)\) to \((K,\gamma_{\mathrm{gc}})\) with uncertainty bands.

**Next PR acceptance criteria:** (1) reference \(K\) from MD or published EOS at one state point; (2) `upscale_potentials` error vs reference &lt; stated tolerance; (3) document temperature / cutoff dependence.


## Solver lanes — Fracture

<span id="solver-lane-fracture"></span>

**Verified under `fracture-at2` (cumulative, see table row):** multi–length-scale **admissibility** smoke (`at2_length_scale_sweep_non_regression`); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not sharp-interface Γ-limit); **`at2_gamma_convergence_three_length_scales`** — three \((l_0,h)\) pairs with \(h/l_0=\tfrac14\), \(\psi^+\!=0\), exponential damage seed; discrete \(D_h\) within **&lt; 2%** of **`Gc`** and non-worsening under refinement (`tests/verification/fracture_gamma_convergence.rs`).

**Latest (harness hygiene only):** **`at2_discrete_surface_functional_toy_chain_matches_hand_total`** exercises **`discrete_at2_bar_surface_energy_1d`**, the same closed-form \(D_h\) sum used above (3-node hand total — **does not** extend Γ claims or \(\psi^+\) coverage). **Research:** explicit acceptance + proposed **`#[test]`** names for driven \(\psi^+\), multi-ratio \((l_0,h)\), staggered \(D_h\) vs \(G_c\), and THMC within-step stagger — [track 12 memo §7](research/v0.4_track12_staggered_fracture_mechanics.md#7-research-backlog-gamma-limit-psi-plus-multi-l0h-schedules).

**Still open:** Γ-limit / \(D_h\) certificates with **nonzero tensile driving** \(\psi^+\) and mechanics-coupled refinement; broader \((l_0,h)\) schedules than the fixed \(h/l_0\) triple; **within-step** THMC stagger so mechanics and fracture can alternate on updated \(d\) inside one `step` (today `damage_m` for transport/mechanics is fixed at step entry; fracture runs once after outer passes); dissipation vs \(G_c\) on refined meshes under staggered \(u\!\leftrightarrow\!d\) beyond compliance monotonicity.

**Next PR acceptance criteria:** (1) extend Γ-type or dissipation harness with \(\psi^+\!\neq 0\) and/or staggered bar loading; (2) **THMC SI path:** fresh \(\varepsilon(\mathbf u)\) after bar equilibrium **met** (above); optional **within-step** \(u\!\leftrightarrow\!d\) iterations or documented single-pass contract; (3) energy monotone band on benchmark chain.


## Solver lanes — Photonics

<span id="solver-lane-photonics"></span>

**Closed in this PR (partial):** uniform x-chain **TE \(E_y\)** curl–curl reduction equals scalar Helmholtz (`curl_curl_y_mode_matches_scalar_helmholtz`); same operator identity under **piecewise** node \(\varepsilon_r\) (`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`); Burn DEC primal matvec matches hand TE stencil (`dec_te_primal_tensor_matches_chain_stencil`); **default builds** (`photonics` **off**): `photonics_curl_curl_stub_default_build` locks [`PhotonicsSolver::solve_maxwell_curl_curl`] as a documented **no-op** (identity on representative chain tensors — not a physics solve).

**Still open:** DEC-assembled vector curl–curl on **2D/3D** patches (\(d_1\) / face topology); tensor permittivity; tighter Fresnel field match vs a **discrete** semi-infinite reference (current CI test uses bridged continuum target and a relaxed LS band).

**Closed partial — discrete DEC triangle (not photonics solve path):** [`tests/dec_identities.rs`](../tests/dec_identities.rs) ships **`dec_curl_d1_annihilates_gradient_on_triangle_faces_b2_burn`** (\(d_1(d_0\omega)=0\) through synthetic **`edges_b1`/`faces_b2`** COO tensors and Burn gather/scatter in [`dec_primal`](../src/physics/dec_primal.rs)) and **`dec_primal_d1_adjoint_identity_single_triangle_burn`** (unweighted \(d_1\) / \(d_1^\top\) Frobenius adjoint on the same face **COO**). **`dec_curl_d1_annihilates_gradient_on_triangle`** is the dense **`ring_b1`** matmul companion on the **same** CCW 3-cycle (hand-coded face sum); it is not the only annihilation check. **Incremental toward 2D patch curl–curl:** **`dec_curl_d1_annihilates_gradient_quad_split_two_faces_burn`** and **`dec_primal_d1_adjoint_identity_quad_split_two_faces_burn`** — a **4-node / 5-edge** quad split into two CCW triangles sharing diagonal **0–2**, so one global edge carries **two signed `faces_b2` columns** (curl annihilation + adjoint scatter on the shared edge). None of these call [`PhotonicsSolver::solve_maxwell_curl_curl`](../src/physics/solvers/photonics.rs) or lift incidence from a full mesh assembly.

**Next PR acceptance criteria:** (1) tighten `two_half_spaces_fresnel_te_no_pml_matches_analytic` LS margin and/or add discrete `r_disc`-only comparison; (2) extend DEC curl / adjoint coverage to **metric-weighted** operators and/or **cube** patches with **`faces_b2` (and higher)** populated from **assembled graph / manifold state**, beyond the hand-authored single-triangle **COO** used above — toward wiring full **2D/3D** vector curl–curl in photonics.


## Solver lanes — Acoustics

<span id="solver-lane-acoustics"></span>

**Closed in this PR (partial):** implicit Newmark on a 1-D periodic bar — `newmark_acceleration_matches_dense_reference_n8` / `_n128` vs dense reference (inner linear solve uses **f64** Cholesky; f32 factorization diverges at large `n`); **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`**, **`plane_wave_return_map_n128_l2_within_two_percent`** (same CFL-scaled substep recipe; **return-map period** \(T=2\pi/\Omega\) with lumped mass \(m=\rho\Delta x\) — not FD dispersion \(\omega_{\mathrm{disp}}=(2c/\Delta x)|\sin(k\Delta x/2)|\) alone); `plane_wave_h_refinement_second_order_band` (analytic snapshot uses \(\omega_{\mathrm{disp}}\) for spatial \(h^2\) band vs continuum target); `undamped_energy_drift_under_half_percent_over_1000_steps` at **n=128** (energy proxy, not the sole return-map certificate).

**Still open:** graph-assembled acoustic stiffness beyond nodal `AcousticWaveSolver` contraction; optional DEC coupling milestones beyond this scalar periodic bar lane.

**Root-cause note (historical §R2.4 slip ~1.4):** the return-map harness used \(T=2\pi/\omega_{\mathrm{disp}}\) while the assembled evolution is \(m\ddot u+Ku=0\) with \(m=\rho\Delta x\). The correct modal frequency satisfies \(\Omega^2=\lambda_K/m=\omega_{\mathrm{disp}}^2/\Delta x\) on a uniform periodic chain, i.e. \(\Omega=\omega_{\mathrm{disp}}/\sqrt{\Delta x}\). Mis-measured \(T\) integrated across \(\mathcal O(\sqrt{n})\) too many temporal cycles at large \(n\), producing order-one relative \(L^2\) vs `u₀` — **not** fixed by substep refinement alone. Tests in `acoustics_plane_wave.rs` (`semi_discrete_omega`, `dispersion_omega_fd`) encode the split; rustdoc on `AcousticNewmarkBar1dPeriodic` summarizes.

**Warning — what CI does *not* certify:** `undamped_energy_drift_under_half_percent_over_1000_steps` (n=128) bounds the **discrete mechanical-energy proxy** over many substeps; use it alongside **`plane_wave_return_map_*`** gates for elastic wave fidelity claims.

**Next PR acceptance criteria:** (1) optional graph-based acoustic assembly exercising `AcousticWaveSolver` + DEC hooks beyond this 1-D periodic benchmark; (2) tighter Fresnel-style coupling checks if acoustics connects to photonics/mechanics lanes.


## Solver lanes — Electrochemistry

<span id="solver-lane-electrochemistry"></span>

**Closed in this PR (partial):** Poisson–NP **explicit Picard** coupling in `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep); MVP path-chain Thomas Poisson with **harmonic edge \(\varepsilon_{i+1/2}\)** and net charge RHS (**`\nabla\cdot(\varepsilon\nabla\phi)=-(c^+-c^-)F`**, consistent uniform-\(\varepsilon\) reduction); SG flux **`mesh_spacing`** scaling (**`J\propto D/h`**; Poisson stencil is **unit-graph** in index space — not rescaled by `mesh_spacing`; see `electrochemistry` rustdoc); **tensor SG Bernoulli** evaluated elementwise via **`f64`** exponentials for large \(|z\Delta\phi|\); **MVP-chain implicit backward Euler** + damped Newton (`try_solve_pnp_backward_euler_newton_chain`, host `f64`) with residual regression tests in `pnp_debye_layer.rs`. **Wiring note:** implicit Newton is **not** merged into `solve_pnp_step_experimental`; the default `solve_pnp_step` path remains explicit Picard-only. Research users can opt in via **`pnp_implicit_newton_chain`** + **`solve_pnp_step_dispatch`**. **λ_D admissibility harness** (`debye_screening_admissibility_check`) sets **`mesh_spacing = h = L/(N-1)`** so SG matches the physical fit abscissa, uses **`debye_implicit_newton_linearized_sg_for_lambda_d_gate()`** (`linearize_sg_fickian: true`) for analytic Jacobian / tractable **`--release`** runs on **`N=256`**, and drives **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** / **`…_phi_100mv…`** (interior LS **`λ_eff`** vs continuum **`λ_D`**, **±11 %** / **±15 %**). **CI-fast dispatch smokes:** `debye_implicit_dispatch_short_horizon_smoke` … **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** … keep default **`mesh_spacing = 1`** on the residual trajectory — orthogonal to the **`mesh_spacing = h`** λ_D LS gates.

**Still open:** coupled implicit Newton on **general graphs**; variable \(\varepsilon\) on **non-chain** Jacobi Poisson relaxers; **full SG** (`linearize_sg_fickian: false`) at **N=256** on **non-chain** topologies without **O(dim³)** dense Newton — the shipped **contiguous chain** path assembles a **node-major band** FD Jacobian and solves each Newton correction with **pivot-safe band LU** (**O(N·bw²)** with fixed `bw`); **dense expand + Gauss** remains for **unit-test parity** only. Matrix-free Krylov / graph assembly at production **N** remains open. **Fickian-linearised** path uses **three Thomas solves / Newton iter** instead of dense \((3N)^2\) factorisation.

**Next PR acceptance criteria:** (1) residual \(\|R_\Phi\|+\|R_c\|\) decreases under damped Newton on a **non-chain** benchmark or matrix-free JVP path; (2) mass conservation band on closed graph; (3) double-precision option for large \(|z\Delta\phi|\); (4) pivot-safe **band LU** or Krylov for **full SG** if **`dim³`** dense expand is unacceptable at **`N=256`**, then tighten or extend the **`debye_screening_256_cells_*`** bands under **nonlinear** SG if the linearised gates no longer bound the shipped physics.


## Solver lanes — Rheology

<span id="solver-lane-rheology"></span>

**Closed in this PR (partial):** **64×16** channel graph **edge-count** scaffold (`channel_lattice_64x16_quadrilateral_edge_count`, **1968** undirected edges on the quad stencil used by `rheology_poiseuille` harnesses). Live analytics / regularized 1-D references / Chorin short smoke / \(\lambda\)–thixotropy checks run under **`--features rheology-bingham`** (see table row). **`f02120d`:** **Jacobi-preconditioned CG** on \(-\mathcal{L}\) for the Chorin pressure increment hardened (`rheology_flow.rs`: [`TopologicalLaplacian::scalar_laplacian`](../src/physics/laplacian.rs) + [`scalar_laplacian_neg_opposite_diag`](../src/physics/laplacian.rs) diagonal, mean-free RHS, relative residual exit); **`chorin_developed_channel_centreline_vs_regularized_reference`** / **`chorin_steady_channel_64x16_vs_regularized_reference`** promoted to **`research-stack`** CI (**no `#[ignore]`**) — **65×17** short-horizon smokes (80 / 100 substeps; finite ‖u‖/‖p‖, bracket vs developed regularized reference — **not** long-run steady **L²**); **`chorin_surrogate_poisson_amplification_regression_guard`** (historical name) + **`chorin_channel_65x17_thirty_substeps_remain_finite`**.

**Still open:** (1) **MAC / staggered** pressure or cell-centred divergence so the discrete split matches developed-channel references without graph-only artefacts. (2) **Open** \(x\) **inlet/outlet** BCs (today periodic \(x\) is only a test harness approximation). (3) **Long-run** steady \(L^2\) / plug-width vs the regularized 1-D profile on **65×17** (multi-thousand-step acceptance) — not wired as default CI yet. **Design:** [research/rheology_pressure_poisson_roadmap.md](research/rheology_pressure_poisson_roadmap.md).

**Warning — what CI does *not* certify:** default CI **short** channel smokes do **not** replace a full steady **\(L^2\)** certificate against plane Poiseuille; long-run acceptance remains a research goal until MAC + open BCs land. `chorin_single_step_finite_smoke` and **`channel_lattice_64x16_quadrilateral_edge_count`** alone do not validate developed physics.

**Next PR acceptance criteria:** (1) centreline \(L^2\) error vs analytic &lt; 15% at stated `dt` / step count on **64×16**; (2) pressure correction Poisson RHS aligned with discrete divergence of \(u^\*\); (3) optional env-gated long benchmark (`UMST_RHEOLOGY_POISEUILLE_BUDGET_MS`).


## Solver lanes — Topology / shell (Tracks B + L)

<span id="solver-lane-topology-shell"></span>

**Related pointers:** Striatus ship checks are summarized in **Verification scope (remaining)** above and in [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md); this section expands **Track B / Track L** artefacts and commands (**B6 / B8**).

**Source brief:** [`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md) — **Track B** (topology shell / Striatus), **Track L** (GIF + committed artefacts).

### v0.3 vs v0.4 honesty

- The brief defines **target** public filenames and thresholds for **v0.4**. A checkout may still ship **v0.3-only** names (e.g. `striatus_shell_v0.3.*`) or lack rib-pattern completion — **do not** claim “v0.4 Striatus shell complete” until the **exact** filenames below exist, meet **Track L** size/structural gates, and **Track B8** checks are satisfied (tests + `print_ready.json`).
- **CI vs brief:** default cartridge CI runs **`shell_topology_rib_pattern_quick`** (`cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental`). **Quick harness:** **9×8×3**, **≤20** Adam outers (`UMST_RIB_QUICK` default), roof ramp **r=0.55** at **96 Pa**, Heaviside **β=28**, **`DensityNet`** width **56**, Adam **0.0075**; **greyness** = roof-slice `mean(4ρ(1−ρ))` on **post–volume-projection** ρ **<** `4·vf·(1−vf)−0.008` — **distinct from** full B6 **volume** mean **< 0.15** on **`shell_topology_rib_pattern_full_v04`**. **`optimize_shell_3d`** uses **50 Pa** and the **x** ramp only when **`UMST_SHELL_ROOF_RAMP=1`**, else **50 Pa** uniform. **Full B6** (`shell_topology_rib_pattern_full_v04`, **`#[ignore]`**): **`UMST_SHELL_RIB_PATTERN=1`**, then `cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental shell_topology_rib_pattern_full_v04 --release -- --ignored` — put **`--release` before `--`**. **40×40×4**, **200** Adam outers (optional **`UMST_SHELL_RIB_FULL_ITERS`** in **1…200** for smoke only; smoke does **not** satisfy B6 greyness / compliance gates). Multi-minute CPU per outer in `--release` is typical; budget **hours** for **200** outers. The brief’s proof stays env-gated off default CI; see **Track B6** table at the top of this file and cartridge **[`../../umst-concrete-cartridge/docs/Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md)**. **Track L doc/manifest alignment** (cartridge **`1c5ab2f`**): richer **`manifest.json`**, default **`UMST_SHELL_DUMP_STRIDE`** when dumps are on, and rustdoc overnight / symmetry notes — **`optimize_shell_3d`** rustdoc + operator bullets below.
- **B6 bar equilibrium:** `packed_bar_network_equilibrium` caps PCG at **`min(max_cg_iterations, 3N)`** and **early-exits** when \(\|P(f-Ku)\|_2 \le \max(\texttt{pcg\_tolerance},\texttt{cg\_tolerance})\,\|Pf\|_2\). The full B6 harness defaults **`max_cg_iterations = 2000`** and **`E_{\min} = 10^{-3} E_0`** (**`UMST_SHELL_MAX_CG`**, **`UMST_SHELL_E_MIN_REL`**). **`UMST_SHELL_HELM`:** default **off** on the Burn tape (same as `optimize_shell_3d`); **only** literal **`UMST_SHELL_HELM=1`** enables graph Helmholtz in-loop (an empty **`UMST_SHELL_HELM=`** assignment must **not** enable — older **`!= "0"`** parsing turned it on; scatter backward can still fail at large Striatus `N` on Burn 0.13 when enabled). **Parsing:** harnesses must **not** use a naive **`v != "0"`** toggle for Helmholtz; cartridge **`shell_topology_rib_pattern`** / **`optimize_shell_3d`** match on **`"1"`** only. Optional **`UMST_SHELL_RIB_FULL_ITERS`** (see cartridge test `//!`) shortens the ignored harness for smoke — when **`UMST_SHELL_RIB_FULL_ITERS` < 200**, the cartridge test **skips** the brief **greyness / planar-variance / compliance-ratio** gates and only checks finite metrics + a loose VF band; the full **200**-outer schedule is required for those acceptance asserts. **Ring 1 harness:** `shell_topology_rib_pattern_full_v04` reads **`UMST_SHELL_SELF_WEIGHT`** (default **off** — traction + roof; **`1`** adds gravity), **`UMST_SHELL_VOL_LOOP`**, **`UMST_SHELL_PCG`**, and **`UMST_SHELL_ROOF_RAMP`** / **`UMST_SHELL_ROOF_RAMP_F`**: **`UMST_SHELL_ROOF_RAMP=0`** forces **uniform** roof pressure (like **`optimize_shell_3d`** with ramp unset); **unset** or any value other than **`0`** / **`1`** turns the gentle **x** ramp **on** at **`UMST_SHELL_ROOF_RAMP_F`** (default **0.2**) so the Striatus-scale load matches the CI quick harness — **`optimize_shell_3d`** itself still requires literal **`UMST_SHELL_ROOF_RAMP=1`** for the same ramp. **Non-finite iter-1 raw compliance** **panics** fast; gate failures print **vf / greyness / xy_var / compliance**. Details in [`../../umst-concrete-cartridge/crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs`](../../umst-concrete-cartridge/crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs) module docs and **`#!`**.

### NaN / conditioning — `optimize_shell_3d` (40²×4)

**Where NaNs enter:** [`AdjointCompliance::forward_and_loss`](../src/physics/adjoint.rs) builds nodal stiffness from `SimpElasticMaterial`, runs inner [`VectorMechanicsSolver::packed_bar_network_equilibrium`](../src/physics/mechanics.rs) (projected **f32** PCG, capped iteration count, optional Jacobi preconditioner), then `masked_dot` → raw compliance `c_raw`. The discrete-adjoint surrogate re-lifts inner tensors with `Tensor::from_inner`; non-finite **`u`** or edge elongations **`δ_e`** poison **`c_raw`** and the surrogate. **Self-weight** body force scales with filtered density on large slabs and can drive bar PCG into **non-finite** `u` in **`f32`** (cartridge `shell_demo_smoke` uses traction-only for that reason). **Helmholtz** Richardson on the **Burn Autodiff** tape can amplify round-off on large 3-D graphs; [`HelmholtzFilter::apply`](../src/physics/topology_filter.rs) honours the constructor iteration count (it no longer forces a **400**-step minimum over a **240**-step caller). **In-loop [`VolumeProjection`]** bisection on AD can trip Burn scatter backward limits on some slab sizes; set **`UMST_SHELL_VOL_LOOP=0`** to skip it during optimisation (mean ρ can drift toward **0.5** and worsen bar conditioning on **40²×4** unless self-weight / Helmholtz / other knobs are aligned). Default in the cartridge example remains **on** (`1` or unset) so mean VF tracks **`UMST_SHELL_VF`**.

**Reproduce (cartridge example — finite with default env flags):**

```bash
cd umst-concrete-cartridge
cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render'
```

**Stress ill-conditioning (historical NaN at iter 1 — low PCG budget + tiny `E_min`):**

```bash
cd umst-concrete-cartridge
UMST_SHELL_MAX_CG=200 UMST_SHELL_E_MIN_REL=5e-9 \
  cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render'
```

**Full B6 ignored harness:**

```bash
cd umst-concrete-cartridge
UMST_SHELL_RIB_PATTERN=1 cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental shell_topology_rib_pattern_full_v04 --release -- --ignored
```

Uniform roof like **`optimize_shell_3d`** with ramp unset: prefix **`UMST_SHELL_ROOF_RAMP=0`** on the same command.

**Mitigations:** raise **`UMST_SHELL_MAX_CG`**, keep **`UMST_SHELL_E_MIN_REL≈1e-3`** (Track B6 **`10^{-3}E_0`**), keep **`UMST_SHELL_PCG=1`**, **`UMST_SHELL_SELF_WEIGHT=0`** for traction-only while **`f32`** bar PCG is hardened, **`UMST_SHELL_HELM=0`** to A/B the graph filter on AD, **`UMST_SHELL_VOL_LOOP=0`** only when debugging backward (expect mean ρ near **0.5** without other constraints); future **f64** inner equilibrium remains deferred.

### 2026-05-11 greyness vs roof-ramp defaults (honest)

- **What was measured:** a full **`shell_topology_rib_pattern_full_v04`** **`--release`** run (**200** outers, **40×40×4**, seed **42**) reported **greyness** `mean(4ρ(1−ρ)) ≈ 0.51` vs the B6 gate **&lt; 0.15** (**~7655 s** wall; **finite** — not an early NaN / `c_raw` blow-up). That log predates the current **B6 harness** roof convention below.
- **Two surfaces today:** **`optimize_shell_3d`** / **`_run_shell_demo.sh`** use **uniform** roof traction unless you set **`UMST_SHELL_ROOF_RAMP=1`**. The **opt-in B6 test harness** uses **`UMST_SHELL_ROOF_RAMP=0`** → uniform; **unset** or any value other than **`0`** / **`1`** → gentle **x** ramp at **`UMST_SHELL_ROOF_RAMP_F`** (default **0.2**) so CI quick matches Striatus load asymmetry — **do not** assume “unset” means the same thing across example vs test.
- **What we are not claiming:** no in-repo **200**-outer re-proof yet that greyness clears under the post-harness ramp story; bounded smokes (e.g. **15** outers) still showed **~0.51** greyness on the roof Heaviside slice while **`c1`** moved with ramp vs uniform — load asymmetry helps compliance, not the grey gate by itself in those short runs.
- **Operator takeaway:** pick roof semantics explicitly (`0` / `1` / “harness default” for B6) before comparing logs to **`export_print_ready.py`** / Track L artefacts.

### Exact artefact paths (relative to `umst-concrete-cartridge/` repo root)

| Role | Path |
| --- | --- |
| Emergence animation | `notebooks/_artifacts/striatus_emergence.gif` |
| Mesh export (v0.4 name) | `notebooks/_artifacts/striatus_shell_v0.4.stl` |
| Print-ready JSON (B7 / B8 / L) | `notebooks/_artifacts/striatus_shell_v0.4.print_ready.json` |
| Optional mesh | `notebooks/_artifacts/striatus_shell_v0.4.obj` |

**Anti-goal (brief):** do **not** commit `crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy` — only final **`striatus_emergence.gif`**, **`striatus_shell_v0.4.stl`**, and **`striatus_shell_v0.4.print_ready.json`** are intended as committed artefacts (plus optional `.obj`).

### Track L — regeneration command (verbatim from brief)

Run from the **workspace root** that contains `umst-concrete-cartridge/` (the brief’s first line `cd`s into it):

```bash
cd umst-concrete-cartridge
rm -rf crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy
rm -rf notebooks/_artifacts/frames notebooks/_artifacts/striatus_emergence.gif
UMST_SHELL_DUMP_ITER=1 UMST_SHELL_DUMP_STRIDE=10 UMST_SHELL_ITERS=200 \
    bash notebooks/_run_shell_demo.sh
```

**Rust-only overnight (GIF/STL off, from `optimize_shell_3d` rustdoc):** from **`umst-concrete-cartridge/`**, after removing stale **`iter_*.npy`**: `UMST_SHELL_NX=40 UMST_SHELL_NY=40 UMST_SHELL_NZ=4 UMST_SHELL_ITERS=200 UMST_SHELL_DUMP_ITER=0 cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render' 2>&1 | tee shell_opt_40cube4_i200.log` — then run **`bash notebooks/_run_shell_demo.sh`** when you need GIF/STL/JSON, or chain **`render_shell_gif.py`** → **`export_print_ready.py`** on an existing **`final.npy`**.

**Shell demo — operator notes (`_run_shell_demo.sh` + Python)** — mirror of [cartridge `Solver-Status.md` §Track L](../../umst-concrete-cartridge/docs/Solver-Status.md) (*Shell demo — operator notes*):

- **`UMST_SHELL_ITERS=5`** (or any smaller value) only shortens the Rust loop; it does **not** fix **`loss=NaN`** at iter 1 when the forward adjoint path is unstable with **self-weight** on the **40×40×4** slab.
- **`notebooks/_run_shell_demo.sh`** exports **`UMST_SHELL_SELF_WEIGHT=0`** by default (traction + roof pressure) so **`f32`** PCG + adjoint usually stays finite; override in the environment only if you accept self-weight risk.
- **`UMST_SHELL_DUMP_STRIDE`:** with **`UMST_SHELL_DUMP_ITER=1`**, if **`UMST_SHELL_DUMP_STRIDE`** is **unset**, **`optimize_shell_3d`** defaults it to **10** (first / every **N**th / last outer), matching **`notebooks/_run_shell_demo.sh`** — avoids writing one full field per outer on long runs.
- **`manifest.json`** (beside **`final.npy`**): written by **`optimize_shell_3d`** with grid / spacing, fixed **`burn_seed`: 42**, **`iters`**, **`symmetry_xy`**, **`sym_period`**, **`roof_x_ramp`**, **`self_weight`**, **`vol_in_loop`**, **`dump_iter`**, **`dump_stride`** — use it with **`final.npy`** for ρ-span / export forensics (**`1c5ab2f`** richer manifest).
- Remove stale **`iter_*.npy`** when changing grid or dump settings — otherwise **`render_shell_gif.py`** may take the iter branch and **`ValueError`** on reshape vs **`manifest.json`** (`find …/shell -name 'iter_*.npy' -delete`).
- If **`manifest.json`** and **`final.npy`** disagree (stale manifest), **`render_shell_gif.py`** raises **`final.npy size … != grid …`**; re-run **`optimize_shell_3d`** once to align.
- **`export_print_ready.py`** needs Trimesh graph engines: **`uv pip install --python .venv/bin/python networkx scipy`** (otherwise **`ImportError: no graph engines available!`**).
- **`optimize_shell_3d`** (cartridge example): default **PCG / projected PCG** residual tolerances **1e−6** (B6 full-harness parity). **XY symmetry vs dumps:** when symmetry is on, **`iter_*.npy`** frames show **in-loop** **ρ** (mirrored only on **`UMST_SHELL_SYMM_PERIOD`** steps); **`final.npy`** applies the **terminal XY reflection average** once after the last forward — the last GIF frame can differ slightly from **`final.npy`**; rely on **`final.npy`** / **`export_print_ready.py`** for Track L exports. Optional **`UMST_SHELL_ROOF_RAMP=1`** — a **bounded ρ-span smoke** recipe (**`16×16×4`**, **40** outers) and B8 pytest honesty live in **[cartridge `Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md)** (*Shell demo — operator notes* + *Honest status — Track L pytest*) and **`notebooks/_run_shell_demo.sh`** comments (full **40×40×4** + **200** outers remains the Ring‑1 B8 lattice proof).

**Track L acceptance (brief):** `notebooks/_artifacts/striatus_emergence.gif` exists, **≤ 5 MB**, **≥ 30 frames**; `notebooks/_artifacts/striatus_shell_v0.4.stl` exists, **≤ 8 MB**, watertight, **`genus ≥ 1`**, **`volume_fraction ∈ [0.10, 0.25]`**; commit GIF and STL to git.

**Print-ready / structural JSON:** after marching-cubes and gates, run **[`../../umst-concrete-cartridge/notebooks/export_print_ready.py`](../../umst-concrete-cartridge/notebooks/export_print_ready.py)** so **`striatus_shell_v0.4.print_ready.json`** includes the B7 variance/genus/volume-fraction fields; **[`../../umst-concrete-cartridge/notebooks/tests/test_print_ready.py`](../../umst-concrete-cartridge/notebooks/tests/test_print_ready.py)** asserts them.

**Full B6 (`shell_topology_rib_pattern_full_v04`, `--release`, `--ignored`, `UMST_SHELL_RIB_PATTERN=1`):** with **`UMST_SHELL_RIB_FULL_ITERS=200`** (default), gates are **greyness `< 0.15`** (**volume** mean), **`xy_var > 0.1`**, **VF ±1%**, **compliance drop `< 0.6×`** vs iter-1 — distinct from NaN / low-PCG-budget path in **`optimize_shell_3d`** stress note above. **2026-05-11 run + roof semantics:** consolidated in **[2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)**. With **`UMST_SHELL_RIB_FULL_ITERS` below 200** (smoke), the same test only checks **finite** compliance / metrics and a **loose VF band** (±15%); smoke **`--nocapture`** prints **vf / greyness / xy_var / c0 / c1**. Default CI **`shell_topology_rib_pattern_quick`** uses the **roof-slice** greyness bound from the **Track B6** table, **not** this **volume** **< 0.15** gate.

### Track B — shell acceptance (exact names)

| Item | Location |
| --- | --- |
| Optimiser example | `umst-concrete-cartridge/crates/umst-concrete-cartridge/examples/optimize_shell_3d.rs` |
| Smoke / determinism | `crates/umst-concrete-cartridge/tests/shell_demo_smoke.rs` |
| Rib-pattern test (B6) | `crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs` — **`shell_topology_rib_pattern_full_v04`** (brief / **200** outers): **40 × 40 × 4**, seed **42**, end volume fraction **±1 %** of `target_vf = 0.15`; **volume** greyness **mean(4ρ(1−ρ)) < 0.15**; spatial variance of **ρ** over **xy** **> 0.1**; compliance at end **<** compliance at iter **1 × 0.6**. **`shell_topology_rib_pattern_quick`** (default CI): compact grid and **roof-slice** greyness **<** `4·vf·(1−vf)−0.008` — see **Track B6** table. |

**Track B8 (brief “all of”):** (1) `shell_demo_smoke` passes; (2) `shell_topology_rib_pattern` at **40 × 40 × 4**; (3) GIF shows rib formation (subjective; objective backup: variance/genus checks); (4) **`striatus_shell_v0.4.print_ready.json`** has **`genus ≥ 1`** AND **`variance ≥ 0.1`** AND **`volume_fraction ∈ [0.10, 0.25]`**.


## Track J — documentation honesty (v0.4 brief)

Cross-check: **[`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md)** — Track J in that file.

- **J1.** This document’s table is the public solver ↔ lane ↔ verification mapping; prose uses “implemented” only where the **Verification** column matches the CI rule in the header.
- **J2.** [`README.md`](../README.md) summarizes solver maturity and links here plus the v0.4 brief at [`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md) (correct relative URL from this `docs/` file when `composer_prompts/` sits beside `umst-manifold/` in the parent workspace). [`README.md`](../README.md) uses `../composer_prompts/...` for that brief because it is one directory shallower.
- **J3.** Short index: [`PROOF-STATUS.md`](PROOF-STATUS.md) — one row per main-table solver with **`lane`**, **`benchmark_test`**, and **`verification_status`** (`mechanised` \| `analytic-benchmark` \| `literature` \| `none`); stable rows must not leave **`benchmark_test`** empty. Extended verification notes and research-track pointers stay in this file.
- **J4.** [`References.bib`](References.bib) holds a Track-J bibliography skeleton (Bendsøe–Sigmund, Allaire, Sigmund 1997, Stolpe–Svanberg, Scharfetter–Gummel, Buckingham, Roussel et al., Manzano et al., CEB-FIP Model Code 2010, Wangler et al.); extend with DOIs and page-accurate metadata as solver rustdoc cites them.

