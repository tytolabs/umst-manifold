# Solver status (v0.4)

From the `umst-manifold/` tree in this workspace, the v0.4 solver-completion brief is **`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`** (the same relative path from `umst-manifold/docs/`).

Public mapping of solver-facing modules to **lane** (`solver-stable` vs `solver-research` in `Cargo.toml`), **verification** tests, and notes.

CI lint: `python3 scripts/check_solver_status.py` (optional `--check-paths` requires every ``tests/...`` path under **Verification** to exist in this repo; optional `--check-memo-links` checks track memo links and backticked ``docs/research/*.md`` paths).

**Rule:** use “implemented” in prose below only where the listed **Verification** tests exercise the claim on CI (default `cargo test` path for stable rows; `--features solver-experimental` for research rows unless noted).


| Solver                                                                                  | Lane     | Verification                                                                                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `solvers::topology_solver` (`TopologyOptimizer`, density evolution)                     | stable   | `tests/topology_continuation.rs`, `tests/topology_filter.rs`                                    | Feature `topology-density-evolution`. Heat-equation / SIMP-adjacent evolution; conservative stable-lane entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `mechanics::VectorMechanicsSolver`, `adjoint::AdjointCompliance`, bar / plate mechanics | research | `tests/verification/mechanics_analytic.rs`, `tests/verification/adjoint_compliance_analytic.rs` | Features `mechanics-voigt-cauchy`, `topology-density-evolution` (Q1 hex path). **Live:** cantilever chain; **adjoint:** 4-node bar compliance + gradient vs FD. **Plate:** Q1 hex shear-locking aware checks (Kirchhoff table vs brick deferred).                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `solvers::fracture_field` (`PhaseFieldFractureSolver`)                                  | research | `tests/verification/fracture_gamma_convergence.rs`, `tests/verification/staggered_fracture_mechanics_chain.rs`, `tests/verification/staggered_ud_loop_milestone.rs` | Feature `fracture-at2`. **Implemented:** inner AT2 damage relaxation (red–black, odd outer passes); `at2_length_scale_sweep_non_regression` (multi–\(l\) admissibility smoke); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not Γ-limit; see fracture module docs); **Milestone 1:** documented `update_damage_staggered` harness on a 3-node chain with **mechanics equilibrium** strain at degraded stiffness, an **analytic strain surrogate**, and **`strain_tensor_for_fracture_after_mechanics`** for **post-mechanics** symmetric strain on the same chain (see `staggered_ud_loop_milestone` module docs). **Deferred:** Γ-convergence / multi-\((l_0,h)\) dissipation limits; THMC pipeline still does not auto-wire post-mechanics \(\varepsilon(\mathbf u)\) into fracture (`ThmcSolver::step` still prefers `matrix_features` when aligned — see memo). See **DEFERRAL — Fracture**. |
| `solvers::acoustics` (`AcousticWaveSolver`, `AcousticNewmarkBar1dPeriodic`)             | research | `tests/verification/acoustics_plane_wave.rs`                                                    | Feature `acoustics-newmark`. **Live:** `newmark_acceleration_matches_dense_reference_n8`, `newmark_acceleration_matches_dense_reference_n128` (f64 dense ref), `plane_wave_return_map_n100_l2_within_two_percent` (semi-discrete period `T=2π/ω_h`), `plane_wave_h_refinement_second_order_band`, `undamped_energy_drift_under_half_percent_over_1000_steps` (**n=128** energy proxy). Inner linear solve uses **f64** Cholesky (f32 factorization diverged at large `n`). **CI vs brief:** return-map gate is **n=100**; **n=128** with the same CFL-scaled substep recipe shows large phase slip (relative \(L^2\) to `u₀` **~1.4** in baseline — not a 2% margin miss); opt-in ignored bracket `plane_wave_return_map_n128_documented_phase_slip_band`. `AcousticWaveSolver` remains graph-free nodal contraction only. See **DEFERRAL — Acoustics**. |
| `solvers::electrochemistry` (`ElectroChemicalSolver`)                                   | research | `tests/verification/pnp_debye_layer.rs`                                                         | Feature `electrochemistry-pnp` (canonical Track I; forwards to `electrochemistry-mvp` for `#[cfg]`). **Implemented:** SG NP + path-chain **Thomas** Poisson (`try_solve_poisson_chain_thomas`); **explicit Picard** only in `solve_pnp_step` → `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep, \(\rho(c^{(k)})\!\to\!\Phi\), then \(c^{n+1}=c^n-\Delta t\,\nabla\!\cdot J(\Phi,c^{(k)})\)); **`NewtonPnpContext` is not used on that path.** Tests: Fickian zero-field, screening smoke, `picard_coupling_iters_finite_smoke`, `backward_euler_implicit_newton_matches_split_in_linearized_small_dt_limit`. **Partial — parallel Track 14 API:** MVP-chain **implicit backward Euler** + damped Newton via `try_solve_pnp_backward_euler_newton_chain` (host `f64` dense Jacobian); **opt-in from solver config** (default unchanged): set `pnp_implicit_newton_chain: Some(NewtonPnpContext {…})` and call `solve_pnp_step_dispatch` (falls back to explicit Picard if the graph is not an MVP batch-1 chain). **Quasi-steady λ_D (linear):** `debye_screening_256_cells_phi_25mv_decay_length_within_band` advances with **`solve_pnp_step_dispatch`** + implicit chain (same `electrochemistry-pnp` / `electrochemistry-mvp` pair). **Deferred:** variable \(\varepsilon\) stiffness; large-\(|z\Delta\phi|\) f32 robustness; **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** (`#[ignore]`, Gouy–Chapman φ₀=4 V_T) until quasi-steady λ_eff band is validated like the linear sibling. See **DEFERRAL — Electrochemistry** and `docs/research/v0.4_track14_implicit_newton_pnp.md`.
| `solvers::photonics` (`PhotonicsSolver`, `PhotonicsHelmholtzSolver`)                    | research | `tests/verification/photonics_fresnel.rs`, `tests/verification/photonics_curl_curl_stub_default_build.rs` | Feature `photonics-fdfd` (canonical Track I; forwards to `photonics` for `#[cfg]`); deprecated alias `photonics-scaffold` → `photonics-fdfd`. **Implemented:** scalar TE FDFD Helmholtz (Thomas + PML); `curl_curl_y_mode_matches_scalar_helmholtz` and **`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`** exercise `PhotonicsSolver::solve_maxwell_curl_curl` on a uniform x-chain (\(E_y\) from \(J_y\)), including **non-uniform** node \(\varepsilon_r\); `dec_te_primal_tensor_matches_chain_stencil` (DEC primal matvec vs hand TE stencil); `two_half_spaces_fresnel_te_no_pml_matches_analytic` (Dirichlet-closed chain vs bridged continuum Fresnel + residual checks, loose field LS tol). **Default lane:** `photonics_curl_curl_stub_default_build` asserts `solve_maxwell_curl_curl` is an identity when the `photonics` feature is **off** (documented stub). **Deferred:** full DEC vector curl–curl on 2D/3D patches; tensor permittivity; tighter discrete semi-infinite Fresnel calibration. See **DEFERRAL — Photonics**.                                                                                                                                                                                                                                                           |
| `solvers::rheology_flow` (`BinghamFlowSolver`)                                          | research | `tests/verification/rheology_poiseuille.rs`                                                     | Feature `rheology-bingham`. **Implemented:** analytic Poiseuille / plug-width / no-slip; Chorin smokes; `channel_lattice_64x16_quadrilateral_edge_count` (graph wiring scaffold for 64×16 developed benchmark). **Deferred:** `chorin_developed_channel_centreline_vs_regularized_reference` (`#[ignore]`, **empty** stub); `chorin_steady_channel_64x16_vs_regularized_reference` (`#[ignore]`): surrogate pressure Poisson on **65×17** (64×16 cells) blows up ~\(10^3\)/step, **dt-independent** (see test docstring). See **DEFERRAL — Rheology**. |
| `solvers::thmc` (`ThmcSolver`, …)                                                       | research | `tests/verification/thmc_drying_shrinkage.rs`                                                   | Feature `thmc-coupled`. **Implemented:** drying + shrinkage band; explicit transport + hydration via configurable `ThmcHydrationKinetics`; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; **track 13 partial:** backward-Euler **thermal + hydration \(\alpha\)** residual in `thmc_residual::ThmcImplicitEulerThermalHydrationResidual` (`thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`, `thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm` for one damped Newton on the same \((T,\alpha)\) block). **Deferred:** monolithic implicit Newton on fully coupled THMC residuals. See **DEFERRAL — THMC** and `docs/research/v0.4_track13_monolithic_newton_thmc.md`.
| `solvers::statistical_mechanics`                                                        | research | `tests/verification/statmech_vinet_eos.rs`, `tests/verification/statmech_lj_bridge_contract.rs`, `tests/verification/statmech_lj_johnson_eos_reference.rs` | **Stable lane:** Vinet scalar EOS (`statistical-mechanics-vinet`). **Research:** `upscale_potentials` LJ→continuum **placeholder** (`statmech_lj_bridge_contract`). **Johnson (1993)** `f64` EOS lives in `lj_johnson_1993_reference`; it is **not** called inside `upscale_potentials` — tensor rows carry only \((\varepsilon,\sigma)\), while \(K^*\) needs **reduced** \((\rho^*,T^*)\), so types do not align without a wider bridge API. **Opt-in** feature `statistical-mechanics-johnson-reference`: scalar `bulk_modulus_from_lj_state_johnson1993` re-export + in-module `upscale_placeholder_bulk_modulus_documented_gap_vs_johnson_scalar_path`; integration `statmech_lj_johnson_eos_reference` (`placeholder_upscale_bulk_modulus_disagrees_with_johnson_reference_documented`). **`upscale_potentials` remains partial** (placeholder \(K,\gamma_{\mathrm{gc}}\)). Cartridge `physics::clinker_eos` holds phase tables. See **DEFERRAL — Statistical mechanics** and `docs/research/v0.4_track16_virial_lj_bridge.md`.


## Research tracks (v0.4 follow-on)

Numbered research memos under `docs/research/` (`v0.4_track12_staggered_fracture_mechanics.md` … `v0.4_track16_virial_lj_bridge.md`). **Research memo:** **present** for tracks **12–16** (five files on disk). **Full-track acceptance** (memo milestones + closing **DEFERRAL** items) remains outstanding where noted **partial** or **deferred** below; shipped milestones are called out per track.

12. **Goal:** Staggered elasticity–damage (AT2) outer loop, strain providers, and coupling to mechanics — aligned with **DEFERRAL — Fracture**. [memo](research/v0.4_track12_staggered_fracture_mechanics.md). **Research memo:** present. **Implementation:** **partial** — inner AT2 + `solve_staggered_with_mechanics` / chain harnesses in listed fracture tests; **`strain_tensor_for_fracture_after_mechanics`** exercised for **mechanics-backed** strain (plus analytic surrogate) in `staggered_ud_loop_milestone`. **Still open:** Γ-limit / multi-\((l_0,h)\); THMC does **not** auto-call that helper on the fracture path (see memo + deferral).

13. **Goal:** Monolithic implicit Newton / JFNK for fully coupled THMC, aligned with **DEFERRAL — THMC**. [memo](research/v0.4_track13_monolithic_newton_thmc.md). **Research memo:** present. **Implementation:** **partial** — `ThmcImplicitEulerThermalHydrationResidual::assemble` + `thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`; **`one_damped_newton_step`** + `thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm` (**thermal + hydration \(\alpha\)** block only, dense FD Jacobian on tiny chains). Full stacked THMC / humidity / mechanics implicit Newton–Krylov still deferred.

14. **Goal:** Implicit Newton (or Newton–Krylov) for coupled Poisson–Nernst–Planck; ties to **DEFERRAL — Electrochemistry**. [memo](research/v0.4_track14_implicit_newton_pnp.md). **Research memo:** present. **Implementation:** **partial** — split-step **Picard** (`solve_pnp_step_experimental`) on general path; **MVP-chain** backward Euler + damped Newton is a **separate** API (`try_solve_pnp_backward_euler_newton_chain`, `f64` dense Jacobian) optionally reachable through **`pnp_implicit_newton_chain` + `solve_pnp_step_dispatch`**; regressions in `pnp_debye_layer.rs`. **Linear quasi-steady λ_D gate on CI:** `debye_screening_256_cells_phi_25mv_decay_length_within_band` uses the shared harness with **`solve_pnp_step_dispatch`** (implicit chain). **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** remains `#[ignore]` (large φ₀) until the same admissibility band is validated.

15. **Goal:** DEC / Whitney-style vector curl–curl Maxwell beyond the 1-D TE chain reduction — aligned with **DEFERRAL — Photonics**. [memo](research/v0.4_track15_dec_curl_curl_photonics.md). **Research memo:** present. **Implementation:** **partial** — uniform x-chain TE \(E_y\): `curl_curl_y_mode_matches_scalar_helmholtz`, **`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`**, and `dec_te_primal_tensor_matches_chain_stencil` in `photonics_fresnel.rs`; production \(d_1\) / 2D–3D DEC Maxwell still deferred.

16. **Goal:** Virial / EOS-grounded Lennard-Jones → continuum bridge for `upscale_potentials` (bulk modulus and grand-canonical surface tension), replacing placeholder scaling in **DEFERRAL — Statistical mechanics**. [memo](research/v0.4_track16_virial_lj_bridge.md). **Research memo:** present. **Implementation:** **partial (reference + API surface)** — Johnson (1993) `f64` reference + CI documenting placeholder mismatch; optional feature **`statistical-mechanics-johnson-reference`** re-exports `bulk_modulus_from_lj_state_johnson1993` for parity checks. **Deferred** for the track goal: virial / coexistence route into **`upscale_potentials` itself** (no physical bridge yet).


## DEFERRAL — THMC

**Shipped split vs monolithic:** `ThmcImplicitTAlphaNewtonConfig` (when `Some`) runs **damped Newton** only on the backward-Euler **\((T,\alpha)\)** block via `ThmcImplicitEulerThermalHydrationResidual` (dense FD Jacobian, \(\le 64\) stacked DOFs, batch 1); **`ThmcSolver::step`** still advances **humidity** and **quasi-static mechanics** on the explicit / outer-pass path afterward — verified boundary: `thmc_t_alpha_newton_residual_preserves_hydro_mechanics_fields` (humidity and displacement unchanged by the residual Newton). **True monolithic** track‑13 work remains **open:** one residual \(R(U^{n+1})=0\) for the **fully coupled** step \((T,h,\alpha,u,\ldots)\) with consistent Jacobians / Krylov and adaptive `dt`. **Partial milestones already in CI:** `ThmcHydrationKinetics` calibration surface; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; `thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`; damped Newton lowers \(\|R\|_2\) on the \((T,\alpha)\) block; linearized thermal Newton vs analytic decay in the same file. **Next acceptance:** \(\|R\|\) down across inner Newton on a 1D chain for the **stacked** THMC unknowns; `tol` early exit (test path may sync); linearized thermal block vs analytic mode under the **full** implicit map once assembled.


## DEFERRAL — Statistical mechanics

**Closed in this PR (partial):** `statmech_lj_bridge_contract` non-regression on \(K \propto \varepsilon/\sigma^3\), \(\gamma \propto \varepsilon/\sigma^2\); **Johnson–Zollweg–Gubbins (1993)** LJ EOS reference grid in `statmech_lj_johnson_eos_reference` / `lj_johnson_1993_reference` (including explicit check that `upscale_potentials` **does not** match Johnson-derived bulk modulus — placeholder honesty); optional feature **`statistical-mechanics-johnson-reference`** exposes the same-name **`bulk_modulus_from_lj_state_johnson1993`** API on `statistical_mechanics` for side-by-side comparisons (see unit test in-module).

**Still open — physical bridge:** virial / coexistence / Kirkwood–Buff route from \((\varepsilon,\sigma)\) to \((K,\gamma_{\mathrm{gc}})\) with uncertainty bands.

**Next PR acceptance criteria:** (1) reference \(K\) from MD or published EOS at one state point; (2) `upscale_potentials` error vs reference &lt; stated tolerance; (3) document temperature / cutoff dependence.


## DEFERRAL — Fracture

**Closed in this PR (partial):** multi–length-scale **admissibility** smoke (`at2_length_scale_sweep_non_regression`); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not Γ-limit).

**Still open:** Γ-limit across \((l_0,h)\); **THMC** path still feeds fracture from `matrix_features` (or zeros) rather than **fresh** \(\varepsilon(\mathbf u)\) after each mechanics solve; dissipation vs \(G_c\) on refined meshes.

**Next PR acceptance criteria:** (1) harness with at least three \((l_0,h)\) pairs and mesh refinement; (2) ~~fresh \(\varepsilon(\mathbf u)\) after mechanics~~ **partially met** — `strain_tensor_for_fracture_after_mechanics` + `staggered_ud_loop_milestone` / `solve_staggered_with_mechanics` (see `fracture_field`); THMC-orchestrated auto-feed still open; (3) energy monotone band on benchmark chain.


## DEFERRAL — Photonics

**Closed in this PR (partial):** uniform x-chain **TE \(E_y\)** curl–curl reduction equals scalar Helmholtz (`curl_curl_y_mode_matches_scalar_helmholtz`); same operator identity under **piecewise** node \(\varepsilon_r\) (`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`); Burn DEC primal matvec matches hand TE stencil (`dec_te_primal_tensor_matches_chain_stencil`); **default builds** (`photonics` **off**): `photonics_curl_curl_stub_default_build` locks [`PhotonicsSolver::solve_maxwell_curl_curl`] as a documented **no-op** (identity on representative chain tensors — not a physics solve).

**Still open:** DEC-assembled vector curl–curl on **2D/3D** patches (\(d_1\) / face topology); tensor permittivity; tighter Fresnel field match vs a **discrete** semi-infinite reference (current CI test uses bridged continuum target and a relaxed LS band).

**Incremental:** [`tests/dec_identities.rs::dec_curl_d1_annihilates_gradient_on_triangle`](../tests/dec_identities.rs) asserts \(d_1(d_0\omega)=0\) on one oriented 3-cycle (hand-built \(d_1\) row consistent with CCW `ring_b1`). Does **not** exercise Burn `faces_b2` or [`PhotonicsSolver::solve_maxwell_curl_curl`](../src/physics/solvers/photonics.rs).

**Next PR acceptance criteria:** (1) tighten `two_half_spaces_fresnel_te_no_pml_matches_analytic` LS margin and/or add discrete `r_disc`-only comparison; (2) DEC curl operator unit tests on a single triangle/cube patch **wired through production incidence tensors** (`faces_b2` / metric), building on the discrete identity above.


## DEFERRAL — Acoustics

**Closed in this PR (partial):** implicit Newmark on a 1-D periodic bar — `newmark_acceleration_matches_dense_reference_n8` / `_n128` vs dense reference (inner linear solve uses **f64** Cholesky; f32 factorization diverges at large `n`); `plane_wave_return_map_n100_l2_within_two_percent` (semi-discrete period `T=2π/ω_h`, **n=100**); `plane_wave_h_refinement_second_order_band`; `undamped_energy_drift_under_half_percent_over_1000_steps` at **n=128** (energy proxy, not the return map).

**Still open — return map at brief mesh count:** the v0.4 brief’s **128-node** return map is not the default CI gate: with the **same** CFL-scaled substep recipe as the **n=100** test, **n=128** shows large discrete **phase slip** (relative discrete \(L^2\) back to `u₀` **~1.4** in baseline runs — order unity, not a mild exceedance of the 2% band). Opt-in ignored bracket: `plane_wave_return_map_n128_documented_phase_slip_band` (`#[ignore]`). Nodal `AcousticWaveSolver` without graph stiffness remains a separate surface.

**Next PR acceptance criteria:** (1) return-map relative \(L^2\) &lt; 2% at **n=128** with a justified timestep/substep recipe, **or** document and CI a revised mesh count; (2) if the slip is fixed, promote a strict n=128 gate and drop or narrow the ignored bracket test; (3) optional: graph-based acoustic stiffness path beyond nodal contraction.


## DEFERRAL — Electrochemistry

**Closed in this PR (partial):** Poisson–NP **explicit Picard** coupling in `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep); Thomas Poisson on MVP path chains; **MVP-chain implicit backward Euler** + damped Newton (`try_solve_pnp_backward_euler_newton_chain`, host `f64`) with residual regression tests in `pnp_debye_layer.rs`. **Wiring note:** implicit Newton is **not** merged into `solve_pnp_step_experimental`; the default `solve_pnp_step` path remains explicit Picard-only. Research users can opt in via **`pnp_implicit_newton_chain`** + **`solve_pnp_step_dispatch`** (or call `try_solve_pnp_backward_euler_newton_chain` directly).

**Still open:** coupled implicit Newton on **general graphs**; variable \(\varepsilon\) in Laplacian stiffness; `h_inv` per-edge non-uniform metrics; f32 Bernoulli robustness at large \(|z\Delta\phi|\); **Gouy–Chapman** quasi-steady Debye gate (`debye_screening_256_cells_phi_100mv_decay_length_within_band`, `#[ignore]`) — shared harness already uses **`solve_pnp_step_dispatch`** with **`pnp_implicit_newton_chain: Some(…)`** (same `electrochemistry-pnp` / `electrochemistry-mvp` lane); un-ignore after λ_eff vs λ_D band is validated end-to-end for φ₀=4 V_T.

**Next PR acceptance criteria:** (1) residual \(\|R_\Phi\|+\|R_c\|\) decreases under damped Newton on a **non-chain** benchmark or matrix-free JVP path; (2) mass conservation band on closed graph; (3) double-precision option for large \(|z\Delta\phi|\); (4) extend λ_D admissibility to the **φ₀=4 V_T** ignored sibling (or tighten bands) with the same implicit MVP-chain harness already used for the linear 25 mV_T CI gate.


## DEFERRAL — Rheology

**Closed in this PR (partial):** **64×16** channel graph **edge-count** scaffold (`channel_lattice_64x16_quadrilateral_edge_count`, **1968** undirected edges on the quad stencil used by `rheology_poiseuille` harnesses).

**Still open:** (1) **`chorin_developed_channel_centreline_vs_regularized_reference`** remains an **empty** ignored placeholder — no runtime until pressure Poisson + BCs land. (2) **`chorin_steady_channel_64x16_vs_regularized_reference`** (ignored): the shipped Chorin step’s **surrogate** pressure Poisson (`rheology_flow.rs`: fixed-count Richardson on a tangent-projected gradient) yields **dt-independent** blow-up on the **65×17** developed-channel graph (~\(10^3\)× amplification per step to NaN — see that test’s docstring); steady centreline vs regularized analytic is blocked until a true pressure solve (or MAC-style staggered grid) plus inlet/outlet treatment.

**Next PR acceptance criteria:** (1) centreline \(L^2\) error vs analytic &lt; 15% at stated `dt` / step count on **64×16**; (2) pressure correction Poisson RHS aligned with discrete divergence of \(u^\*\); (3) optional env-gated long benchmark (`UMST_RHEOLOGY_POISEUILLE_BUDGET_MS`).


## DEFERRAL — Topology / shell (Track B)

**Expected v0.4 public artefacts (cartridge, not guaranteed in tree):** under `umst-concrete-cartridge/notebooks/_artifacts/`, the v0.4 brief names `striatus_emergence.gif`, `striatus_shell_v0.4.stl`, `striatus_shell_v0.4.print_ready.json`, and optionally `striatus_shell_v0.4.obj` (see **`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`** — Track B8 acceptance and Track L regeneration). A checkout may still only have older names (e.g. `striatus_shell_v0.3.*`); **do not assume** the v0.4 files exist until they are produced and committed per that brief. **Regenerate:** run the shell demo / frame pipeline from the cartridge (`notebooks/_run_shell_demo.sh` with the env vars described in Track L of the same brief) for GIF and mesh exports; run **`../../umst-concrete-cartridge/notebooks/export_print_ready.py`** (and the notebook flow that calls it, if used) for print-ready JSON and structural gates. Per-track-L hygiene: do **not** commit `crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy` dumps—only the final GIF, STL, and `print_ready.json` are intended as committed artefacts.
