# Solver status (v0.4)

**Verification index:** one page of section → memo → tests — [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md). **Completion matrix** (numbered **Verification scope (remaining)** items → PR slices): [`VERIFICATION_COMPLETION_MATRIX.md`](VERIFICATION_COMPLETION_MATRIX.md).

**Completion scoring (v0.4):** Numbered rows **1–10** in **Verification scope (remaining)** align with [`VERIFICATION_COMPLETION_MATRIX.md`](VERIFICATION_COMPLETION_MATRIX.md) (**Exact acceptance criterion** column). Assign **Completion (%)** ∈ **{0, 25, 50, 75, 100}** from that text — not from test count alone. **100** — acceptance fully met on default CI or committed artefacts (matrix may label the row **Done**). **75** — primary acceptance met; remaining bullets in that matrix row still open. **50** — substantial partial milestones without satisfying the full acceptance paragraph. **25** — hooks, smokes, or doc-honesty slices only. **0** — no defensible progress toward that row’s acceptance. **Never** claim **100** unless the matrix acceptance narrative is satisfied end-to-end. Main-table **Completion** maps: topology → **#1**; mechanics → **min(#2, #10)** (**#2** exact acceptance = §R2.1 within-5% thin-plate gate per matrix — **not** met by the \(w/w_K\) ratio-band regression harness alone; see matrix [**#2**](VERIFICATION_COMPLETION_MATRIX.md)); fracture → **#3**; acoustics → **#4**; electrochemistry → **#5**; photonics → **#6**; rheology → **#7**; THMC → **#8**; statistical mechanics → **#9** (`solvers::statistical_mechanics` research row; stable Vinet lane does not substitute for **#9** acceptance).

From the `umst-manifold/` tree in this workspace, the v0.4 solver-completion brief is **`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`** (the same relative path from `umst-manifold/docs/`).

## Pending registry (canonical sources)

**YAML close-out rows (authoritative):** [`MULTI_AGENT_GAP_CLOSURE_PLAN.md`](MULTI_AGENT_GAP_CLOSURE_PLAN.md) frontmatter `todos:` — update the table below when that file’s `id` / `status` fields change.

**Swarm dispatch (MaOS-Workspace layout only):** [`../../MAOS_V04_SWARM_TODO_2026_05_12.md`](../../MAOS_V04_SWARM_TODO_2026_05_12.md) — waves **A–E** and task IDs; keep long narratives there or in this file’s solver lanes, not in repo `README.md` files.

### `MULTI_AGENT_GAP_CLOSURE_PLAN.md` — snapshot verified 2026-05-13

| `id` | `status` in plan frontmatter | Notes |
| --- | --- | --- |
| `closeout-m1-b6` | `pending` | Full **B6** harness at Striatus scale (`shell_topology_rib_pattern_full_v04` / plan prose). |
| `closeout-m1-b8` | `pending` | **`gates_track_b8_all_pass`** in sibling cartridge `notebooks/_artifacts/striatus_shell_v0.4.print_ready.json`. |
| `closeout-m1-l` | `completed-with-residual` | Track **L** artefacts per plan; Ring‑1 still gated on **B8** rollup (plan §Todo registry). |
| `closeout-int-striatus` | `pending` | Coupled cartridge `verify_striatus_coupled_gates.sh` + rollup per plan. |

**Wave D / E (from swarm pack §4):** after **Wave C** (`phase2-b6-200outer`), **Wave D** runs `phase2-export-sidecar-true` then `phase3-striatus-strict`; **Wave E** is `phase5-final-ci` — see [`MAOS_V04_SWARM_TODO_2026_05_12.md`](../../MAOS_V04_SWARM_TODO_2026_05_12.md).

For planned **solver multithreading** (rayon hot-loops, BLAS interaction, feature surface), see the [v0.4 multithreading brief](../../composer_prompts/v0.4_multithreading_solver_parallelisation.md).

Public mapping of solver-facing modules to **lane** (`solver-stable` vs `solver-research` in `Cargo.toml`), **verification** tests, and notes.

**CI** (`.github/workflows/rust.yml`): **`solver-status`** — `python3 scripts/check_solver_status.py --check-paths --check-memo-links --check-statmech-verification-set` (**Verification** test paths exist; when present, memo links under `docs/research/` beside this file resolve; the checker skips removed memos (see track 12–16 section); the `solvers::statistical_mechanics` row lists the full `statmech_*` verification set — see `scripts/check_solver_status.py` docstring). **`phase4-verification-pr`** (PR-only) — `cargo test --release` on dedicated integration crates: **`thmc_monolithic_newton_chain`**, **`photonics_curl_curl_{2d_patch,3d_brick}`** (with **`photonics-fdfd`**), **`statmech_lj_johnson_upscale_bridge`** (with **`solver-research`** union as in the workflow). **`lint`** (merge gate) — `cargo fmt --all -- --check` and `cargo clippy --all-targets --features solver-experimental -- -D warnings` (same feature union as meta `solver-tests` / v0.4 ship checklist). **`research-stack`** — `cargo test --verbose --release --features solver-experimental` with one retry on failure; runs on pushes to `main` and on `workflow_dispatch`.

**Track B6 (CI vs opt-in):**

| Gate | Test | Command sketch |
| --- | --- | --- |
| **Green on default CI** | `shell_topology_rib_pattern_quick` | `cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental` (add `--release` for faster numerics if desired). **Quick harness (`run_rib_quick_metrics` / `short_mesh_and_iters`, all `UMST_SHELL_{NX,NY,NZ,ITERS}` unset):** **9×8×2** cells, **24** Adam outers at **0.005** LR, **50 Pa** roof traction with **x**-ramp **r=0.2**, Heaviside **β=10**, **`DensityNet`** hidden width **32** (scaled init). **Gates:** projected VF within **±15 %** of target (default **`UMST_SHELL_VF` = 0.15**); **top z-face** variance of Heaviside **ρ̂** **> 2×10⁻⁵**; Heaviside **greyness** `mean(4ρ̂(1−ρ̂))` finite in **[0,1]** (quick path does **not** assert full-B6 post–`VolumeProjection` greyness **< 0.15**); scaled compliance **`c1 ≤ 1.45·c0`**. Enlarge via **`UMST_RIB_QUICK=0`** + `UMST_SHELL_*`. Full B6 **volume**-mean greyness **< 0.15**, **`xy_var > 0.1`**, compliance drop on **40²×4 / 200** outers remains on **`shell_topology_rib_pattern_full_v04`**. |
| **`#[ignore]` full Striatus B6** | `shell_topology_rib_pattern_full_v04` | Set **`UMST_SHELL_RIB_PATTERN=1`**, then `cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental shell_topology_rib_pattern_full_v04 --release -- --ignored`. Put **`--release` on the `cargo test` side** (before `--`); flags after `--` are forwarded to the test binary and are not compile profiles. |

<span id="p0-runbook-b6"></span>

### P0 runbook — `shell_topology_rib_pattern_full_v04` (B6, honest)

**Purpose:** one Striatus-scale **B6** proof attempt (**40×40×4**, Burn seed **42**); default CI remains **`shell_topology_rib_pattern_quick`**.

**Environment — 200 Adam outers:** **`UMST_SHELL_RIB_PATTERN=1`** (required). Use **`UMST_SHELL_RIB_FULL_ITERS=200`** or omit it — the harness **defaults to 200** (clamped **1…200**). Values **< 200** are **smoke only**: the test **skips** greyness / planar-variance / compliance-ratio gates and only checks finite metrics + a loose VF band.

**Command (`--release` must sit on the `cargo test` argv, before `--`):**

```bash
cd umst-concrete-cartridge
export UMST_SHELL_RIB_PATTERN=1
export UMST_SHELL_RIB_FULL_ITERS=200   # optional; default 200 when unset
cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern \
  --features solver-experimental shell_topology_rib_pattern_full_v04 --release -- --ignored
```

Uniform roof (match **`optimize_shell_3d`** with ramp off): prefix the same command with **`UMST_SHELL_ROOF_RAMP=0`**. Harness default when **`UMST_SHELL_ROOF_RAMP` is unset** is the gentle **x** ramp at **`UMST_SHELL_ROOF_RAMP_F`** (default **0.2**) — see **[2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)**.

**Greyness target:** B6 asserts **volume**-mean **`mean(4ρ(1−ρ)) < 0.15`** on the final **post–volume-projection** nodal **ρ** (`crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs`, **`greyness_mean`** on **`last_rho`**). Last in-repo documented **200**-outer **`--release`** value was **~0.51** (finite **~7655 s** wall) — **not** a pass; see **Solver lanes — Topology / shell** → [2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest) for roof semantics and honesty.

**`gates_track_b8` path (Track L / B8 rollup):** boolean **`gates_track_b8_all_pass`** lives in **`notebooks/_artifacts/striatus_shell_v0.4.print_ready.json`** (paths relative to **`umst-concrete-cartridge/`** repo root in a sibling checkout). It is emitted by **`notebooks/export_print_ready.py`**; **`notebooks/tests/test_print_ready.py`** (or **`python notebooks/test_print_ready.py`**) reads the same field — **`test_print_ready_track_b8_topology_gates`** **skips** when false unless **`UMST_REQUIRE_B8=1`**. **Profiling:** peak GPU VRAM or unified-memory figures are **not** part of default CI or this status table — cite them only from an explicit profiling task or hardware note.

**`gates_track_b8_all_pass` semantics:** in **`notebooks/export_print_ready.py`** (sibling **`umst-concrete-cartridge/`** checkout), the rollup is exactly **`gate_topo_complexity_b7` ∧ `gate_volume_fraction_mesh_b7` ∧ `gate_density_xy_variance_b8`**. It is **`true` only when all three are `true`**; do **not** hand-edit the rollup (or the three gate booleans) out of sync with the numeric fields — re-run the exporter on a **`final.npy`** / STL that actually meets the thresholds. **`gate_volume_fraction_mesh_b7`** uses **nodal** **`mean(ρ)`** (design VF band **[0.10, 0.25]**); **`mesh_volume_fraction_in_bbox`** remains a **diagnostic** (watertight MC volume ÷ mesh AABB).

**Matrix row 1 — milestone gates (`m1-l` / `m1-b8`) (2026-05-11):** aligns with [`VERIFICATION_COMPLETION_MATRIX.md`](VERIFICATION_COMPLETION_MATRIX.md) **#1** (Topology / shell).

- **`m1-l` (Track L committed artefacts):** in the sibling cartridge checkout, **`notebooks/_artifacts/striatus_emergence.gif`**, **`striatus_shell_v0.4.stl`**, and **`striatus_shell_v0.4.print_ready.json`** are present. **`test_striatus_stl_feasibility`** passes (watertight, winding-consistent, overhang, feature-size). **Track L artefact budgets** (GIF ≤ **5 MB**, ≥ **30** frames; STL ≤ **8 MB**): `bash notebooks/check_shell_artifact_budgets.sh` in that repo. **Nodal** design VF **`mean(ρ)` ∈ [0.10, 0.25]** is **met** on the committed sidecar (**`gate_volume_fraction_mesh_b7`**: **true**; **`nodal_volume_fraction`** ≈ **0.153** on last committed export). **Shell topology / rib texture** acceptance (**`gate_topo_complexity_b7`**, **`gate_density_xy_variance_b8`**) is **not** met — see **`m1-b8`**.
- **`m1-b8` (B8 rollup blocked):** committed **`striatus_shell_v0.4.print_ready.json`** has **`gates_track_b8_all_pass`: false** because **`gate_topo_complexity_b7`** and **`gate_density_xy_variance_b8`** are still **false** (largest closed orientable genus **0**, χ **2**; planar density variance **≪ 0.1**). **`gate_volume_fraction_mesh_b7`** is **true** (**`nodal_volume_fraction`** ≈ **0.153**); **`mesh_volume_fraction_in_bbox`** (diagnostic) can remain **≈ 0.98** on thin shells. **`UMST_REQUIRE_B8=1 pytest notebooks/tests/test_print_ready.py`** **fails** until Track L regeneration yields **`gates_track_b8_all_pass`: true** (all three booleans **true** per exporter rollup in that repo).



### Appendix — `UMST_SHELL_*` reference (Matrix #1 / B6 full harness + Striatus demo)

Quick index for **`shell_topology_rib_pattern_full_v04`** (`#[ignore]`; set **`UMST_SHELL_RIB_PATTERN=1`**) and overlapping **`optimize_shell_3d`** / **`notebooks/_run_shell_demo.sh`** env vars.

| Variable | Scope | Default | Notes |
| --- | --- | --- | --- |
| **`UMST_SHELL_RIB_PATTERN`** | Ignored B6 test gate | — | Must be **`1`** or the test refuses to run. |
| **`UMST_SHELL_RIB_FULL_ITERS`** | Full B6 harness | **200** | Clamped **1…200**; **< 200** skips greyness / **`xy_var`** / compliance-drop asserts (smoke). |
| **`UMST_SHELL_ADAM_LR`** | Full B6 harness only | **0.005** | Burn Adam learning rate on outers with finite scaled loss (clamped **1e−7…0.25**). |
| **`UMST_SHELL_ADAM_EPS`** | Full B6 harness only | **1e−5** | Burn Adam ε (clamped **1e−12…1e−2**). |
| **`UMST_SHELL_ADAM_GRAD_CLIP_NORM`** | Full B6 harness only | **off** | **L2** norm clip on **`density_net`** grads when set **> 0** (optional NaN mitigation). |
| **`UMST_SHELL_GREY_LAMBDA`** | Full B6 harness only | **0** | If **`> 0`**, adds **`λ·mean(4ρ(1−ρ))`** to the scaled compliance loss (experimental de-greying); **unset/`0`** preserves historical behaviour. |
| **`UMST_SHELL_SELF_WEIGHT`** | Full B6 + demo | **off** | **`1`** enables gravity (**risky** at Striatus **N** with **f32** PCG). |
| **`UMST_SHELL_VOL_LOOP`** | Full B6 + demo | **on** | **`0`** skips in-loop **`VolumeProjection`**. |
| **`UMST_SHELL_PCG`** | Full B6 + demo | **on** | Jacobi preconditioner for bar PCG. |
| **`UMST_SHELL_MAX_CG`** | Full B6 + demo | **2000** | PCG budget (**also** capped by **`3N`** in equilibrium). |
| **`UMST_SHELL_E_MIN_REL`** | Full B6 + demo | **1e−3** | Void stiffness floor **`E_min / E₀`**. |
| **`UMST_SHELL_HELM`** | Full B6 + demo | **off** | **Only** literal **`=1`** enables Helmholtz on the Burn tape. |
| **`UMST_SHELL_ROOF_RAMP`** | Full B6 vs **`optimize_shell_3d`** | see Topology § | Harness: **`0`** uniform roof; **unset** / other → ramp at **`UMST_SHELL_ROOF_RAMP_F`**. Example **`optimize_shell_3d`**: **only** **`=1`** enables ramp — **unset ≠ harness unset** (see [greyness vs roof-ramp](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)). |
| **`UMST_SHELL_ROOF_RAMP_F`** | Full B6 + demo | **0.2** | **\(w = 1 + r\,i_x/n_x\)** when ramp is on. |
| **`UMST_SHELL_NX` / `NY` / `NZ`** | Demo / quick CI | varies | Full harness fixes **40×40×4**; **`optimize_shell_3d`** clamps env grid. |
| **`UMST_SHELL_ITERS`** | Demo | — | Track L / B8 regeneration expects **200** on **40×40×4** for meaningful **`final.npy`**. |

**Adam NaN / Inf outers (scaled loss):** Total loss is **`surrogate / c_raw[iter1]`** plus optional **`λ·mean(4ρ(1−ρ))`**. Non-finite values **skip Adam** that outer (weights unchanged); the test counts skips and **`eprintln!`** a summary when **`adam_skipped > 0`**. Distinct from iter‑1 **`c_raw`** non-finite (**panic**). Mitigate with **`UMST_SHELL_ADAM_LR`**, **`UMST_SHELL_ADAM_EPS`**, **`UMST_SHELL_ADAM_GRAD_CLIP_NORM`**, **`UMST_SHELL_MAX_CG`**, **`UMST_SHELL_E_MIN_REL`**, **`UMST_SHELL_SELF_WEIGHT=0`**, **`UMST_SHELL_HELM=0`**, or small **`UMST_SHELL_GREY_LAMBDA`** experiments — not an acceptance substitute until a **200**-outer run clears **greyness < 0.15**.

Cartridge-side mirror (deep links / Striatus pipeline): [`../../umst-concrete-cartridge/docs/Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md) — same P0 / Track B / Track L / B8 story.

**Full B6:** **40×40×4**, **200** Adam outers by default; **`UMST_SHELL_RIB_FULL_ITERS`** (clamped **1…200**) shortens the loop for smoke only — **one outer** still runs the full Striatus-scale **forward + autodiff backward** and can take **many CPU minutes** in `--release**, and **does not** satisfy the brief greyness / compliance-ratio gates (those need the full **200**-outer schedule). Budget **hours** for the default **200**-outer proof on a typical workstation unless noted otherwise. **Greyness / roof (2026-05-11 log + today's defaults):** see **Solver lanes — Topology / shell → [2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)** — last documented **200**-outer **`--release`** miss **~0.51** vs **< 0.15** (**finite**, **~7655 s**); short smokes show **`c1`** improves with **x** ramp more than greyness; **no** fresh **200**-outer in-repo proof that greyness clears. **2026-05-11 smoke sample (gates skipped):** `UMST_SHELL_RIB_FULL_ITERS=1`, **`--release`**, **~37 s** wall (`time -p`); harness printed **vf=0.1500, greyness=0.5100, xy_var=0, c0=c1=1** — **not** evidence toward the **200**-outer B6 pass.

**Rule:** use “implemented” in prose below only where the listed **Verification** tests exercise the claim on CI (default `cargo test` path for stable rows; `--features solver-experimental` for research rows unless noted).


| Solver                                                                                  | Lane     | Completion (%) | Verification                                                                                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------- | -------- | -------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `solvers::topology_solver` (`TopologyOptimizer`, density evolution)                     | stable   | **25** ([#1](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/topology_continuation.rs`, `tests/topology_filter.rs`                                    | Feature `topology-density-evolution`. Heat-equation / SIMP-adjacent evolution; conservative stable-lane entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `mechanics::VectorMechanicsSolver`, `adjoint::AdjointCompliance`, `adjoint_q1_hex::AdjointComplianceQ1Hex`, bar / plate mechanics | research | **25** (min [**#2**](VERIFICATION_COMPLETION_MATRIX.md), [**#10**](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/mechanics_analytic.rs`, `tests/verification/adjoint_compliance_analytic.rs`, `tests/verification/adjoint_q1_hex_compliance_analytic.rs`, `tests/verification/adjoint_q1_hex_matches_bar_in_limit.rs` | Features `mechanics-voigt-cauchy`, `topology-density-evolution`, **`mechanics-adjoint-q1-hex`** (Q1 hex discrete-adjoint compliance). **`extruded_plate`** / **`q1_hex_elasticity`** compile when **either** `mechanics-voigt-cauchy` **or** `topology-density-evolution` is enabled (`src/physics/mod.rs`); **`mechanics_analytic`** runs on **`solver-stable`** (topology) **or** **`cargo test --release -p umst-manifold --features mechanics-voigt-cauchy`**. **Phase 1A (v0.4 honest closeout):** `q1_hex_elasticity` keeps **B-bar** on volumetric normals + centroid **\(\gamma_{yz},\gamma_{xz}\)** with **unified** Voigt **\(\mathbf D\)** on that strain operator (a naive **\(\mathbf D_{\mathrm{vol}}\)** centroid + **\(\mathbf D_{\mathrm{dev}}\)** Gauss **quadrature** split was **not** shipped — it is **not** pointwise **\(\mathbf D\varepsilon\)** and regressed consistency checks; see `q1_hex_elasticity` roadmap). **`#[ignore]` `adjoint_q1_hex_compliance_near_bar_z_skeleton_slender_limit`:** z-skeleton bar compliance matches the four-chain closed form (\(c_{\mathrm{bar}}\approx2.47\times10^9\)); the **1×1** cross-section Q1 brick is still **≈64%** more compliant (\(c_{\mathrm{hex}}\approx4.05\times10^9\), **`--release`** **2026-05-12**) because bars omit face/shear coupling the solid carries — not a tight 1D bar limit until the harness is refined. **Live:** cantilever chain; **`packed_bar_network_equilibrium_uniform_axial_strain_tip_load_distinct_from_acoustic_newmark_bar_path`** (Solver-Status **#10** — vector static bar network vs scalar **`acoustics-newmark`**); **adjoint:** 4-node bar compliance + gradient vs FD. **Plate:** Q1 hex shear-locking aware checks; **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`** pins a **narrowed** \(w/w_K\) band (`1.1\times10^{-4}<w/w_K<1.6\times10^{-4}`) with bilinear-consistent top pressure `ExtrudedPlateMechanics::body_force_top_uniform_pressure`. **`#[ignore]` `plate_centre_deflection_kirchhoff_ssss_q1_hex_within_five_percent`** (**32²×4**, **h/L=0.02**) holds the §R2.1 **\(\le 5\%\)** centre-deflection assertion vs [`kirchhoff_centre_w_ssss`]; **2026-05-12** `--release` sample **rel_err≈0.999997** (\(w\approx 6.7\times10^{-9}\,\mathrm{m}\), \(w_K\approx 1.95\times10^{-3}\,\mathrm{m}\); **same** centre \(w\) with the **unified-\(\mathbf D\)** matvec on the same mesh) — **not** default CI. **`#[ignore]` `plate_r21_kirchhoff_ssss_centre_w_within_5pct_brick_path_gate`** (48²×4) remains the env-gated manual brick probe. **FE BCs differ** from classical Kirchhoff SSSS (full-face bottom \(u_z=0\) plus pins vs simply-supported edge data on all spanwise edges), so the active ratio band is a **regression** guard on the shipped brick path, **not** a within-5% thin-plate accuracy gate and **not** §R2.1 closure. Follow-up [§R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate) tracks the **separate** planned thin-plate **error** target once enrichment + aligned BCs land. **Geometric NL / finite-strain gap:** mechanics scope memo.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `solvers::fracture_field` (`PhaseFieldFractureSolver`)                                  | research | **50** ([#3](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/fracture_gamma_convergence.rs`, `tests/verification/staggered_fracture_mechanics_chain.rs`, `tests/verification/staggered_ud_loop_milestone.rs`, `tests/verification/thmc_drying_shrinkage.rs` (kinematic THMC ↔ helper parity) | Feature `fracture-at2`. **Implemented:** inner AT2 damage relaxation (red–black, odd outer passes); `at2_length_scale_sweep_non_regression` (multi–\(l\) admissibility smoke); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not sharp-interface Γ-limit); **`at2_gamma_convergence_three_length_scales`** — partial Γ-type check: three \((l_0,h)\) pairs, \(h/l_0=\tfrac14\), \(\psi^+\!=0\), discrete \(D_h\) vs **`Gc`** \(\lt 2\%\) and non-worsening across refinement (fixed-strain relaxation); **`at2_discrete_surface_functional_toy_chain_matches_hand_total`** (hand-checked discrete \(D_h\) on a 3-node toy via shared `discrete_at2_bar_surface_energy_1d`); **`staggered_fracture_compliance_monotone_increasing`** (staggered bar compliance vs outer iterations). **Milestone 1:** documented `update_damage_staggered` harness on a 3-node chain with **mechanics equilibrium** strain at degraded stiffness, an **analytic strain surrogate**, and **`strain_tensor_for_fracture_after_mechanics`** for **post-mechanics** symmetric strain on the same chain (see `staggered_ud_loop_milestone` module docs). **THMC (`thmc-coupled`):** with SI `node_positions` `[N,3]` and compatible BC mask, **`ThmcSolver::step`** feeds fracture from **`strain_tensor_from_bar_network_displacement`** on the equilibrium displacement (same kinematics as post-mechanics helper; `thmc_drying_shrinkage::bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`). **Remaining:** Γ-limit with driven \(\psi^+\); broader \((l_0,h)\); within-step THMC \(u\!\leftrightarrow\!d\); embedding / `matrix_features` fallbacks — **Solver lanes — Fracture** + track 12 memo §2.3 / §7 (§7 = proposed acceptance + test names only). **Compressive crushing / DP–cap plasticity:** not implemented; AT2 drive uses spectral **ψ⁺** (tensile principal strains) only — `docs/research/concrete_compressive_plasticity_scope`; composer context [`v0.4_phase_2_3_master_plan.md`](../../composer_prompts/v0.4_phase_2_3_master_plan.md) (P2.4 fracture, P3.1 staggered mechanics). **Trust:** highly compressed slender struts have **no** in-repo crushing/yield cap. |
| `solvers::acoustics` (`AcousticWaveSolver`, `AcousticNewmarkBar1dPeriodic`)             | research | **100** ([#4](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/acoustics_plane_wave.rs`                                                    | Feature `acoustics-newmark`. **Live:** `newmark_acceleration_matches_dense_reference_n8`, `newmark_acceleration_matches_dense_reference_n128` (f64 dense ref), **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`**, **`plane_wave_return_map_n128_l2_within_two_percent`** (return map after one lumped eigenperiod \(T=2\pi/\Omega\) with \(m=\rho\Delta x\); same CFL-scaled substep recipe); `plane_wave_h_refinement_second_order_band`; `undamped_energy_drift_under_half_percent_over_1000_steps` (**n=128** energy proxy). Inner linear solve uses **f64** Cholesky (f32 factorization diverged at large `n`). **`AcousticWaveSolver`** remains graph-free nodal contraction only. See **Solver lanes — Acoustics**. |
| `solvers::electrochemistry` (`ElectroChemicalSolver`)                                   | research | **75** ([#5](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/pnp_debye_layer.rs`                                                         | Feature `electrochemistry-pnp` (canonical Track I; forwards to `electrochemistry-mvp` for `#[cfg]`). **Verified on default CI (`pnp_debye_layer` when `electrochemistry-pnp` is enabled; root **`research-stack`** runs **`--release`):** SG NP + path-chain **Thomas** Poisson (`try_solve_poisson_chain_thomas`); **explicit Picard** only in `solve_pnp_step` → `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep, \(\rho(c^{(k)})\!\to\!\Phi\), then \(c^{n+1}=c^n-\Delta t\,\nabla\!\cdot J(\Phi,c^{(k)})\)); **`NewtonPnpContext` is not used on that path.** Tests: Fickian zero-field, screening smoke, `picard_coupling_iters_finite_smoke`, `backward_euler_implicit_newton_matches_split_in_linearized_small_dt_limit`, **`debye_implicit_dispatch_short_horizon_smoke`** (Track 14 **`solve_pnp_step_dispatch`** + **`pnp_implicit_newton_chain`**, short chain), **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** (same dispatch wiring with default **`mesh_spacing = 1`**; samples implicit BE `‖R‖₂` along a Debye-style trajectory — **orthogonal** to the **`mesh_spacing = h`** λ_D LS gates below). **Quasi-steady λ_D exponential LS gates (default `pnp_debye_layer`, not `#[ignore]`):** **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** (φ₀ = 1 V_T, interior LS **`λ_eff`** vs continuum **`λ_D`**, **±10 %**) and **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** (φ₀ = 4 V_T, **±15 %**) use **`solve_pnp_step_dispatch`** + **`pnp_implicit_newton_chain: Some(debye_implicit_newton_linearized_sg_for_lambda_d_gate())`** — **`linearize_sg_fickian: true`**, **`max_newton_iters: 24`**, host **`f64`** residual tol **`1e-9`** (Fickian-linearised SG in the implicit BE residual → **analytic** Jacobian; **not** full nonlinear SG at Newton level). Harness sets **`mesh_spacing = h = L/(N-1)`** with Poisson **\(h^2\)**-scaled interior RHS consistent with the SG **`J\propto D/h`** map; tail-heavy LS window **≈0.58…0.97** on interior indices at **`N=256`**, **`dt`·`steps` = `1.5e-3`×`10_000` ≈ 15** nondimensional time units. **`linearize_sg_fickian: true`** (Debye-gate / analytic path): each Newton correction is **\(O(N)\)** via **three Thomas solves** (no dense \((3N)\times(3N)\) solve). **`linearize_sg_fickian: false`** (**chain** implicit Newton): **node-major band** column FD Jacobian (envelope **O(N·bw)**); Newton correction via **dense expand + Gaussian elimination** on a **(3N)²** scratch (**O((3N)³)**) in **`try_solve_pnp_backward_euler_newton_chain`**. Experimental **row-band partial-pivot band LU** (**O(dim·bw²)**) is implemented but **not** wired into **`try_solve`** — it does **not** yet match dense full-column pivot on the same Jacobian at fixed envelope width (**`#[ignore]`** **N=256** harness prints **`max|δ_lu−δ_de|`**, not asserted on CI). **`full_sg_frozen_jacobian_inner_iters`** can freeze the assembled band across inner Newton iterations (**extra band solves only**). **Runtime honesty:** the pair is **~0.7 s** in **`--release`** on typical laptops but **~10 s each** in unoptimised **`cargo test`** — prefer **`--release`** for local reruns. **Remaining:** **O(dim·bw)** scratch-only or Krylov / matrix-free at large **N**; sparse layout on **general graphs**; variable \(\varepsilon\) on **non-chain** graphs beyond the **uniform-\(\mathcal{L}\)** Poisson **Jacobi–PCG** surrogate in **`solve_pnp_split_step_experimental_with_refs`** (lib **`poisson_graph_pcg_four_node_star_reduces_laplacian_residual`**); general-graph implicit Newton; large-\(|z\Delta\phi|\) f32 robustness — **Solver lanes — Electrochemistry** and `docs/research/v0.4_track14_implicit_newton_pnp`.
| `solvers::photonics` (`PhotonicsSolver`, `PhotonicsHelmholtzSolver`)                    | research | **50** ([#6](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/photonics_fresnel.rs`, `tests/verification/photonics_curl_curl_2d_patch.rs`, `tests/verification/photonics_curl_curl_3d_brick.rs`, `tests/verification/photonics_curl_curl_stub_default_build.rs` | Feature `photonics-fdfd` (canonical Track I; forwards to `photonics` for `#[cfg]`); deprecated alias `photonics-scaffold` → `photonics-fdfd`. **Implemented:** scalar TE FDFD Helmholtz (Thomas + PML); `curl_curl_y_mode_matches_scalar_helmholtz` and **`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`** exercise `PhotonicsSolver::solve_maxwell_curl_curl` on a uniform x-chain (\(E_y\) from \(J_y\)), including **non-uniform** node \(\varepsilon_r\); `dec_te_primal_tensor_matches_chain_stencil` (DEC primal matvec vs hand TE stencil); `two_half_spaces_fresnel_te_no_pml_matches_analytic` (Dirichlet-closed chain vs bridged continuum Fresnel + residual checks, loose field LS tol). **Default lane:** `photonics_curl_curl_stub_default_build` asserts `solve_maxwell_curl_curl` is an identity when the `photonics` feature is **off** (documented stub). **Partial (embedded patch):** `PhotonicsDecFacesPatch` + `solve_maxwell_curl_curl` — small-`N` host-dense nodal **vector** DEC (**unweighted** **d₁ᵀd₁** on edge tangents from SI **`coords_n3`**, nodal **`k₀² ε`** with **full `3×3` permittivity** in the listed patch tests, gauge pin at node **`0`**; requires **`eps_r_imag` ≈ 0**): **`solve_maxwell_dec_patch_quad_split_pin_residual_tight`**, **`solve_maxwell_dec_patch_quad_split_tensor_eps_residual`**, **`solve_maxwell_dec_patch_quad_split_embedded_r3_residual`**, **`solve_maxwell_dec_patch_two_quads_strip_residual`**. **Remaining for production 3D:** dual **Hodge** / circumdual metrics, **sparse** Krylov, **complex ε** + PML on patches, BCs beyond gauge pin, Fresnel on assembled meshes. See **Solver lanes — Photonics**.                                                                                                                                                                                                                                                           |
| `solvers::rheology_flow` (`BinghamFlowSolver`)                                          | research | **50** ([#7](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/rheology_poiseuille.rs`                                                     | Feature `rheology-bingham`. **Implemented:** analytic Poiseuille / plug-width / no-slip; regularized Bingham centreline and small-\(\varepsilon\) vs Buckingham profile; Chorin `chorin_single_step_finite_smoke`; **pressure Poisson:** Jacobi-preconditioned CG on \(-\mathcal{L}\) (`rheology_flow.rs`); **`f02120d`:** hardened Jacobi-PCG pressure; **`chorin_jacobi_pcg_step_velocity_amplification_regression_guard`** (historical name; two-step \(\|u\|_\infty\) bracket on **65×17**); `chorin_channel_65x17_thirty_substeps_remain_finite`; **`chorin_developed_channel_centreline_vs_regularized_reference`** + **`chorin_steady_channel_64x16_vs_regularized_reference`** on **`research-stack`** (**no `#[ignore]`** — short-horizon **65×17** smokes: finite ‖u‖/‖p‖, centreline below developed regularized reference / ‖u‖∞ band); Roussel \(\lambda\) explicit-step parity + Wangler-style quiescent yield-growth fit; `channel_lattice_64x16_quadrilateral_edge_count` (graph scaffold). **Not default `cargo test` without features:** lane needs **`rheology-bingham`** (bundled in root **`research-stack`**). **Still not CI at default stable union:** long-run steady \(L^2\) / plug-width vs the regularized 1-D reference (multi-thousand-step acceptance) — research harness when MAC + open BCs land. See **Solver lanes — Rheology**. |
| `solvers::thmc` (`ThmcSolver`, …)                                                       | research | **75** ([#8](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/thmc_drying_shrinkage.rs`, `tests/verification/thmc_monolithic_newton_chain.rs` | Feature `thmc-coupled`. **Implemented:** drying + shrinkage band; explicit transport + hydration via configurable `ThmcHydrationKinetics`; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; **track 13 partial:** backward-Euler **thermal + hydration \(\alpha\)** residual in `thmc_residual::ThmcImplicitEulerThermalHydrationResidual` (`thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`, `thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm` for one damped Newton on the same \((T,\alpha)\) block). **Track 13 — monolith bookkeeping (Phase 1–4):** Phase **1** — field-major stacked flat residual including a **placeholder** \(R_u=m(\mathbf u-\mathbf u^n)\) tail + `evaluate_quasi_static_r_u` vs `projected_bar_equilibrium_residual` (`thmc_implicit_euler_t_h_alpha_u_placeholder_r_u_and_flat_layout_two_nodes`, `thmc_r_u_zero_at_solved_equilibrium_two_node_chain`); Phase **2** — `assemble_with_quasi_static_r_u` / `stacked_flat_residual_field_major_quasi_static` + L² parity (`thmc_monolithic_residual_blocks_consistent_two_nodes`); Phase **3** — dense damped Newton on field-major \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) (`one_damped_newton_step_with_quasi_static_r_u`, `damped_newton_iterations_with_quasi_static_r_u`; CI `thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes`); Phase **4** (notional shrink **increment** in quasi-static \(R_u\), coupling plan §4) — optional `ThmcImplicitEulerThermalHumidityHydrationResidual::ru_shrinkage_water_cement_ratio` feeds per-edge \(\Delta\varepsilon_{\mathrm{sh}}(h^n\to h^{\mathrm{trial}})\) into `projected_bar_equilibrium_residual` (`shrink_strain_from_saturation_loss_tensor`, `thmc_quasi_static_r_u_shrink_increment_flat_humidity_parity_two_node_chain`, `thmc_quasi_static_r_u_shrink_increment_raises_norm_when_humidity_drops_two_node_chain`) — roadmap: v0.4_track13_monolithic_thmc_mechanics_coupling_plan.md. **Phase 5 (integration, small-DOF) — `3e93a0a`:** [`ThmcSolver::monolithic_thmc_newton`](../src/physics/solvers/thmc.rs) wires `ThmcImplicitEulerThermalHumidityHydrationResidual::damped_newton_iterations_with_quasi_static_r_u` inside [`ThmcSolver::step`](../src/physics/solvers/thmc.rs) → **`step_experimental`** per outer pass when `batch==1`, `[N,3]` `node_positions`, \(\mathrm{DOF}_\mathrm{stack}\le 64\), **`drying_last_node_evaporation_k == 0`**, mutually exclusive with `implicit_t_alpha_newton`; optional call-site guard [`ThmcSolver::step_monolithic_implicit`](../src/physics/solvers/thmc.rs); **`ThmcMonolithicNewtonConfig::stacked_residual_l2_tolerance`** / **`stacked_residual_relative_to_initial`** wire stacked \(\|R\|_2\) early-exit predicates into `damped_newton_iterations_with_quasi_static_r_u` (host reads; see [`thmc_residual.rs`](../src/physics/solvers/thmc_residual.rs)); CI `thmc_step_monolithic_newton_*` (including stacked-residual early-exit parity tail on `thmc_step_monolithic_newton_matches_standalone_dense_newton_two_nodes`), `thmc_monolithic_newton_residual_tol_early_exit_truncates_norm_trail`, `thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes` in `thmc_drying_shrinkage.rs`. **Fracture coupling (SI bar path):** post-mechanics \(\varepsilon(\mathbf u)\) via **`strain_tensor_from_bar_network_displacement`** when `node_positions` is `[N,3]` — **Solver lanes — Fracture** + track 12 memo §2.3; kinematic parity test `bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`. **Remaining:** large-\(N\) monolith / JFNK; **within-step** \(u\!\leftrightarrow\!d\) stagger — **Solver lanes — THMC** and `docs/research/v0.4_track13_monolithic_newton_thmc`.
| `solvers::statistical_mechanics`                                                        | research | **25** ([#9](VERIFICATION_COMPLETION_MATRIX.md)) | `tests/verification/statmech_vinet_eos.rs`, `tests/verification/statmech_lj_bridge_contract.rs`, `tests/verification/statmech_lj_johnson_eos_reference.rs`, `tests/verification/statmech_lj_johnson_upscale_bridge.rs`, `tests/verification/statmech_mechanics_fracture_bridge.rs` | **Stable lane:** Vinet scalar EOS (`statistical-mechanics-vinet`). **Research:** **`[B,2]`** `upscale_potentials` LJ→continuum **placeholder** (`statmech_lj_bridge_contract`). **`[B,4]`** rows \((\varepsilon,\sigma,\rho^*,T^*)\) → Johnson **`K`** via `physical_bulk_modulus_johnson1993` (`upscale_potentials_b4_matches_physical_bulk_modulus_johnson1993`, lib `upscale_potentials_b4_batch_matches_physical_johnson_per_row`, lib `upscale_potentials_b4_johnson_matches_cat_of_column_slices`). **Johnson (1993)** `f64` EOS in `lj_johnson_1993_reference`. **Scalar bridge (always-on):** `physical_bulk_modulus_johnson1993` / `relative_placeholder_bulk_modulus_gap_vs_johnson1993`; integrations `physical_bulk_modulus_johnson1993_statmech_bridge_matches_lj_reference` + `placeholder_upscale_bulk_modulus_disagrees_with_johnson_reference_documented` (**`[B,2]`** gap). **Opt-in** `statistical-mechanics-johnson-reference`: `bulk_modulus_from_lj_state_johnson1993` re-export + `upscale_placeholder_bulk_modulus_documented_gap_vs_johnson_scalar_path`. **`γ_gc`** remains placeholder; **`[B,4]`** **`K`** not AD-safe (host materialise). Cartridge `physics::clinker_eos` holds phase tables. See **Solver lanes — Statistical mechanics** and `docs/research/v0.4_track16_virial_lj_bridge`.


## Research memos (v0.4 follow-on)

Former numbered memos under `docs/research/` (tracks **12–16**) were **removed** from this repository to reduce doc drift; the bullets below retain the **scope summary** and verification pointers. Full-track acceptance remains **open** where solver lanes below say **partial**; shipped milestones are cited in the main table.

- **12 — Fracture / AT2:** memo. Partial implementation in listed fracture tests; §7 lists proposed **`#[test]`** names for future Γ-type and coupling work.
- **13 — THMC:** Newton / JFNK memo; monolithic THMC + mechanics coupling plan. Partial: backward-Euler **\((T,\alpha)\)** residual + damped Newton smoke on tiny chains; Phase 1–2 stacked-\(u\) bookkeeping + quasi-static \(R_u\) assembly tests above; **Phase 3** dense Newton on \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) (`thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes`); **Phase 4** shrink-increment hook in `evaluate_quasi_static_r_u` (optional `ru_shrinkage_water_cement_ratio`) + tests named in the THMC table row; **Phase 5** **`3e93a0a`:** **`ThmcMonolithicNewtonConfig`** on **`monolithic_thmc_newton`** wired in **`ThmcSolver::step` → `step_experimental`** (`thmc_step_monolithic_newton_*`, `thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes`).
- **14 — Electrochemistry:** memo. Partial: Picard path + MVP-chain implicit Newton dispatch (`solve_pnp_step_dispatch`); quasi-steady **`debye_screening_256_cells_*`** λ_D LS gates on default CI (**`8a2163a`**, no **`#[ignore]`**).
- **15 — Photonics:** memo. Partial: 1-D TE chain + **`PhotonicsDecFacesPatch`** small dense vector DEC in **`solve_maxwell_curl_curl`** (`photonics_fresnel.rs` tests named in [`Solver-Status.md`](Solver-Status.md) photonics row).
- **16 — Statistical mechanics:** memo. Partial: Johnson reference + documented placeholder gap vs `upscale_potentials`.


## Verification scope (remaining)

Single checklist of **open verification / ship items** (solver-by-solver); solver-lane sections below hold acceptance detail and commands without repeating this list verbatim.

**Completion (%)** for rows **1–10** uses the **Completion scoring (v0.4)** rubric near the top of this file; criterion text lives in [`VERIFICATION_COMPLETION_MATRIX.md`](VERIFICATION_COMPLETION_MATRIX.md).

| # | Completion (%) |
| --- | --- |
| 1 | **25** |
| 2 | **25** |
| 3 | **50** |
| 4 | **100** |
| 5 | **75** |
| 6 | **50** |
| 7 | **50** |
| 8 | **75** |
| 9 | **25** |
| 10 | **25** |

1. **Topology / shell (Tracks B + L):** Run **`shell_topology_rib_pattern_full_v04`** at **40×40×4** (documented NaN / bar-network CG conditioning); commit Track L artefacts (`striatus_emergence.gif`, `striatus_shell_v0.4.stl`, `striatus_shell_v0.4.print_ready.json`, optional `.obj`) and satisfy Track B8 — regeneration command and paths under **Solver lanes — Topology / shell**. **Track L demo (cartridge):** `notebooks/_run_shell_demo.sh` from **`umst-concrete-cartridge/`** repo root (env `UMST_SHELL_DUMP_ITER` / `UMST_SHELL_DUMP_STRIDE` / `UMST_SHELL_ITERS`; optional `UMST_SHELL_*` stability knobs — **`../../umst-concrete-cartridge/docs/Solver-Status.md`** §Topology / shell + `optimize_shell_3d` rustdoc). **Quick vs full greyness (honest):** default CI **`shell_topology_rib_pattern_quick`** measures **Heaviside** **ρ̂** (pre–`VolumeProjection` in `run_rib_quick_metrics`): asserts **top z-face** slice variance **> 2×10⁻⁵**, **Heaviside** greyness `mean(4ρ̂(1−ρ̂))` finite in **[0,1]**, and **`c1 ≤ 1.45·c0`** — it does **not** assert full-B6 **post–projection** **volume**-mean greyness **< 0.15** (that gate lives only on **`shell_topology_rib_pattern_full_v04`** with **200** outers; see **Track B6** table above and [2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)). **Quick vs demo roof (code):** with all `UMST_SHELL_{NX,NY,NZ,ITERS}` unset, quick uses **9×8×2**, **24** outers, **50 Pa**, **r = 0.2** (see **`shell_topology_rib_pattern.rs`** `#!` / **`run_rib_quick_metrics`**); **`optimize_shell_3d`** uses **50 Pa** and the **x** ramp only when **`UMST_SHELL_ROOF_RAMP=1`** (**`UMST_SHELL_ROOF_RAMP_F`**, default **0.2**), else **50 Pa** uniform. The **opt-in B6 harness** uses **`UMST_SHELL_ROOF_RAMP=0`** → uniform roof; **unset** or any value other than **`0`** / **`1`** turns the gentle **x** ramp **on** at **`UMST_SHELL_ROOF_RAMP_F`** (default **0.2**) so CI quick matches Striatus load asymmetry — **`optimize_shell_3d`** still requires literal **`UMST_SHELL_ROOF_RAMP=1`** for the same ramp. **Shell demo operator notes** (pitfalls: short `UMST_SHELL_ITERS`, self-weight, stale `iter_*.npy` / manifest, Trimesh deps): see **Solver lanes — Topology / shell** → *Shell demo — operator notes* below and the mirrored bullets in [`../../umst-concrete-cartridge/docs/Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md) (*Shell demo — operator notes* under Track L).
2. **Mechanics (plates):** Move Q1 hex SRI toward the brief’s **within-5% thin-plate Kirchhoff** target ([follow-up §R2.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r21--q1-hex-sri-for-kirchhoff-plate-5-gate)) — completion-matrix [**#2**](VERIFICATION_COMPLETION_MATRIX.md) **Exact acceptance criterion**. **Phase 1A (in-tree):** `q1_hex_elasticity` documents why a naive **\(D_{\mathrm{vol}}\)** centroid + **\(D_{\mathrm{dev}}\)** \(2^3\) **additive stress** split was **not** merged (slender-column **`adjoint_q1_hex_matches_bar_in_limit`** regression); the shipped kernel remains **unified \(\mathbf D\)** on the B-bar / centroid-shear strain operator. **`plate_centre_deflection_kirchhoff_ssss_q1_hex_within_five_percent`** (**32²×4**, **h/L=0.02**) is **`#[ignore]`** with measured **rel_err≈1** on the current brick BCs (see mechanics table row). **`plate_centre_deflection_kirchhoff_ratio_q1_hex_locked_band`** today pins a **narrowed** \(w/w_K\) interval on the **mismatched-BC** brick — a **regression** harness only, **not** §R2.1 closure and **not** a within-5% accuracy claim (**orthogonal** to matrix **#2** acceptance, per that row’s **Goal** text). **`#[ignore]` `plate_r21_kirchhoff_ssss_centre_w_within_5pct_brick_path_gate`** documents the **within-5%** centre-deflection gate for when BCs align (see mechanics table row).
3. **Fracture:** Γ-limit with driven **ψ⁺**; broader **(l₀,h)** schedules; within-step THMC **u↔d** stagger; embedding / `matrix_features` when no bar solve — memo §2.3 / §7.
4. **Acoustics (closed in-tree):** return-map **n=128** gate — **`plane_wave_return_map_n128_l2_within_two_percent`** on default CI; period **\(T=2\pi/\Omega\)** with lumped \(m=\rho\Delta x\) (see **Solver lanes — Acoustics**). Prior slip (~1.4 rel \(L^2\)) was a **mis-timed period** (FD \(\omega_{\mathrm{disp}}\) vs lumped \(\Omega\)), not substep starvation.
5. **Electrochemistry:** **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** and **`debye_screening_256_cells_phi_100mv_decay_length_within_band`** now run on default **`pnp_debye_layer`** with **`electrochemistry-pnp`** (Poisson **`h^2`** interior RHS + **`mesh_spacing = h`** + tail LS window; **±10 %** / **±15 %** vs continuum **`λ_D`**). **`debye_implicit_dispatch_short_horizon_smoke`** + **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** remain complementary dispatch / residual smokes. **Shipped increment:** Fickian-linearised implicit Newton (`linearize_sg_fickian: true`) uses **\(O(N)\)** **three-Thomas** corrections (unit test parity vs dense Jacobian on the same linear system). **Full-SG chain lane:** band FD Jacobian assembled in a fixed row band; each Newton correction tries **row-band partial-pivot LU** (`solve_newton_correction_full_sg_row_band_band_lu_or_dense_expand`) and falls back to **dense expand + Gaussian elimination** on a **\((3N)^2\)** scratch (**\(O((3N)^3)\)**) when the static LU slice fails — shipped in **`try_solve_pnp_backward_euler_newton_chain`**. **Lib regression:** **`full_sg_newton_dense_expand_matches_direct_gaussian_multi_n`** asserts dense-expand **δ** matches **direct Gaussian elimination** on the expanded matrix at **N ∈ {17,33,49,65,81}**. Experimental **row-band partial-pivot band LU** (**\(O(dim\cdot bw^2)\)**) is **not** wired into **`try_solve`** — it does **not** yet reproduce dense full-column pivot on the expanded Jacobian at fixed envelope width (**`#[ignore]` N=256** harness prints **`max|δ_lu−δ_de|`**) — see **Solver lanes — Electrochemistry**. **Still open:** band LU / skyline parity **or** adopt matrix-free / Krylov and trim the **\((3N)^2\)** dense scratch at large **N**; **general-graph** implicit Newton; variable ε on **non-chain** graphs (experimental non-chain Poisson uses **Jacobi–PCG** on uniform \(\mathcal{L}\) — **`poisson_graph_pcg_four_node_star_reduces_laplacian_residual`**); large-|zΔφ| f32 robustness ([follow-up §R2.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r23--pnp-λ_d-quasi-steady-gates-un-ignored) — narrative; λ_D LS gates are no longer open).
6. **Photonics:** Production 2D/3D DEC vector curl–curl; tensor permittivity; tighter Fresnel calibration ([follow-up §R3.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r32--dec-vector-curl-curl-on-2d3d-patches)).
7. **Rheology:** MAC-style staggered solve and/or consistent **open** \(x\) BCs so long-run developed flow matches plane Poiseuille — graph **Jacobi-PCG** on \(-\mathcal{L}\) is shipped for the Chorin pressure step (`rheology_flow.rs`). **`f02120d`:** hardened Jacobi-PCG pressure; **`chorin_steady_channel_64x16_vs_regularized_reference`** and **`chorin_developed_channel_centreline_vs_regularized_reference`** run on **`research-stack`** (**`rheology-bingham`**) **without `#[ignore]`** as **short-horizon** **65×17** smokes (finite / bracket semantics — **not** long-run steady **L²** vs Poiseuille). **`chorin_jacobi_pcg_step_velocity_amplification_regression_guard`** (historical name) remains a two-step amplification bracket on **65×17** ([follow-up §R2.2](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r22--rheology-chorin-64--16--fix-surrogate-poisson)).
8. **THMC:** Production-scale monolithic Newton–Krylov / JFNK on large graphs; optional within-step **u↔d** stagger; adaptive **`dt`** on stacked residual ([follow-up §R3.1](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r31--monolithic-thmc-newton-on-t-h-u-c)). *(**`3e93a0a`:** small-\(N\) dense Phase 5 path in **`ThmcSolver::step` → `step_experimental`** behind **`monolithic_thmc_newton: Some(ThmcMonolithicNewtonConfig { .. })`** — see THMC table row.)*
9. **Statistical mechanics:** Virial / coexistence route into **`upscale_potentials`** ([follow-up §R3.3](../../composer_prompts/v0.4_phase_3_followup_for_composer.md#r33--lj-virial-bridge-inside-upscale_potentials)).
10. **Mechanics (transient solids & contact):** Shipped mechanics is quasi-static **`VectorMechanicsSolver::solve_equilibrium`** on the bar-network 1-skeleton — **not** a CI-unified **M ü + C u̇ + Ku = f** stack on general 3D solid DOFs. The **`acoustics-newmark`** 1-D periodic bar Newmark lane (`tests/verification/acoustics_plane_wave.rs`) exercises a **scalar** bar-wave semi-discrete operator, **not** vector 3D solid transient elasticity on the mechanics graph. **Contact / collision / friction:** no contact constraints or inter-body Coulomb friction in the shipped stack — see contact mechanics scope.

#### Matrix PR slices — item **#10** (scope lock)

Per [`VERIFICATION_COMPLETION_MATRIX.md`](VERIFICATION_COMPLETION_MATRIX.md) row **#10** *Next concrete PR slice*:

- **Slice A (docs / honesty):** README §solver maturity + this row — tie **`acoustics-newmark`** to the **scalar 1-D periodic bar** lane only; do **not** conflate with **vector** 3D solid transient elasticity on the mechanics graph. There is still **no** default-CI **2-DOF vector** transient mechanics-graph harness beyond that scalar lane. Quasi-static bar/plate mechanics stays under **`VectorMechanicsSolver`** until a separate transient-vector verification story lands.
- **Slice B (engineering / open roadmap item):** **Contact** (constraints, collision, Coulomb friction) remains **out of scope** for the current matrix milestone — see contact mechanics scope. Any future contact work ships with its own **`tests/verification/*`** row and matrix row update.

#### Striatus-scale **int**egration — THMC → mechanics strain → fracture (**micro**)

**Forward:** `thmc_step_matrix_features_strain_feeds_fracture_without_si_embedding` (`thmc_drying_shrinkage.rs`) runs **`ThmcSolver::step`** → **`PhaseFieldFractureSolver::update_damage`** using the **`matrix_features`** strain stub (no **`[N,3]`** SI bar path). **`bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics`** checks kinematic parity between bar-network \(\varepsilon(\mathbf u)\) and the post-mechanics fracture helper on a tiny SI chain. **`striatus_micro_thmc_matrix_stub_fracture_max_damage_central_fd_wrt_exx`** (same file) applies a **central** finite difference to max post-step **`damage`** w.r.t. the uniform tensile stub in **`matrix_features[..,0,0,0]`** and checks loose agreement with a **backward** difference — a smoothness sanity on the stub path, **not** autodiff through the full step.

**SI split `ThmcSolver::step` + loaded-bar tensile \(\varepsilon(\mathbf u)\) in the same step:** the mechanics sub-step uses **`body_force = 0`** on the default split path (`src/physics/solvers/thmc.rs` — bar equilibrium for SI embedding). Without a cartridge / config channel for nonzero **`bf`**, **end-to-end** THMC transport → **loaded** bar equilibrium → fracture strain in **one** `step` is **not** a claimed CI surface (nonzero **`u`** on the SI path today comes from shrinkage / monolith wiring, not generic tip loads).

**Finite backward** through full **`ThmcSolver::step`** into fracture: **not shipped** — dense Newton / equilibrium paths avoid autodiff-unsafe control flow; **`_implicit_step`** remains a placeholder rustdoc hook (`thmc.rs`).

Lower-priority stretch items from the follow-up brief (stacked THMC beyond split ship, DEC Maxwell breadth, LJ bridge wiring, within-step stagger) appear above where they touch default CI or ship artefacts.


## Solver lanes — THMC

<span id="solver-lane-thmc"></span>

**Lane completion:** **75** ([`#8`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

| | **Shipped:** `ThmcImplicitTAlphaNewtonConfig` **or** small-DOF **`ThmcMonolithicNewtonConfig`** (**`3e93a0a`**, `step` → `step_experimental`) | **Still open (memo 13 / §R3.1):** large-\(N\) monolith, sparse or matrix-free Jacobian, adaptive **`dt`**, within-step **u↔d** |
| --- | --- | --- |
| **Implicit unknowns** | **Split:** \(T^{n+1},\alpha^{n+1}\) only (backward Euler + dense FD Jacobian on \(\le 64\) DOFs, batch 1). **Monolith (small graph):** stacked \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) when **`monolithic_thmc_newton: Some(_)`** and geometry/drying guards pass — same dense cap. | At production \(N\): \(T,h,\alpha,u,\ldots\) jointly in one \(R(U^{n+1})=0\) with consistent blocks / Krylov–JFNK (beyond dense \(\le 64\) harness) |
| **Fracture strain (SI bar)** | Post-\(u\) symmetric \(\varepsilon\) via **`strain_tensor_from_bar_network_displacement`** when `node_positions` is `[N,3]` and the BC mask allows bar equilibrium — same kinematics as **`strain_tensor_for_fracture_after_mechanics`** after one solve; CI: `bar_network_strain_matches_strain_tensor_for_fracture_after_mechanics` in `thmc_drying_shrinkage.rs` | **Within-step** \(u\!\leftrightarrow\!d\) stagger (mechanics still uses `damage` snapshot at step entry; single end-of-step `update_damage`); non-embedding / `matrix_features` fallback |
| **CI boundary** | Assembler: `thmc_t_alpha_newton_residual_preserves_hydro_mechanics_fields` (trial `h`,`u` untouched by Newton). Solver: `thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split` (transport does not branch on implicit \(T,\alpha\)). Monolith: `thmc_step_monolithic_newton_*`, `thmc_monolithic_newton_residual_tol_early_exit_truncates_norm_trail`, `thmc_monolithic_newton_relative_to_initial_early_exit_truncates_norm_trail`, `thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes`. **Phase-4 stacked-chain harness:** `monolithic_thmc_newton_stacked_norm_monotone_decrease_on_five_node_chain` in `tests/verification/thmc_monolithic_newton_chain.rs` (solver-research; PR **`phase4-verification-pr`**). | At scale: adaptive `dt`; matrix-free / AD-safe Jacobian follow-ups beyond the small-\(N\) dense Newton harness |

**Shipped split vs monolithic (summary):** `ThmcImplicitTAlphaNewtonConfig` (when `Some`) runs **damped Newton** only on the backward-Euler **\((T,\alpha)\)** block via `ThmcImplicitEulerThermalHydrationResidual` (dense FD Jacobian, \(\le 64\) stacked DOFs, batch 1). **`ThmcMonolithicNewtonConfig`** (when `Some`) replaces **each outer-pass** operator-split sequence \((T,\alpha)\to h\to u\) with **`damped_newton_iterations_with_quasi_static_r_u`** on \((T,h,\alpha,\mathbf u)\) when SI **`[N,3]`** embedding holds, \(\mathrm{DOF}_\mathrm{stack}\le 64\), **`drying_last_node_evaporation_k == 0`**, and `implicit_t_alpha_newton` is **`None`** (mutually exclusive). **`ThmcSolver::step` / `step_experimental` substep order when monolithic is off (each outer pass, then once per step):** \((T,\alpha)\) → humidity \(h\) (Laplacian + drying) → bar equilibrium \(u\) if SI embedding allows → repeat `max_newton` times for split residuals; **`update_damage`** runs **once** after the outer loop with post-\(u\) strain on the SI path. Humidity does **not** use the implicit \((T,\alpha)\) branch (CI: `thmc_step_implicit_t_alpha_newton_same_humidity_as_explicit_split`). The table rows above are the verified boundary vs track‑13 scope. **True monolithic** track‑13 work at **production scale** remains **open:** Krylov–JFNK, adaptive `dt`, and consistent sparse Jacobians for large \(N\). **Partial milestones already in CI:** `ThmcHydrationKinetics` calibration surface; `thmc_hydration_alpha_rate_scalar_matches_closed_form`; `thmc_implicit_euler_t_alpha_residual_matches_brute_force_two_nodes`; damped Newton lowers \(\|R\|_2\) on the \((T,\alpha)\) block (`thmc_implicit_euler_t_alpha_one_newton_lowers_residual_norm`, `thmc_implicit_euler_t_alpha_multi_newton_monotone_residual_norm_decrease`); linearized thermal Newton vs analytic decay in the same file; **Phase 1 toward monolith:** placeholder \(R_u\) tail + `evaluate_quasi_static_r_u` (`thmc_implicit_euler_t_h_alpha_u_placeholder_r_u_and_flat_layout_two_nodes`, `thmc_r_u_zero_at_solved_equilibrium_two_node_chain`); **Phase 3:** `thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes` — see `docs/research/v0.4_track13_monolithic_thmc_mechanics_coupling_plan`. **Phase 3 (verification harness):** dense damped Newton on \((T,h,\alpha,\mathbf u)\) with quasi-static \(R_u\) — `thmc_monolithic_t_h_alpha_u_newton_lowers_stacked_norm_two_nodes`. **Phase 5 (integration, `3e93a0a`):** same stacked Newton **inside** [`ThmcSolver::step`](../src/physics/solvers/thmc.rs) → **`step_experimental`** behind **`monolithic_thmc_newton`** — `thmc_step_monolithic_newton_matches_standalone_dense_newton_two_nodes`, mutual-exclusion / drying guards, **`thmc_step_monolithic_implicit_lowers_coupled_be_residual_norm_vs_split_two_nodes`** (coupled BE \(\|R\|_2\) vs operator split on the 2-node harness), **`ThmcSolver::step_monolithic_implicit`**. **Still open after Phase 5 (at scale / beyond small-DOF harness):** linearized thermal block vs analytic mode under the **full** implicit map once assembled at scale; research memo 13 routes detail to this section + `docs/research/v0.4_track13_monolithic_newton_thmc` + coupling plan link. **Shipped (small graph):** monolithic path wires **`ThmcMonolithicNewtonConfig::stacked_residual_l2_tolerance`** / **`stacked_residual_relative_to_initial`** into `damped_newton_iterations_with_quasi_static_r_u` (stacked \(\|R\|_2\) absolute / optional relative-to-initial gates; host reads; see [`thmc.rs`](../src/physics/solvers/thmc.rs)); CI covers absolute + relative early-exit assembler tests and solver parity tail on `thmc_step_monolithic_newton_matches_standalone_dense_newton_two_nodes`. **Dense-cap guard:** when field-major stacked unknowns exceed **64**, `ThmcSolver::step` returns `Err` starting with `ThmcSolver::step: monolithic_thmc_newton stacked DOFs > 64` (same cap as implicit \((T,\alpha)\) Newton).


## Solver lanes — Statistical mechanics

<span id="solver-lane-statistical-mechanics"></span>

**Lane completion:** **25** ([`#9`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

**Closed in this PR (partial):** `statmech_lj_bridge_contract` (`lj_bridge_scaling_non_regression`) on \(K \propto \varepsilon/\sigma^3\), \(\gamma \propto \varepsilon/\sigma^2\) (**`[B,2]`**); **Johnson–Zollweg–Gubbins (1993)** LJ EOS reference grid in `statmech_lj_johnson_eos_reference` / `lj_johnson_1993_reference` (including explicit check that **`[B,2]`** `upscale_potentials` **does not** match Johnson-derived bulk modulus — placeholder honesty); **`[B,4]`** `upscale_potentials` matches Johnson **`K_T`** at explicit \((\rho^*,T^*)\) (`upscale_potentials_b4_matches_physical_bulk_modulus_johnson1993`, in-crate batch test); **scalar Johnson bridge helpers** `physical_bulk_modulus_johnson1993` / `relative_placeholder_bulk_modulus_gap_vs_johnson1993` on `statistical_mechanics` (integration `physical_bulk_modulus_johnson1993_statmech_bridge_matches_lj_reference`); optional feature **`statistical-mechanics-johnson-reference`** exposes **`bulk_modulus_from_lj_state_johnson1993`** for reduced-\(K^*\) parity (see unit test in-module).

**Path sketch — Johnson / virial → `upscale_potentials` (target wiring, not shipped in the Burn tensor):**

| Stage | What is evaluated | State / parameters |
| --- | --- | --- |
| **Virial / MD** | \(P = \rho k_B T - \frac{1}{3V}\langle\sum_{i<j}\mathbf r_{ij}\cdot\mathbf F_{ij}\rangle\); then \(K_T=\rho(\partial P/\partial\rho)_T\) at matched protocol (cutoff, tail) | \(\rho, T\) plus potential parameters |
| **Johnson (1993) reference** | MBWR-style reduced EOS: \(P^*(\rho^*,T^*)\), \(K^*=\rho^*(\partial P^*/\partial\rho^*)_{T^*}\) in LJ reduced units (\(T^*=k_BT/\varepsilon\), \(\rho^*=\rho\sigma^3\)) | \((\rho^*,T^*)\) only in the analytic surface |
| **Physical \(K_T\)** | \(K_T = (\varepsilon/\sigma^3)\,K^*\) — [`lj_johnson_1993_reference::bulk_modulus_from_reduced`](../../src/physics/solvers/lj_johnson_1993_reference.rs) | joins \((\rho^*,T^*)\) with \((\varepsilon,\sigma)\) |
| **`upscale_potentials` (`[B,4]` today)** | Johnson **`K_T`** per row via **Burn column slices** (`[…,0..1]` … `[…,3..4]`) into a host **`f64`** loop, **`f32`** tensor; \(\gamma_{\mathrm{gc}}\propto \varepsilon/\sigma^2\) placeholder | columns \((\varepsilon,\sigma,\rho^*,T^*)\) |
| **`upscale_potentials` (`[B,2]` today)** | Placeholder \(K \propto \varepsilon/\sigma^3\), \(\gamma_{\mathrm{gc}}\propto \varepsilon/\sigma^2\) | **columns 0–1:** \((\varepsilon,\sigma)\) only — **no** \(\rho^*,T^*\) for **`K`** |

**Tensor channel design (future API):** **`[B,4]`** wide rows are **shipped** for Johnson **`K`**. A parallel **`[B,2]`** state tensor \((\rho^*,T^*)\) on a dedicated `StatisticalBridge` method remains a valid alternative for callers who want to keep LJ knobs separate. A **differentiable** Burn path (tape through **`K`**) would broadcast state into a native EOS map — still open.

**Feature gate sketch:** **`[B,4]`** Johnson **`K`** is always compiled. **`statistical-mechanics-johnson-reference`:** adds scalar **`bulk_modulus_from_lj_state_johnson1993`** re-export for **`f64`** reduced-\(K^*\) parity checks. **`[B,4]`** **`K`** is **not** AD-safe (host materialise).

**Still open — physical bridge:** virial / coexistence / Kirkwood–Buff route for **`γ_gc`**; MD-backed **`K`** cross-check at matched cutoff; AD-safe **`K`** in Burn.

**Next PR acceptance criteria:** (1) reference \(K\) from MD or independent tabulation at one state; (2) document **T** / cutoff / \(r_c\) mapping vs JZG analytic surface; (3) **`γ_gc`** from interface excess or explicit open roadmap item.


## Solver lanes — Fracture

<span id="solver-lane-fracture"></span>

**Lane completion:** **50** ([`#3`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

**Verified under `fracture-at2` (cumulative, see table row):** multi–length-scale **admissibility** smoke (`at2_length_scale_sweep_non_regression`); **`at2_gc_linear_scaling_smoke`** (\(G_c\) doubling at fixed \((l,\varepsilon)\), \(\bar d\) / \(G_c/l\cdot\bar d\) loose tracking — explicit sweep, not sharp-interface Γ-limit); **`at2_gamma_convergence_three_length_scales`** — three \((l_0,h)\) pairs with \(h/l_0=\tfrac14\), \(\psi^+\!=0\), exponential damage seed; discrete \(D_h\) within **&lt; 2%** of **`Gc`** and non-worsening under refinement (`tests/verification/fracture_gamma_convergence.rs`); **`at2_gamma_convergence_multi_ratio_schedule_smoke`** (§7.3) and **`at2_gamma_convergence_multi_ratio_psi_plus_schedule_smoke`** (§7.3.1 — same \(\rho\) schedule with uniform tensile \(\psi^+\) drive); **`at2_gamma_convergence_psi_plus_nonzero_three_length_scales`** (§7.2). **§7.4 outer stopping** — `update_damage_staggered_with_outer_cfg` / `StaggeredFractureConfig::outer_stopping` (damage ℓ∞, strain ℓ∞, relative batch–node mean \((1-d)^2\psi^+\); **OR** when several tolerances are set): `at2_staggered_outer_cfg_fixed_iters_matches_legacy`, `at2_staggered_outer_loose_damage_linf_one_pass`, `at2_staggered_outer_rel_psi_loose_two_passes`, `at2_solve_staggered_mechanics_outer_loose_stopping_one_pass`, `staggered_mechanics_outer_damage_stop_matches_long_budget` in the same harness.

**Latest (harness hygiene only):** **`at2_discrete_surface_functional_toy_chain_matches_hand_total`** exercises **`discrete_at2_bar_surface_energy_1d`**, the same closed-form \(D_h\) sum used above (3-node hand total — **does not** extend Γ claims or \(\psi^+\) coverage). **Research:** explicit acceptance + proposed **`#[test]`** names for driven \(\psi^+\), multi-ratio \((l_0,h)\), staggered \(D_h\) vs \(G_c\), and THMC within-step stagger — track 12 memo §7.

**Still open:** Γ-limit / \(D_h\) certificates with **nonzero tensile driving** \(\psi^+\) and mechanics-coupled refinement; broader \((l_0,h)\) schedules than the fixed \(h/l_0\) triple; **within-step** THMC stagger so mechanics and fracture can alternate on updated \(d\) inside one `step` (today `damage_m` for transport/mechanics is fixed at step entry; fracture runs once after outer passes); dissipation vs \(G_c\) on refined meshes under staggered \(u\!\leftrightarrow\!d\) beyond compliance monotonicity.

**Next PR acceptance criteria:** (1) extend Γ-type or dissipation harness with \(\psi^+\!\neq 0\) and/or staggered bar loading; (2) **THMC SI path:** fresh \(\varepsilon(\mathbf u)\) after bar equilibrium **met** (above); optional **within-step** \(u\!\leftrightarrow\!d\) iterations or documented single-pass contract; (3) energy monotone band on benchmark chain.


## Solver lanes — Photonics

<span id="solver-lane-photonics"></span>

**Lane completion:** **50** ([`#6`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

**Closed in this PR (partial):** uniform x-chain **TE \(E_y\)** curl–curl reduction equals scalar Helmholtz (`curl_curl_y_mode_matches_scalar_helmholtz`); same operator identity under **piecewise** node \(\varepsilon_r\) (`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps`); **piecewise nodal `ε` with tensor `yy` coupling** on the chain (`curl_curl_y_mode_matches_scalar_helmholtz_piecewise_eps_tensor_yy`); **affine \(x\)-metric** sanity on the embedded chain (`curl_curl_y_mode_matches_scalar_helmholtz_affine_x_metric_preserves_ex_ez`); Burn DEC primal matvec matches hand TE stencil (`dec_te_primal_tensor_matches_chain_stencil`, `dec_te_primal_piecewise_eps_matches_chain_stencil`); **analytic transfer-matrix** gate **`quarter_wave_stack_n10_reflectivity_above_0p95`** (\(n_H{=}2\), \(n_L{=}1\), 10 bilayers, \(|r|^2>0.95\) at design frequency); **default builds** (`photonics` **off**): `photonics_curl_curl_stub_default_build` locks [`PhotonicsSolver::solve_maxwell_curl_curl`] as a documented **no-op** (identity on representative chain tensors — not a physics solve).

**Closed partial — vector DEC on small embedded 2-surface patches (`PhotonicsDecFacesPatch` → [`solve_maxwell_curl_curl`](../src/physics/solvers/photonics.rs)):** PR **`phase4-verification-pr`** runs **`solve_maxwell_uniform_brick_two_quad_strip_residual`** / **`solve_maxwell_uniform_brick_tet_boundary_residual`** (`photonics_curl_curl_{2d_patch,3d_brick}.rs`, **`photonics-fdfd`**); host **dense** real **batch 1** systems on hand-built **`faces_b2`** / SI **`coords_n3`** (quad split, two-quads strip, **ℝ³** placement): **`solve_maxwell_dec_patch_quad_split_pin_residual_tight`**, **`solve_maxwell_dec_patch_quad_split_tensor_eps_residual`**, **`solve_maxwell_dec_patch_quad_split_embedded_r3_residual`**, **`solve_maxwell_dec_patch_two_quads_strip_residual`**; Burn Maxwell-assembly annihilation / adjoint checks on the same patch tensors (`dec_maxwell_assembly_quad_split_d1_annihilates_gradient_burn`, `dec_maxwell_assembly_quad_split_d1_adjoint_identity_burn`); **non-chain** guardrails (`apply_dec_te_curl_curl_chain_operator_none_on_quad_split_expanded_patch`, `solve_maxwell_curl_curl_pass_through_quad_split_not_chain`); embedded monotone chain in **ℝ³** (`curl_curl_y_mode_matches_scalar_helmholtz_xy_embedded_chain`). Operator uses **unweighted** primal **\(d_1^\top d_1\)** on edge tangents + nodal **\(k_0^2 \varepsilon\)** with **full `3×3` permittivity** where exercised — **not** circumdual **Hodge** metrics, **not** sparse Krylov, **`eps_r_imag`≈0** on the patch path.

**Still open:** **automatic** incidence + metrics from **full** mesh / graph assembly (vs test-authored **COO**); **3D volumetric** simplicial or hex complexes (beyond embedded **2D** sheets); **dual Hodge** / edge–face metric weights in the DEC operator; **sparse** inner solves at production **N**; **complex ε** and **PML** on the patch path; physical **BCs** beyond a gauge **pin**; tighter Fresnel field match vs a **discrete** semi-infinite reference (current CI harness uses a bridged continuum target and a relaxed LS band).

**Closed partial — discrete DEC triangle (not photonics solve path):** [`tests/dec_identities.rs`](../tests/dec_identities.rs) ships **`dec_curl_d1_annihilates_gradient_on_triangle_faces_b2_burn`** (\(d_1(d_0\omega)=0\) through synthetic **`edges_b1`/`faces_b2`** COO tensors and Burn gather/scatter in [`dec_primal`](../src/physics/dec_primal.rs)) and **`dec_primal_d1_adjoint_identity_single_triangle_burn`** (unweighted \(d_1\) / \(d_1^\top\) Frobenius adjoint on the same face **COO**). **`dec_curl_d1_annihilates_gradient_on_triangle`** is the dense **`ring_b1`** matmul companion on the **same** CCW 3-cycle (hand-coded face sum); it is not the only annihilation check. **Incremental toward 2D patch curl–curl:** **`dec_curl_d1_annihilates_gradient_quad_split_two_faces_burn`** and **`dec_primal_d1_adjoint_identity_quad_split_two_faces_burn`** — a **4-node / 5-edge** quad split into two CCW triangles sharing diagonal **0–2**, so one global edge carries **two signed `faces_b2` columns** (curl annihilation + adjoint scatter on the shared edge). None of these call [`PhotonicsSolver::solve_maxwell_curl_curl`](../src/physics/solvers/photonics.rs) or lift incidence from a full mesh assembly.

**Next PR acceptance criteria:** (1) tighten `two_half_spaces_fresnel_te_no_pml_matches_analytic` LS margin and/or add discrete `r_disc`-only comparison; (2) **metric / dual-Hodge** weights and/or **cube** topology with **`faces_b2` (and higher)** populated from **assembled graph / manifold state**, wired into **`solve_maxwell_curl_curl`** — today’s **small-patch** path is **unweighted** **\(d_1^\top d_1\)** on test-authored **COO**; (3) **sparse** inner solve + **complex ε** / **PML** on the patch path, BCs beyond gauge **pin** — per matrix [**#6**](VERIFICATION_COMPLETION_MATRIX.md) **Next PR** slice.


## Solver lanes — Acoustics

<span id="solver-lane-acoustics"></span>

**Lane completion:** **100** ([`#4`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

**Closed in this PR (partial):** implicit Newmark on a 1-D periodic bar — `newmark_acceleration_matches_dense_reference_n8` / `_n128` vs dense reference (inner linear solve uses **f64** Cholesky; f32 factorization diverges at large `n`); **`plane_wave_return_map_n64_l2_within_two_percent`**, **`plane_wave_return_map_n100_l2_within_two_percent`**, **`plane_wave_return_map_n128_l2_within_two_percent`** (same CFL-scaled substep recipe; **return-map period** \(T=2\pi/\Omega\) with lumped mass \(m=\rho\Delta x\) — not FD dispersion \(\omega_{\mathrm{disp}}=(2c/\Delta x)|\sin(k\Delta x/2)|\) alone); `plane_wave_h_refinement_second_order_band` (analytic snapshot uses \(\omega_{\mathrm{disp}}\) for spatial \(h^2\) band vs continuum target); `undamped_energy_drift_under_half_percent_over_1000_steps` at **n=128** (energy proxy, not the sole return-map certificate).

**Still open:** graph-assembled acoustic stiffness beyond nodal `AcousticWaveSolver` contraction; optional DEC coupling milestones beyond this scalar periodic bar lane.

**Root-cause note (historical §R2.4 slip ~1.4):** the return-map harness used \(T=2\pi/\omega_{\mathrm{disp}}\) while the assembled evolution is \(m\ddot u+Ku=0\) with \(m=\rho\Delta x\). The correct modal frequency satisfies \(\Omega^2=\lambda_K/m=\omega_{\mathrm{disp}}^2/\Delta x\) on a uniform periodic chain, i.e. \(\Omega=\omega_{\mathrm{disp}}/\sqrt{\Delta x}\). Mis-measured \(T\) integrated across \(\mathcal O(\sqrt{n})\) too many temporal cycles at large \(n\), producing order-one relative \(L^2\) vs `u₀` — **not** fixed by substep refinement alone. Tests in `acoustics_plane_wave.rs` (`semi_discrete_omega`, `dispersion_omega_fd`) encode the split; rustdoc on `AcousticNewmarkBar1dPeriodic` summarizes.

**Warning — what CI does *not* certify:** `undamped_energy_drift_under_half_percent_over_1000_steps` (n=128) bounds the **discrete mechanical-energy proxy** over many substeps; use it alongside **`plane_wave_return_map_*`** gates for elastic wave fidelity claims.

**Next PR acceptance criteria:** (1) optional graph-based acoustic assembly exercising `AcousticWaveSolver` + DEC hooks beyond this 1-D periodic benchmark; (2) tighter Fresnel-style coupling checks if acoustics connects to photonics/mechanics lanes.


## Solver lanes — Electrochemistry

<span id="solver-lane-electrochemistry"></span>

**Lane completion:** **75** ([`#5`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

**Closed in this PR (partial):** Poisson–NP **explicit Picard** coupling in `solve_pnp_step_experimental` (`coupling_picard_iters`, fixed \(c^n\) per timestep); MVP path-chain Thomas Poisson with **harmonic edge \(\varepsilon_{i+1/2}\)** and net charge RHS (**`\nabla\cdot(\varepsilon\nabla\phi)=-(c^+-c^-)F`**, consistent uniform-\(\varepsilon\) reduction); SG flux **`mesh_spacing`** scaling (**`J\propto D/h`**; Poisson stencil is **unit-graph** in index space — not rescaled by `mesh_spacing`; see `electrochemistry` rustdoc); **tensor SG Bernoulli** evaluated elementwise via **`f64`** exponentials for large \(|z\Delta\phi|\); **MVP-chain implicit backward Euler** + damped Newton (`try_solve_pnp_backward_euler_newton_chain`, host `f64`) with residual regression tests in `pnp_debye_layer.rs`. **Wiring note:** implicit Newton is **not** merged into `solve_pnp_step_experimental`; the default `solve_pnp_step` path remains explicit Picard-only. Research users can opt in via **`pnp_implicit_newton_chain`** + **`solve_pnp_step_dispatch`**. **λ_D admissibility harness** (`debye_screening_admissibility_check`) sets **`mesh_spacing = h = L/(N-1)`** so SG matches the physical fit abscissa, uses **`debye_implicit_newton_linearized_sg_for_lambda_d_gate()`** (`linearize_sg_fickian: true`) for analytic Jacobian / tractable **`--release`** runs on **`N=256`**, and drives **`debye_screening_256_cells_phi_25mv_decay_length_within_band`** / **`…_phi_100mv…`** (interior LS **`λ_eff`** vs continuum **`λ_D`**, **±5 %** / **±15 %**). **CI-fast dispatch smokes:** `debye_implicit_dispatch_short_horizon_smoke` … **`debye_dispatch_newton_backward_euler_residual_bounded_over_screening_trajectory_smoke`** … keep default **`mesh_spacing = 1`** on the residual trajectory — orthogonal to the **`mesh_spacing = h`** λ_D LS gates.

**Still open:** coupled implicit Newton on **general graphs**; variable \(\varepsilon\) on **non-chain** graphs (experimental non-chain Poisson uses **Jacobi–PCG** on the **uniform** topology Laplacian — lib **`poisson_graph_pcg_four_node_star_reduces_laplacian_residual`**, not a variable-\(\varepsilon\) DEC stencil); **contiguous chain** full-SG Newton now **tries band LU first** with **dense-expand fallback**, but the **`(3N)^2`** fallback scratch is still allocated at **N=256** when the static band envelope is narrower than **`dim−1`** — eliminating that worst-case path (adaptive envelope, **\(O(dim\cdot bw)\)** factorisation-only workspace, or matrix-free Krylov defaults) at production **N** remains open. **Fickian-linearised** path uses **three Thomas solves / Newton iter** instead of dense \((3N)^2\) factorisation. Lib parity (electrochemistry MVP unit tests): **`full_sg_newton_band_expand_dense_matches_dense_column_fd_reference`** (band FD Jacobian + dense-expand \(\delta\) vs all-dense column FD on **N=17** — matches **`try_solve`** inner solve); **`full_sg_newton_dense_expand_matches_direct_gaussian_multi_n`** (dense-expand \(\delta\) vs direct Gaussian on the expanded matrix at **N ∈ {17,33,49,65,81}**). **Manual N=256** wall-clock / diagnostic (**`#[ignore]`**): `full_sg_chain_n256_band_lu_vs_dense_expand_wall_clock_and_residual_parity` — prints band-LU vs dense-expand timings and `max|δ_lu-δ_de|`; `cargo test -p umst-manifold --features electrochemistry-pnp,solver-experimental full_sg_chain_n256_band_lu_vs_dense_expand_wall_clock_and_residual_parity --release -- --ignored --nocapture`.

**Next PR acceptance criteria:** (1) residual \(\|R_\Phi\|+\|R_c\|\) decreases under damped Newton on a **non-chain** benchmark or matrix-free JVP path; (2) mass conservation band on closed graph; (3) double-precision option for large \(|z\Delta\phi|\); (4) **O(dim·bw)** scratch-only or Krylov / matrix-free inner solve for **full SG** at **`N=256`** if the retained dense workspace is unacceptable, then tighten or extend the **`debye_screening_256_cells_*`** bands under **nonlinear** SG if the linearised gates no longer bound the shipped physics.


## Solver lanes — Rheology

<span id="solver-lane-rheology"></span>

**Lane completion:** **50** ([`#7`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

**Closed in this PR (partial):** **64×16** channel graph **edge-count** scaffold (`channel_lattice_64x16_quadrilateral_edge_count`, **1968** undirected edges on the quad stencil used by `rheology_poiseuille` harnesses). Live analytics / regularized 1-D references / Chorin short smoke / \(\lambda\)–thixotropy checks run under **`--features rheology-bingham`** (see table row). **`f02120d`:** **Jacobi-preconditioned CG** on \(-\mathcal{L}\) for the Chorin pressure increment hardened (`rheology_flow.rs`: [`TopologicalLaplacian::scalar_laplacian`](../src/physics/laplacian.rs) + [`scalar_laplacian_neg_opposite_diag`](../src/physics/laplacian.rs) diagonal, mean-free RHS, relative residual exit); **`chorin_developed_channel_centreline_vs_regularized_reference`** / **`chorin_steady_channel_64x16_vs_regularized_reference`** promoted to **`research-stack`** CI (**no `#[ignore]`**) — **65×17** short-horizon smokes (80 / 100 substeps; finite ‖u‖/‖p‖, bracket vs developed regularized reference — **not** long-run steady **L²**); **`chorin_jacobi_pcg_step_velocity_amplification_regression_guard`** (historical name) + **`chorin_channel_65x17_thirty_substeps_remain_finite`**.

**Still open:** (1) **MAC / staggered** pressure or cell-centred divergence so the discrete split matches developed-channel references without graph-only artefacts. (2) **Open** \(x\) **inlet/outlet** BCs (today periodic \(x\) is only a test harness approximation). (3) **Long-run** steady \(L^2\) / plug-width vs the regularized 1-D profile on **65×17** (multi-thousand-step acceptance) — not wired as default CI yet. **Design:** research/rheology_pressure_poisson_roadmap.md.

**Warning — what CI does *not* certify:** default CI **short** channel smokes do **not** replace a full steady **\(L^2\)** certificate against plane Poiseuille; long-run acceptance remains a research goal until MAC + open BCs land. `chorin_single_step_finite_smoke` and **`channel_lattice_64x16_quadrilateral_edge_count`** alone do not validate developed physics.

**Next PR acceptance criteria:** (1) centreline \(L^2\) error vs analytic &lt; 15% at stated `dt` / step count on **64×16**; (2) pressure correction Poisson RHS aligned with discrete divergence of \(u^\*\); (3) optional env-gated long benchmark (`UMST_RHEOLOGY_POISEUILLE_BUDGET_MS`).


## Solver lanes — Topology / shell (Tracks B + L)

<span id="solver-lane-topology-shell"></span>

**Lane completion:** **25** ([`#1`](VERIFICATION_COMPLETION_MATRIX.md)) — same rubric as the main solver table.

**Related pointers:** Striatus ship checks are summarized in **Verification scope (remaining)** above and in [`VERIFICATION_SCOPE_INDEX.md`](VERIFICATION_SCOPE_INDEX.md); this section expands **Track B / Track L** artefacts and commands (**B6 / B8**).

**Source brief:** [`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md) — **Track B** (topology shell / Striatus), **Track L** (GIF + committed artefacts).

### v0.3 vs v0.4 honesty

- The brief defines **target** public filenames and thresholds for **v0.4**. A checkout may still ship **v0.3-only** names (e.g. `striatus_shell_v0.3.*`) or lack rib-pattern completion — **do not** claim “v0.4 Striatus shell complete” until the **exact** filenames below exist, meet **Track L** size/structural gates, and **Track B8** checks are satisfied (tests + `print_ready.json`).
- **CI vs brief:** default cartridge CI runs **`shell_topology_rib_pattern_quick`** (`cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental`). **Quick harness** matches **`run_rib_quick_metrics`** / **`short_mesh_and_iters`** when all `UMST_SHELL_{NX,NY,NZ,ITERS}` are unset: **9×8×2**, **24** Adam outers at **0.005** LR, **50 Pa** roof with **x**-ramp **r=0.2**, Heaviside **β=10**, **`DensityNet`** hidden width **32** — **distinct from** full B6 **post–projection** **volume**-mean greyness **< 0.15** on **`shell_topology_rib_pattern_full_v04`** (see **Track B6** table). **`optimize_shell_3d`** uses **50 Pa** and the **x** ramp only when **`UMST_SHELL_ROOF_RAMP=1`**, else **50 Pa** uniform. **Full B6** (`shell_topology_rib_pattern_full_v04`, **`#[ignore]`**): **`UMST_SHELL_RIB_PATTERN=1`**, then `cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental shell_topology_rib_pattern_full_v04 --release -- --ignored` — put **`--release` before `--`**. **40×40×4**, **200** Adam outers (optional **`UMST_SHELL_RIB_FULL_ITERS`** in **1…200** for smoke only; smoke does **not** satisfy B6 greyness / compliance gates). Multi-minute CPU per outer in `--release` is typical; budget **hours** for **200** outers. The brief’s proof stays env-gated off default CI; see **Track B6** table at the top of this file and cartridge **[`../../umst-concrete-cartridge/docs/Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md)**. **Track L doc/manifest alignment** (cartridge **`1c5ab2f`**): richer **`manifest.json`**, default **`UMST_SHELL_DUMP_STRIDE`** when dumps are on, and rustdoc overnight / symmetry notes — **`optimize_shell_3d`** rustdoc + operator bullets below.
- **B6 bar equilibrium:** `packed_bar_network_equilibrium` caps PCG at **`min(max_cg_iterations, 3N)`** and **early-exits** when \(\|P(f-Ku)\|_2 \le \max(\texttt{pcg\_tolerance},\texttt{cg\_tolerance})\,\|Pf\|_2\). The full B6 harness defaults **`max_cg_iterations = 2000`** and **`E_{\min} = 10^{-3} E_0`** (**`UMST_SHELL_MAX_CG`**, **`UMST_SHELL_E_MIN_REL`**). **`UMST_SHELL_HELM`:** default **off** on the Burn tape (same as `optimize_shell_3d`); **only** literal **`UMST_SHELL_HELM=1`** enables graph Helmholtz in-loop (an empty **`UMST_SHELL_HELM=`** assignment must **not** enable — older **`!= "0"`** parsing turned it on; scatter backward can still fail at large Striatus `N` on Burn 0.13 when enabled). **Parsing:** harnesses must **not** use a naive **`v != "0"`** toggle for Helmholtz; cartridge **`shell_topology_rib_pattern`** / **`optimize_shell_3d`** match on **`"1"`** only. Optional **`UMST_SHELL_RIB_FULL_ITERS`** (see cartridge test `//!`) shortens the ignored harness for smoke — when **`UMST_SHELL_RIB_FULL_ITERS` < 200**, the cartridge test **skips** the brief **greyness / planar-variance / compliance-ratio** gates and only checks finite metrics + a loose VF band; the full **200**-outer schedule is required for those acceptance asserts. **Ring 1 harness:** `shell_topology_rib_pattern_full_v04` reads **`UMST_SHELL_SELF_WEIGHT`** (default **off** — traction + roof; **`1`** adds gravity), **`UMST_SHELL_VOL_LOOP`**, **`UMST_SHELL_PCG`**, and **`UMST_SHELL_ROOF_RAMP`** / **`UMST_SHELL_ROOF_RAMP_F`**: **`UMST_SHELL_ROOF_RAMP=0`** forces **uniform** roof pressure (like **`optimize_shell_3d`** with ramp unset); **unset** or any value other than **`0`** / **`1`** turns the gentle **x** ramp **on** at **`UMST_SHELL_ROOF_RAMP_F`** (default **0.2**) so the Striatus-scale load matches the CI quick harness — **`optimize_shell_3d`** itself still requires literal **`UMST_SHELL_ROOF_RAMP=1`** for the same ramp. **Non-finite iter-1 raw compliance** **panics** fast; gate failures print **vf / greyness / xy_var / compliance**. Details in [`../../umst-concrete-cartridge/crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs`](../../umst-concrete-cartridge/crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs) module docs and **`#!`**.

### NaN / conditioning — `optimize_shell_3d` (40²×4)

**Where NaNs enter:** [`AdjointCompliance::forward_and_loss`](../src/physics/adjoint.rs) builds nodal stiffness from `SimpElasticMaterial`, runs inner [`VectorMechanicsSolver::packed_bar_network_equilibrium`](../src/physics/mechanics.rs) (projected **f32** PCG, capped iteration count, optional Jacobi preconditioner), then `masked_dot` → raw compliance `c_raw`. The discrete-adjoint surrogate re-lifts inner tensors with `Tensor::from_inner`; non-finite **`u`** or edge elongations **`δ_e`** poison **`c_raw`** and the surrogate. **Self-weight** body force scales with filtered density on large slabs and can drive bar PCG into **non-finite** `u` in **`f32`** (cartridge `shell_demo_smoke` uses traction-only for that reason). **Helmholtz** Richardson on the **Burn Autodiff** tape can amplify round-off on large 3-D graphs; [`HelmholtzFilter::apply`](../src/physics/topology_filter.rs) honours the constructor iteration count (it no longer forces a **400**-step minimum over a **240**-step caller). **In-loop [`VolumeProjection`]** bisection on AD can trip Burn scatter backward limits on some slab sizes; set **`UMST_SHELL_VOL_LOOP=0`** to skip it during optimisation (mean ρ can drift toward **0.5** and worsen bar conditioning on **40²×4** unless self-weight / Helmholtz / other knobs are aligned). Default in the cartridge example remains **on** (`1` or unset) so mean VF tracks **`UMST_SHELL_VF`**.

**Reproduce (cartridge example — finite with default env flags):**

```bash
cd umst-concrete-cartridge
cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render'
```

**Stress ill-conditioning (historical NaN at iter 1 — low PCG budget + tiny `E_min`):**

```bash
cd umst-concrete-cartridge
UMST_SHELL_MAX_CG=200 UMST_SHELL_E_MIN_REL=5e-9 \
  cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render'
```

**Full B6 ignored harness:**

```bash
cd umst-concrete-cartridge
UMST_SHELL_RIB_PATTERN=1 cargo test -p umst-concrete-cartridge --test shell_topology_rib_pattern --features solver-experimental shell_topology_rib_pattern_full_v04 --release -- --ignored
```

Uniform roof like **`optimize_shell_3d`** with ramp unset: prefix **`UMST_SHELL_ROOF_RAMP=0`** on the same command.

**Mitigations:** raise **`UMST_SHELL_MAX_CG`**, keep **`UMST_SHELL_E_MIN_REL≈1e-3`** (Track B6 **`10^{-3}E_0`**), keep **`UMST_SHELL_PCG=1`**, **`UMST_SHELL_SELF_WEIGHT=0`** for traction-only while **`f32`** bar PCG is hardened, **`UMST_SHELL_HELM=0`** to A/B the graph filter on AD, **`UMST_SHELL_VOL_LOOP=0`** only when debugging backward (expect mean ρ near **0.5** without other constraints); future **f64** inner equilibrium remains open.

### 2026-05-11 greyness vs roof-ramp defaults (honest)

- **What was measured:** a full **`shell_topology_rib_pattern_full_v04`** **`--release`** run (**200** outers, **40×40×4**, seed **42**) reported **greyness** `mean(4ρ(1−ρ)) ≈ 0.51` vs the B6 gate **&lt; 0.15** (**~7655 s** wall; **finite** — not an early NaN / `c_raw` blow-up). That log predates the current **B6 harness** roof convention below.
- **Two surfaces today:** **`optimize_shell_3d`** / **`_run_shell_demo.sh`** use **uniform** roof traction unless you set **`UMST_SHELL_ROOF_RAMP=1`**. The **opt-in B6 test harness** uses **`UMST_SHELL_ROOF_RAMP=0`** → uniform; **unset** or any value other than **`0`** / **`1`** → gentle **x** ramp at **`UMST_SHELL_ROOF_RAMP_F`** (default **0.2**) so CI quick matches Striatus load asymmetry — **do not** assume “unset” means the same thing across example vs test.
- **What we are not claiming:** no in-repo **200**-outer re-proof yet that greyness clears under the post-harness ramp story; bounded smokes (e.g. **15** outers) still showed **~0.51** greyness on the roof Heaviside slice while **`c1`** moved with ramp vs uniform — load asymmetry helps compliance, not the grey gate by itself in those short runs.
- **Operator takeaway:** pick roof semantics explicitly (`0` / `1` / “harness default” for B6) before comparing logs to **`export_print_ready.py`** / Track L artefacts.

### Exact artefact paths (relative to `umst-concrete-cartridge/` repo root)

| Role | Path |
| --- | --- |
| Emergence animation | `notebooks/_artifacts/striatus_emergence.gif` |
| Mesh export (v0.4 name) | `notebooks/_artifacts/striatus_shell_v0.4.stl` |
| Print-ready JSON (B7 / B8 / L); **`gates_track_b8_all_pass`** | `notebooks/_artifacts/striatus_shell_v0.4.print_ready.json` |
| Optional mesh | `notebooks/_artifacts/striatus_shell_v0.4.obj` |

**Anti-goal (brief):** do **not** commit `crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy` — only final **`striatus_emergence.gif`**, **`striatus_shell_v0.4.stl`**, and **`striatus_shell_v0.4.print_ready.json`** are intended as committed artefacts (plus optional `.obj`).

### Track L — regeneration command (verbatim from brief)

Run from the **workspace root** that contains `umst-concrete-cartridge/` (the brief’s first line `cd`s into it):

```bash
cd umst-concrete-cartridge
rm -rf crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy
rm -rf notebooks/_artifacts/frames notebooks/_artifacts/striatus_emergence.gif
UMST_SHELL_DUMP_ITER=1 UMST_SHELL_DUMP_STRIDE=10 UMST_SHELL_ITERS=200 \
    bash notebooks/_run_shell_demo.sh
```

### Copy-paste overnight — 40×40×4 × 200 outers (Track L, no `iter_*.npy`)

Single block: **`notebooks/_run_shell_demo.sh`** ends with **`notebooks/export_print_ready.py`** (same `PYTHON` as the GIF/STL steps). Rust uses **`--release`** inside the script (`cargo run --release … optimize_shell_3d`).

```bash
cd umst-concrete-cartridge
rm -f crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy

export UMST_SHELL_NX=40 UMST_SHELL_NY=40 UMST_SHELL_NZ=4 UMST_SHELL_ITERS=200
export UMST_SHELL_DUMP_ITER=0 UMST_SHELL_DUMP_STRIDE=10
export UMST_SHELL_SELF_WEIGHT=0
# Roof in optimize_shell_3d: only literal UMST_SHELL_ROOF_RAMP=1 enables x ramp (UMST_SHELL_ROOF_RAMP_F default 0.2).
# unset / omitted => uniform roof pressure (differs from B6 full harness — see “2026-05-11 greyness” below).

bash notebooks/_run_shell_demo.sh 2>&1 | tee shell_track_l_40cube4_i200.log
```

**Artefacts (relative to `umst-concrete-cartridge/`):**

| Step | Path |
| --- | --- |
| Rust manifest + density | `crates/umst-concrete-cartridge/examples/_artifacts/shell/manifest.json`, `…/final.npy` |
| GIF / STL / print-ready | `notebooks/_artifacts/striatus_emergence.gif`, `notebooks/_artifacts/striatus_shell_v0.4.stl`, `notebooks/_artifacts/striatus_shell_v0.4.print_ready.json` |

If the Python stack stops early, re-run exporter only from the same cwd: `python3 notebooks/export_print_ready.py` (needs Trimesh + graph engines — see operator notes below).

(`optimize_shell_3d` + **`manifest.json`** record **`burn_seed`: 42**, **`symmetry_xy`**, **`sym_period`**, **`dump_stride`**, **`iters`**, roof/self-weight / vol-loop flags — use the manifest beside **`final.npy`** for **ρ-span** / export forensics.)

**Rust-only overnight (GIF/STL off, from `optimize_shell_3d` rustdoc):** from **`umst-concrete-cartridge/`**, after removing stale **`iter_*.npy`**: `UMST_SHELL_NX=40 UMST_SHELL_NY=40 UMST_SHELL_NZ=4 UMST_SHELL_ITERS=200 UMST_SHELL_DUMP_ITER=0 cargo run --release -p umst-concrete-cartridge --example optimize_shell_3d --features 'solver-experimental render' 2>&1 | tee shell_opt_40cube4_i200.log` — then run **`bash notebooks/_run_shell_demo.sh`** when you need GIF/STL/JSON, or chain **`render_shell_gif.py`** → **`export_print_ready.py`** on an existing **`final.npy`**.

**Shell demo — operator notes (`_run_shell_demo.sh` + Python)** — mirror of [cartridge `Solver-Status.md` §Track L](../../umst-concrete-cartridge/docs/Solver-Status.md) (*Shell demo — operator notes*):

- **`UMST_SHELL_ITERS=5`** (or any smaller value) only shortens the Rust loop; it does **not** fix **`loss=NaN`** at iter 1 when the forward adjoint path is unstable with **self-weight** on the **40×40×4** slab.
- **`notebooks/_run_shell_demo.sh`** exports **`UMST_SHELL_SELF_WEIGHT=0`** by default (traction + roof pressure) so **`f32`** PCG + adjoint usually stays finite; override in the environment only if you accept self-weight risk.
- **`UMST_SHELL_DUMP_STRIDE`:** with **`UMST_SHELL_DUMP_ITER=1`**, if **`UMST_SHELL_DUMP_STRIDE`** is **unset**, **`optimize_shell_3d`** defaults it to **10** (first / every **N**th / last outer), matching **`notebooks/_run_shell_demo.sh`** — avoids writing one full field per outer on long runs.
- **`manifest.json`** (beside **`final.npy`**): written by **`optimize_shell_3d`** with grid / spacing, fixed **`burn_seed`: 42**, **`iters`**, **`symmetry_xy`**, **`sym_period`**, **`roof_x_ramp`**, **`self_weight`**, **`vol_in_loop`**, **`dump_iter`**, **`dump_stride`** — use it with **`final.npy`** for ρ-span / export forensics (**`1c5ab2f`** richer manifest).
- Remove stale **`iter_*.npy`** when changing grid or dump settings — otherwise **`render_shell_gif.py`** may take the iter branch and **`ValueError`** on reshape vs **`manifest.json`** (`find …/shell -name 'iter_*.npy' -delete`).
- If **`manifest.json`** and **`final.npy`** disagree (stale manifest), **`render_shell_gif.py`** raises **`final.npy size … != grid …`**; re-run **`optimize_shell_3d`** once to align.
- **`export_print_ready.py`** needs Trimesh graph engines: **`uv pip install --python .venv/bin/python networkx scipy`** (otherwise **`ImportError: no graph engines available!`**).
- **`optimize_shell_3d`** (cartridge example): default **PCG / projected PCG** residual tolerances **1e−6** (B6 full-harness parity). **XY symmetry vs dumps:** when symmetry is on, **`iter_*.npy`** frames show **in-loop** **ρ** (mirrored only on **`UMST_SHELL_SYMM_PERIOD`** steps); **`final.npy`** applies the **terminal XY reflection average** once after the last forward — the last GIF frame can differ slightly from **`final.npy`**; rely on **`final.npy`** / **`export_print_ready.py`** for Track L exports. Optional **`UMST_SHELL_ROOF_RAMP=1`** — a **bounded ρ-span smoke** recipe (**`16×16×4`**, **40** outers) and B8 pytest honesty live in **[cartridge `Solver-Status.md`](../../umst-concrete-cartridge/docs/Solver-Status.md)** (*Shell demo — operator notes* + *Honest status — Track L pytest*) and **`notebooks/_run_shell_demo.sh`** comments (full **40×40×4** + **200** outers remains the Ring‑1 B8 lattice proof).

**Track L acceptance (brief):** `notebooks/_artifacts/striatus_emergence.gif` exists, **≤ 5 MB**, **≥ 30 frames**; `notebooks/_artifacts/striatus_shell_v0.4.stl` exists, **≤ 8 MB**, watertight, **`genus ≥ 1`**, **`volume_fraction ∈ [0.10, 0.25]`**; commit GIF and STL to git.

**Print-ready / structural JSON:** after marching-cubes and gates, run **[`../../umst-concrete-cartridge/notebooks/export_print_ready.py`](../../umst-concrete-cartridge/notebooks/export_print_ready.py)** so **`striatus_shell_v0.4.print_ready.json`** includes the B7 variance/genus/volume-fraction fields; **[`../../umst-concrete-cartridge/notebooks/tests/test_print_ready.py`](../../umst-concrete-cartridge/notebooks/tests/test_print_ready.py)** asserts them.

**Full B6 (`shell_topology_rib_pattern_full_v04`, `--release`, `--ignored`, `UMST_SHELL_RIB_PATTERN=1`):** with **`UMST_SHELL_RIB_FULL_ITERS=200`** (default), gates are **greyness `< 0.15`** (**volume** mean), **`xy_var > 0.1`**, **VF ±1%**, **compliance drop `< 0.6×`** vs iter-1 — distinct from NaN / low-PCG-budget path in **`optimize_shell_3d`** stress note above. **2026-05-11 run + roof semantics:** consolidated in **[2026-05-11 greyness vs roof-ramp defaults (honest)](#2026-05-11-greyness-vs-roof-ramp-defaults-honest)**. With **`UMST_SHELL_RIB_FULL_ITERS` below 200** (smoke), the same test only checks **finite** compliance / metrics and a **loose VF band** (±15%); smoke **`--nocapture`** prints **vf / greyness / xy_var / c0 / c1**. Default CI **`shell_topology_rib_pattern_quick`** uses the **Heaviside** quick gates in the **Track B6** table (slice variance, finite Heaviside greyness, compliance ratio), **not** this **volume** **< 0.15** gate.

### Track B — shell acceptance (exact names)

| Item | Location |
| --- | --- |
| Optimiser example | `umst-concrete-cartridge/crates/umst-concrete-cartridge/examples/optimize_shell_3d.rs` |
| Smoke / determinism | `crates/umst-concrete-cartridge/tests/shell_demo_smoke.rs` |
| Rib-pattern test (B6) | `crates/umst-concrete-cartridge/tests/shell_topology_rib_pattern.rs` — **`shell_topology_rib_pattern_full_v04`** (brief / **200** outers): **40 × 40 × 4**, seed **42**, end volume fraction **±1 %** of `target_vf = 0.15`; **volume** greyness **mean(4ρ(1−ρ)) < 0.15**; spatial variance of **ρ** over **xy** **> 0.1**; compliance at end **<** compliance at iter **1 × 0.6**. **`shell_topology_rib_pattern_quick`** (default CI): **9×8×2** default grid, **Heaviside** top-slice variance **> 2×10⁻⁵**, finite Heaviside greyness, **`c1 ≤ 1.45·c0`**, VF **±15%** — see **Track B6** table. |

**Track B8 (brief “all of”):** (1) `shell_demo_smoke` passes; (2) `shell_topology_rib_pattern` at **40 × 40 × 4**; (3) GIF shows rib formation (subjective; objective backup: variance/genus checks); (4) **`striatus_shell_v0.4.print_ready.json`** has **`genus ≥ 1`** AND **`variance ≥ 0.1`** AND **nodal `mean(ρ)` ∈ [0.10, 0.25]** (see **`gate_volume_fraction_mesh_b7`**; **`mesh_volume_fraction_in_bbox`** is diagnostic) AND rollup boolean **`gates_track_b8_all_pass`** (see **[P0 runbook](#p0-runbook-b6)** / **`export_print_ready.py`**).


## Track J — documentation honesty (v0.4 brief)

Cross-check: **[`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md)** — Track J in that file.

- **J1.** This document’s table is the public solver ↔ lane ↔ verification mapping; prose uses “implemented” only where the **Verification** column matches the CI rule in the header.
- **J2.** [`README.md`](../README.md) summarizes solver maturity and links here plus the v0.4 brief at [`../../composer_prompts/v0.4_solver_completion_no_namesakes.md`](../../composer_prompts/v0.4_solver_completion_no_namesakes.md) (correct relative URL from this `docs/` file when `composer_prompts/` sits beside `umst-manifold/` in the parent workspace). [`README.md`](../README.md) uses `../composer_prompts/...` for that brief because it is one directory shallower.
- **J3.** Short index: [`PROOF-STATUS.md`](PROOF-STATUS.md) — one row per main-table solver with **`lane`**, **`benchmark_test`**, and **`verification_status`** (`mechanised` \| `analytic-benchmark` \| `literature` \| `none`); stable rows must not leave **`benchmark_test`** empty. Extended verification notes and research-track pointers stay in this file.
- **J4.** [`References.bib`](References.bib) holds a Track-J bibliography skeleton (Bendsøe–Sigmund, Allaire, Sigmund 1997, Stolpe–Svanberg, Scharfetter–Gummel, Buckingham, Roussel et al., Manzano et al., CEB-FIP Model Code 2010, Wangler et al.); extend with DOIs and page-accurate metadata as solver rustdoc cites them.

