# Proof / verification status (solver index)

Authoritative solver–lane–verification mapping and long-form notes live in **[`Solver-Status.md`](Solver-Status.md)** (same directory). This table is a **short index** (Track J3): one row per solver in that document’s main table. **OPEN ROADMAP ITEM** sections and narrative limits are not duplicated here.

**Columns (Track J):** `lane`, `benchmark_test` (path(s) to `tests/**/*.rs` exercised on CI for the claim), `verification_status` ∈ {`mechanised`, `analytic-benchmark`, `literature`, `none`}. **Reject** a row with `lane = stable` and empty `benchmark_test`. Formal citations for solver rustdoc accumulate in **[`References.bib`](References.bib)** (Track J4).

**`verification_status` honesty:** `mechanised` here means **VALIDATED integration tests on the stated CI path** — not a Lean solver witness or formal proof obligation on the hot path. See [`Solver-Status.md`](Solver-Status.md) for scope.

CI lint for the full table: `python3 scripts/check_solver_status.py --check-paths --check-memo-links --check-statmech-verification-set` (from `umst-manifold/`; validates [`Solver-Status.md`](Solver-Status.md)).

| solver | lane | benchmark_test | verification_status | notes |
| ------ | ---- | ---------------- | --------------------- | ----- |
| `solvers::topology_solver` | stable | `tests/topology_continuation.rs`, `tests/topology_filter.rs` | analytic-benchmark | Heat/SIMP density evolution + Helmholtz filter smokes on **`default`** / **`solver-stable`** CI — integration tests only, not Lean topology witness. |
| `mechanics::VectorMechanicsSolver`, `adjoint::AdjointCompliance`, `adjoint_q1_hex::AdjointComplianceQ1Hex` | research | `tests/verification/mechanics_analytic.rs`, `tests/verification/adjoint_compliance_analytic.rs`, `tests/verification/adjoint_q1_hex_compliance_analytic.rs`, `tests/verification/adjoint_q1_hex_matches_bar_in_limit.rs` | analytic-benchmark | Bar/plate + discrete adjoint; Q1-hex adjoint on **`solver-research`** / **`research-main`**. Kirchhoff **R2.1-A** green @ 38²; **R2.1-B** brick gate open — see full table. Coupled THMC/fracture paths still use bar-network mechanics (Wave 3). |
| `solvers::fracture_field` (`PhaseFieldFractureSolver`) | research | `tests/verification/fracture_gamma_convergence.rs`, `tests/verification/staggered_fracture_mechanics_chain.rs`, `tests/verification/staggered_ud_loop_milestone.rs`, `tests/verification/thmc_drying_shrinkage.rs` | analytic-benchmark | AT2 + partial Γ-type harnesses; THMC kinematic parity. |
| `solvers::acoustics` | research | `tests/verification/acoustics_plane_wave.rs` | analytic-benchmark | **1-D periodic bar** Newmark vs dense ref; return-map at **n = 100** and **`plane_wave_return_map_n128_l2_within_two_percent`** (n = 128). Graph **`AcousticWaveSolver`** not certified — see full table. |
| `solvers::electrochemistry` | research | `tests/verification/pnp_debye_layer.rs` | analytic-benchmark | Picard + opt-in MVP-chain implicit Newton; **λ_D** screening-length LS gates on N=256 chains run in **`pnp_debye_layer.rs`** on **`research-main`** (linearized SG / Debye–Hückel limit). |
| `solvers::photonics` (`PhotonicsSolver`, `PhotonicsHelmholtzSolver`) | research | `tests/verification/photonics_fresnel.rs`, `tests/verification/photonics_curl_curl_2d_patch.rs`, `tests/verification/photonics_curl_curl_3d_brick.rs`, `tests/verification/photonics_curl_curl_stub_default_build.rs` | analytic-benchmark | TE Helmholtz / 1-D curl–curl; 2D/3D DEC patch tests on **`phase4-pr`**; production DEC + Krylov open. |
| `solvers::rheology_flow` (`BinghamFlowSolver`) | research | `tests/verification/rheology_poiseuille.rs` | analytic-benchmark | **Bingham only** (no Herschel–Bulkley); analytic / Chorin smoke; developed-channel steady L² in **`#[ignore]`**. |
| `solvers::thmc` (`ThmcSolver`, …) | research | `tests/verification/thmc_drying_shrinkage.rs`, `tests/verification/thmc_monolithic_newton_chain.rs` | analytic-benchmark | Drying/shrinkage; implicit **(T, α)** block; stacked **(T, h, α, u)** monolithic dense Newton **partially closed** at **≤ 64** DOFs (`phase4-pr`). Large-N monolith / residual-exit honesty — Wave 1. |
| `solvers::statistical_mechanics` | research | `tests/verification/statmech_vinet_eos.rs`, `tests/verification/statmech_lj_bridge_contract.rs`, `tests/verification/statmech_lj_johnson_eos_reference.rs`, `tests/verification/statmech_lj_johnson_upscale_bridge.rs`, `tests/verification/statmech_mechanics_fracture_bridge.rs` | analytic-benchmark | Vinet EOS; LJ bridge; **f64** Johnson reference; **`[B,4]` upscale = virial surrogate** (not Johnson **K** in Burn). **P0:** `statmech_mechanics_fracture_bridge.rs` cited here and in `check_solver_status.py` — confirm `Cargo.toml` **`[[test]]`** registration (fracture audit). |
